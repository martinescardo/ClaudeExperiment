---
name: dialogue-tree-height-project
description: "State of the effort to prove Escardó's conjecture that System T dialogue-tree heights are < ε₀"
metadata: 
  node_type: memory
  type: project
  originSessionId: 2a82e525-8643-468f-8681-ff604cde3dc6
---

Effort (through 2026-06) on Escardó's MFPS-2013 conjecture: the **height** (ordinal rank) of the dialogue tree of a System T term `t : (ι⇒ι)⇒ι` is `< ε₀`. All artefacts live in `source/EffectfulForcing/`.

**Status: the conjecture is OPEN.** It was not proved. What exists:

- **Canonical exposition:** `dialogue-tree-height.md` — read this first. Elegant account; central idea is "**depth cannot see itself**": a term reads/writes oracle *answers* but cannot read the *tree* (the transcript), so iteration counts are values never the current depth ⟹ the height-transform is linear ⟹ ε₀ would emerge as `sup_ℓ ω↑↑ℓ` (one ω per type level). The other `dialogue-tree-height-*.md` are superseded working notes (banners point to the canonical one); `-correct-framing.md` is the attribution-correction record.

- **REFACTOR 2026-07-08 — pure ordinal arithmetic extracted to a standalone library `source/BrouwerOrdinals/` (namespace `BrouwerOrdinals.*`), built via `agda BrouwerOrdinals/index.lagda`, conjecture-free (no Dialogue/System-T/majorant deps; keeps only the generic `Combinators.iter`).** 16 modules: `Order` (≤, ⊕ — split out of `Constructive`), `Orbit` (ω, ⊗, ⊕-mono/assoc, orbit-sup engine — split out of `DialogueTreeHeight.Orbit`), `Epsilon0`, `OmegaPoly`, `CNF`, `CNFAffine`, `MultBump`, `MultDominated`, `Affine`, `Affine2`, `AffineClosure`, `AffineOrbit`, `MultAffine`, `MultOrbit`, `MultSquareOrbit` + `index`. **The two splits:** `DialogueTreeHeight.Constructive` now keeps only `height`/`height-*` and does `open import BrouwerOrdinals.Order fe public`; `DialogueTreeHeight.Orbit` keeps only `height-iter-from-step` and does `open import BrouwerOrdinals.Orbit fe public` — so conjecture-side modules importing Constructive/Orbit for ≤/⊕/ω/⊗/engine are UNCHANGED (public re-export). Conjecture modules that used the 13 moved modules now import `BrouwerOrdinals.<M>`. So: **if a future session can't find Epsilon0/Affine/MultBump/etc. in DialogueTreeHeight, they're in `BrouwerOrdinals/`.** Both indexes build EXIT 0; nothing committed.
- **Machine-checked Agda** — all in `source/EffectfulForcing/DialogueTreeHeight/` (moved there 2026-06-29 from `MFPSAndVariations/`; modules `EffectfulForcing.DialogueTreeHeight.<Leaf>`, leaves dropped the `DialogueTreeHeight` prefix: classical base is now `Classical`, others `Constructive`/`Count`/`Operator`/`Conditional`/`Orbit`/`Magnitude`/`Epsilon0`/`FirstOrder`/`Majorant`/`Affine`/`Index`). The 9 markdown notes moved there too. All `--safe`, no postulates; tour = `DialogueTreeHeight.Index`):
  - `DialogueTreeHeight` (classical, ordinals, needs EM) and `…Constructive` (Brouwer codes, no EM): the height calculus L1–L3.
  - `…Count`: the Count Lemma (counts are leaf values; bounded count ⟹ bounded iteration).
  - `…Operator`: the operator `H`, composition law, and recursor identity `height(iter' f x n) = H(λm. height(fᵐ x)) n`.
  - `…Conditional`: *given* an orbit bound, the recursor height is bounded (the bound is a hypothesis, not a postulate).
  - `…Orbit` (added 2026-06-29): the orbit-sup **engine** of bridge lemma (B). Adds minimal Brouwer-code arithmetic (`ι[_]`, `ω`, `_⊗_`, `⊕-mono-left`, `⊕-assoc`) and proves: a *fixed* per-step increment `step : height(fᵏ⁺¹x) ≤ height(fᵏx) ⊕ c` (depth-independent!) forces `orbit-sup-≤ : L a ≤ height x ⊕ (c ⊗ ω)` and `height-iter-from-step : height(iter' f x n) ≤ (height x ⊕ c⊗ω) ⊕ height n`. This is "depth cannot bootstrap" as a theorem: a depth-independent increment costs one factor of ω, never an exponential. `c` is an explicit hypothesis (for first-order f, `c = ω^ω`). All `--safe --without-K`, EXIT 0, in the Index tour.
  - `…Magnitude` (added 2026-06-29): the **value-side**. Oracle-relative magnitude `mag-≤ V x := ∀ b α, α≤[b] → dialogue x α ≤ V b`; propagation `mag-η/mag-succ'/mag-generic/mag-kleisli`. `mag-generic` = oracle resets magnitude to `λb.b`; `mag-kleisli` realises **Lemma R1** (count `dialogue n α ≤ V b`). Self-contained over `Dialogue`.
  - `…Epsilon0` (added 2026-06-29): **sub-development (I) COMPLETE** — the ε₀ ordinal-arithmetic library on Brouwer codes. `ω^_`, `tower`, `ε₀ = L tower`, `_<_`; full monotonicity toolkit (`⊕`,`⊗`,`ω^_`); inflationary `a ≤ ω^a`; strict `ω<ω^ω` and `tower n < ε₀` (descent bottoming at `ω<ω^ω`, dissolving the successor-of-limit obstruction); the key absorption `ω^ω⊗ω-<-ε₀ : ω^ω ⊗ ω < ε₀`; and additive closure `⊕-<-ε₀`. Pure ordinal arithmetic, EXIT 0. **(added 2026-07-01)** two exponentiation closures `S-<-ε₀ : a<ε₀→S a<ε₀` and `ω^-<-ε₀ : a<ε₀→ω^ a<ε₀` (ε₀ closed under ω-exponentiation, via `a<tower n ⟹ ω^a≤ω^(tower n)=tower(succ n)<ε₀`) — needed to let the tower-step exponent be an ordinal, not a numeral.
  - `…FirstOrder` (added 2026-06-29): **capstone** `height-iter-<-ε₀` — the first-order recursor height bound `height(iter' f x n) < ε₀`, GIVEN (per-step increment c=ω^ω) ∧ (height x < ε₀) ∧ (height n < ε₀). Pure assembly of `…Orbit` + `…Epsilon0`. EXIT 0.
  - `…Affine` (added 2026-06-29): toward `μ t < ε₀`. Proves `⊗-left-distrib` (LEFT-distributivity holds), `affine-fold : x⊗ι[p+1]⊕d ≤ (x⊕d)⊗ι[p+1]` (THE key lemma), `⊕-increasing-left`, `⊗ω-<-ε₀ : a<ε₀ → a⊗ω<ε₀`, and the multiplicative orbit `double-orbit-<-ε₀ : a<ε₀ → L(λk iter (λb.b⊕b) a k) < ε₀` (≤ a⊗ω). **KEY FINDING: the affine-composition obstruction is NOT fundamental.** RIGHT-distributivity `(x⊕y)⊗p ≤ x⊗p⊕y⊗p` is FALSE for non-commutative ordinal ⊗ (e.g. (1⊕ω)⊗2). BUT keeping the constant INSIDE — form `(b⊕d)⊗ι[p]` not `b⊗ι[p]⊕d` — makes affine maps compose via `affine-fold` + ⊗-assoc + LEFT-distrib, NO Hessenberg natural sum needed. So `μ t < ε₀` is reachable in plain Brouwer-code arithmetic. **The general affine-orbit bound is now PROVED** (added later 2026-06-29): `affine-orbit-<-ε₀` — for affine `φ b ≤ (b⊕d)⊗ι[succ p]` with `d<ε₀` and `a<ε₀`, `L(λk iter φ a k) ≤ (a⊕d⊗ω)⊗ω < ε₀`. Supporting: `⊗-assoc`, `_·ℕ_`, `ι-*-homo`, `affine-fold-num`, `Qpow`/`Qpow-pos`, `one-≤-⊗`. This is exactly what `μ-Iter` consumes. REMAINING for `μ t < ε₀`: only the **hereditary affine closure** — that `μ F` is affine for every first-order `F : ι⇒ι`.
  - `…AffineClosure` (added 2026-06-30): the **affine-closure toolkit**, no postulates, EXIT 0, in tour (13 modules now). `Aff φ = Σ m d, (S Z ≤ ι[m]) × (d<ε₀) × (∀b. φ b ≤ (b⊕d)⊗ι[m])` (positive numeral multiplier, constant inside — cleaner than ι[succ p], avoids succ-extraction in closures). Proves: `Aff-id`, `Aff-S`, `const-Aff` (K), `const-left-Aff` (λν.C⊕ν), `Aff-Iter-partial` (recursor partial app, via affine-orbit), `Aff-∘` (composition), `Aff-⊕` (pointwise sum), `Aff-app` (apply to <ε₀ → <ε₀). Helpers `SZ-⊗ : (S Z)⊗y ＝ y`, `x-≤-x⊗`, `y-≤-⊗`, `ι<ε₀`, `⊗ι-<-ε₀`.
  - **The precise remaining obstacle (worked out 2026-06-30):** the hereditary predicate needs ground arguments to have a *uniform joint* affine bound (`φ a b ≤ (a⊕b⊕d)⊗ι[m]`, uniform d,m — the curried `(a)→Aff(φ a)` is too weak, data varies with a, so the S-diagonal `λa.φ a (γ a)` can't be bounded), BUT function arguments need a *data-dependent transformer* (result bound depends on the arg's affine data — e.g. `μ-Iter g` depends on g). These conflict in a single accumulator: a fixed accumulator can't handle a function arg applied k-times (degree mg^k, k unknown), and the transformer doesn't give joint-uniformity for ground args. FURTHER: `Iter₁·G : ι⇒ι⇒ι` is **ω-affine in its start argument** (orbit sup ≤ (a⊕dg⊗ω)⊗ω, ω-coefficient not finite ι[m]), so the affine class must also allow ω (and ω^k) multipliers, not just finite ι[m]. This is genuinely Howard's hereditarily-majorizable functionals + a general (≤ε₀) multiplier semiring — a multi-day formalization, NOT quick bookkeeping. The toolkit above is the maximal clean sub-result.
  - `…Hereditary` (added 2026-06-30): the **hereditary predicate `𝔅`**, defined & compiling, no postulates, in tour (14 modules). Resolves joint/transformer tension: `𝔅 σ φ = (fa : FunArgs σ) → JointAff (GroundType σ) (plug σ φ fa)` — function args ∀-quantified (`FunArgs`/`plug`, carrying their 𝔅), ground args bounded *jointly* relative to a *common upper bound* `s` (`JB σ φ s d m`: each ground arg ≤ s, result ≤ (s⊕d)⊗ι[m]). Common `s` ⟹ symmetric ⟹ combine via associativity not commutativity. Mutual `FunArgs`/`plug`/`𝔅` recursion accepted. PROVED: ground extraction `𝔅ι-to-<ε₀`/`<ε₀-to-𝔅ι` (`𝔅 ι a ⟺ a<ε₀`), `JB-shift` (absorb ground arg into constant via assoc), `𝔅-app` (application closure!), `𝔅-Zero/Succ/Ω`. REMAINING: `𝔅-K`, `𝔅-S` (polymorphic type-inductions), `𝔅-Iter`, fundamental theorem.
  - **DEFINITIVE CEILING FINDING (2026-06-30):** the finite-`ι[m]`-multiplier affine approach caps at `≈ ω^ω`; it CANNOT reach `ε₀`. Reason: nested iteration *from a varying start* — e.g. `Iter (λx. Iter G x M) X N`, which IS first-order (both Iter at ι) — makes the iterated function `ω`-affine (`λx. Iter G x N` ↦ orbit-over-start, `(x⊕d)⊗ω`), and iterating it climbs the tower (ω^ω, ω↑↑n…). Bounding this needs `ω^c` multipliers. BUT `affine-fold` (`x⊗c⊕d ≤ (x⊕d)⊗c`) provably FAILS for limit multipliers `c` (e.g. c=ω): `(L g)⊕d` cannot be absorbed into a single `≤-ℓ` witness when `g`'s base is a limit. So the "constant-inside + affine-fold" trick (which avoided natural sum) does NOT extend to ω. **Full first-order ε₀ genuinely requires the `ω^c`-multiplier affine class closed under Hessenberg natural sum — Part II's actual route — which is the substantial separate development.** The finite-multiplier toolkit + hereditary predicate built here is the clean sub-ε₀ (≈ω^ω) fragment; the tower to ε₀ is fast-growing-hierarchy territory.
  - `…OmegaPoly` (added 2026-06-30): **the natural sum, BUILT** (no postulates, EXIT 0, 15 modules). The affine-fold counterexample (`ω·ω+1 = ω²+1 > ω² = (ω+1)·ω`) means Hessenberg natural sum is REQUIRED. For first-order (<ω^ω), ordinals are **ω-polynomials** `Poly = List ℕ` (little-endian coeffs), natural sum `_⊞_` = coefficient-wise `+ℕ` (manifestly commutative, `⊞-comm`). Horner denotation `⟦ c ∷ cs ⟧ = (ω ⊗ ⟦cs⟧) ⊕ ι[c]` into Brouwer codes. KEY LEMMA `poly-key : ⟦p⟧⊕⟦q⟧ ≤ ⟦p⊞q⟧` and `poly-key' : ⟦q⟧⊕⟦p⟧ ≤ ⟦p⊞q⟧` (ordinary sum ≤ natural sum, BOTH orders = the commutative upper bound the non-commutative ⊕ couldn't give). Crux sublemma `absorb : ι[c]⊕(ω⊗Y) ≤ (ω⊗Y)⊕ι[c]` (finite absorbed by ω-power, re-emerges right) + `fin-ω`. This is the foundation that unblocks the ω^k-multiplier reordering. **NOW CONCLUDES <ε₀** (added 2026-06-30): `poly-<-ε₀ : ⟦p⟧ < ε₀` (every ω-poly < ε₀), `⊞-<-ε₀`, via the exponent homomorphism `ω^⊗ : ω^a⊗ω^b ＝ ω^(a⊕b)` + `S⊕≤S` (1+β≤β+1) + `ω⊗tower n ≤ tower(succ n)` → `ω⊗-<-ε₀`. So the natural-sum route genuinely concludes <ε₀, not just upper bounds. **RIGHT-DISTRIBUTING SCALAR MULTIPLICATION BUILT** (added 2026-06-30): "multiply by ω^k" = `shift k` (prepend k zeros); `shift-⊞ : shift k (p⊞q) ＝ shift k p ⊞ shift k q` (distributes over natural sum — the exact thing affine-fold couldn't do for ω!), `shift-shift` (composes, exponents add via +ℕ), `shift-pow : ⟦shift k p⟧ ＝ ω^ι[k]⊗⟦p⟧` (bridge to Brouwer-code height bounds). So the **ARITHMETIC foundation for first-order <ε₀ is now COMPLETE**: commutative sum ⊞ + commutative bound poly-key + <ε₀ conclusion poly-<-ε₀ + distributing/composing ω^k scalar shift. REMAINING for first-order <ε₀: the **hereditary STRUCTURE** (Howard's majorizability over function types — the joint/transformer tension, the K/S/Iter closure) — this is INDEPENDENT of the arithmetic and is the persistent hard wall (see `…Hereditary` for the predicate with app+base closure proved). The arithmetic no longer blocks it.
  - `…PolyAffine` (added 2026-07-01, 18 modules, no postulates): **AFFINE COMPOSITION UNBLOCKED**. Old affine class `f b ≤ (b⊕d)⊗ω^k` did NOT compose (needs affine-fold, false for ω). Fix: carry the bound as a POLYNOMIAL — `PAff k d f := ∀ pb, b≤⟦pb⟧ → f b ≤ ⟦(shift k pb) ⊞ d⟧` (input scaled by ω^k via shift, + constant poly d, commutative ⊞). `PAff-∘` PROVES composition closes: `PAff k d ∘ PAff k' e = PAff (k+k') (shift k e ⊞ d)`, by pure poly algebra (shift-⊞, shift-shift, ⊞-assoc). This is the K/S building block non-commutative ⊕/⊗ couldn't give. Plus `PAff-id`, `PAff-S`, payoff `PAff-<-ε₀`; helpers `+ℕ-assoc`, `⊞-assoc`, `⊞-[]`. **ORBIT LEMMA DISCHARGED for poly-affine transforms** (added 2026-07-01): `poly-family-<-ε₀ : (P : ℕ→Poly) → L(λj ⟦P j⟧) < ε₀` (ANY recursively-presented family of ω-polys has sup <ε₀ — the first-order orbit lemma in clean form), resting on the STRICT degree bound `poly-str : S⟦p⟧ ≤ ω^ι[length p]` (naive `⟦p⟧≤ω^ι[length]` overflows at ⊕ι[c]; the S-version works) → `poly-≤-tower1 : ⟦p⟧ ≤ tower 1 = ω^ω`. Then `PAff-orbit : PAff f → b≤⟦pb⟧ → L(λj. iter f b j) < ε₀` — the orbit of a poly-affine height-transform is <ε₀ (each iterate stays poly-bounded via q↦shift k q ⊞ d). So the (first-order) orbit lemma — the thing the conjecture REDUCES to — is now machine-checked for every poly-affine transform, with PAff-∘ giving combinator closure. **REDUCED TO REAL DIALOGUE HEIGHT** (added 2026-07-01): `transform-orbit : (f : B ℕ→B ℕ)(f̂ : 𝓑→𝓑) → PAff f̂ → (∀ y, height(f y) ≤ f̂(height y)) → height x ≤ ⟦px⟧ → L(λk. height(iter f x k)) < ε₀`. So the conjecture's ORBIT LEMMA is reduced EXACTLY to: the height-transform of the iterated tree-function is bounded by a poly-affine map (= the machine-checked "depth cannot see itself" for f). No monotonicity needed (PAff bounds f̂ b for every b below a poly). UNCONDITIONAL instance `generic-orbit`: iterating the oracle `generic` has orbit height <ε₀ (htrans = height-generic, f̂ = λa.Sa = PAff-S). Base leaves PAff-id/PAff-S/PAff-const. REMAINING for first-order: prove `∀ y, height(F y) ≤ F̂(height y)` with F̂ poly-affine, for every first-order combinator F (the "htrans" hypothesis) — hereditarily through K/S/Iter. This is the structural test; the orbit machinery below it is now DONE. REMAINING for FULL: (a) higher type levels need the height-transform's PAff coefficients to be magnitude-dependent (two-component) reaching CNF beyond ω-polys; (b) bridge (B1) — that the height-transform IS poly/CNF-affine hereditarily — remains open mathematics.

**ADDITIVE-CLASS finding (2026-07-01):** Explored the "linear/additive" route. Added (PolyAffine): `PAff-add` (λa. a⊕⟦c⟧ is PAff), `Additive f c := ∀y height(f y) ≤ height y ⊕ ⟦c⟧`, `add-∘` (composites: increments add via ⊞, the COMMUTATIVE natural sum), `add-orbit` (additive transform ⟹ orbit <ε₀ via transform-orbit). So EVERY finite composite of constant-increment (oracle/relabel) tree operations has orbit <ε₀ unconditionally. KEY INSIGHT confirmed: first-order iterated ι⇒ι functions all have ADDITIVE height-transforms (fixed constant increment) — even nested start-varying Iter (increment = inner-orbit-const, e.g. g-incr⊗ω, still FIXED, giving ω² etc.), and count-varying Iter (constant transform). The magnitude-dependence is absorbed into the fixed orbit constant. This is the "depth cannot see itself ⟹ LINEAR" made concrete. WALL RE-CONFIRMED (why hereditary K/S is genuinely hard, not just arithmetic): the left-additive `D⊕s` and additive classes FAIL for MULTI-ARGUMENT combinators — μ-Iter uses two ground args (a,ν) both ≤ s, giving `s⊕s` which exceeds any `D⊕s` (needs s≤D, impossible for variable s). The Hereditary `(s⊕d)⊗ι[m]` multiplier absorbs this multiplicity (s⊕s ≤ (s⊕d)⊗ι[2]); left-additive/⊕ cannot. So the multiplier (or natural sum for reordering) is ESSENTIAL for the joint multi-arg bound, and the K/S closure over arbitrary types (GroundType/plug combinatorics) is the real structural wall — independent of the orbit machinery (which is now DONE for the single-threaded/additive case).

**POLY-BOUNDED ORBIT / recursive enabler (2026-07-01):** `add-orbit-poly : Additive f c → height x ≤ ⟦px⟧ → L(λk height(iter f x k)) ≤ ω^ι[maxℕ(length px)(length c)]`. The additive orbit stays POLY-BOUNDED (fixed degree, only coefficients grow) — bounded by the FIXED poly ω^ι[L], L=max degree. Proved via: length-⊞ (length(p⊞q)=maxℕ), length-orbit (all iterates have length ≤ L), poly-str (S⟦q⟧≤ω^ι[length q]), ι-mono-ℕ, maxℕ-lub. THIS IS THE TOWER MECHANISM: a nested recursor's orbit is again a poly bound (ω^ι[L]) that feeds the next level as the increment constant → ω,ω²,...,<ω^ω. Combined with the ⊞-based multi-arg joint bound (⟦ps⟧⊕⟦ps⟧≤⟦ps⊞ps⟧, commutative), this suggests the RIGHT hereditary design is poly-affine-with-⊞ (natural sum resolves BOTH the ω-multiplier composition AND the multi-arg joint bound that the finite-ι[m] Hereditary.JointAff cannot). **ORBIT TOOLKIT COMPLETE at majorant level (2026-07-01):** `poly-map-orbit : (g:𝓑→𝓑)(dg:Poly) → (∀ b pb, b≤⟦pb⟧ → g b ≤ ⟦pb ⊞ dg⟧) → a≤⟦pa⟧ → L(λj iter g a j) ≤ ω^ι[maxℕ(length pa)(length dg)]` (abstract ADDITIVE map, poly-bounded orbit). And `poly-map-orbit-mult` — an iterated majorant that MULTIPLIES (multiplicity k, `g b ≤ ⟦nmul k pb ⊞ dg⟧`, arises via S using its arg k times) STILL has poly-bounded orbit ≤ ω^ι[L], because `nmul k` (k copies via ⊞) scales COEFFICIENTS not DEGREE (length(nmul k q) ≤ length q). Helpers: `nmul`, `≤ℕ-trans`, `length-nmul-≤`, `length-orbit-mult`. So the recursor orbit is POLYNOMIAL for EVERY first-order iterated majorant (additive or multiplicative) — the recursor's core lemma is DONE. NEXT: build the ⊞-based hereditary predicate (JointAffp: JBp ι a ps k d = a ≤ ⟦nmul k ps ⊞ d⟧, multiplicity k for the S double-use; JBp(ι⇒τ) quantifies x≤⟦ps⟧; JointAffp = Σ k d, ∀ps JBp) with FunArgs/plug/𝔅p mutual, ground extraction (poly-bounded ⟺ 𝔅pι), and prove Zero/Succ/Ω/app/Iter (Iter uses poly-map-orbit-mult) + K/S. The orbit (poly-map-orbit-mult) and multi-arg joint bound (poly-key/⊞) pieces are READY; remaining risk = K/S GroundType/plug type-combinatorics + multiplicity bookkeeping.
  - `…Nested` (added 2026-07-01, 22 modules, no postulates, EXIT 0, in tour): **(B1) ATTACKED DIRECTLY — per-step increment PROVEN from grafting, not assumed.** (B) splits into (B1) "increment ≤ ω^e" + (B2) "orbit <ε₀" (done). Everywhere prior, (B1) is a HYPOTHESIS (Orbit/Bridge/Conditional take the per-step increment as a function arg). This module DISCHARGES it for the tower-driving shape: a body `f y = kleisli-extension g y` (graft family `g`; for a nested recursor `g = iter h z` = inner orbit). NEW: the **LEFT-increment orbit engine** (companion to Orbit's RIGHT engine — `⊕` non-commutative so genuinely different): `b⊗-succ-swap : b⊕(b⊗ι[k]) ≤ b⊗ι[succ k]` (b·(1+k)≤b·(k+1), inductive since ⊕ non-comm) + `orbit-left-≤/sup-≤/uniform` — a fixed LEFT increment `b` per step forces `L a ≤ (b⊗ω)⊕a₀`, one factor of ω. The increment `b = sup_k height(g k)` is READ OFF `height-kleisli-extension` (the machine-checked "depth cannot see itself": grafts `g k` chosen without seeing `y`'s depth, so add fixed `b` on the LEFT regardless of `height y`). Payoffs: `kleisli-orbit-uniform`, `height-nested-iter` (`≤ ((b⊗ω)⊕height x)⊕height n`), `height-nested-iter-<-ε₀` (graft heights ≤ω^e ⟹ nested iter <ε₀, using `b⊗ω=ω^e⊗ω=ω^(S e)` DEFINITIONALLY + S-<-ε₀/ω^-<-ε₀/⊕-<-ε₀). **So ONE recursor nesting costs exactly ONE ω-power, PROVEN.** Converts (B1)'s assumed increment into the recursive GRAFT-HEIGHT bound `height(g k) ≤ ω^e` — for a nested recursor that's the INNER orbit bound = the recursive structure. **AND IT COMPOSES (added same session):** `nested-orbit-uniform-<-ε₀` generalizes the graft bound to ANY `c<ε₀` and shows the uniform orbit bound `(c⊗ω)⊕height x` is AGAIN `<ε₀` (via `⊗-<-ε₀` + `ω<ε₀=tower-<-ε₀ 0` + `⊕-<-ε₀`), so a grafting body's orbit can serve as the graft family of an OUTER nesting — the class of grafting bodies with uniformly-<ε₀ graft families is CLOSED under the nested-recursor step (`height-nested-iter-<-ε₀-gen`). **These lemmas apply to a GENUINE term:** `⟦Iter (λm. Iter H Z m) X N⟧` interprets (via `MFPS-XXIX.iter' f x = Kleisli-extension (iter f x)`, ground case `= kleisli-extension`) to EXACTLY `kleisli-extension (iter (λy. kleisli-extension (iter ĥ ẑ) y) x) n`, graft family = inner orbit `iter ĥ ẑ`. So the canonical tower-climbing first-order term (nested recursor on the bound variable) is handled, everything PROVEN except the base graft bound = the recursive instance. RESIDUAL of (B1): the **syntactic bridge** — that a System T body's dialogue interpretation has this grafting-body shape hereditarily, grounding `c` in the magnitude (Howard (A)/Count Lemma). Now reduced from an assumed per-step increment to that structural/syntactic step. (Substantial next piece: a logical relation over `T₁` threading the grafting shape.)
  - `…Applicative` (added 2026-07-01, 23 modules, no postulates, EXIT 0, in tour): **THE FIRST UNCONDITIONAL `height < ε₀` THEOREM** — for a recursor-closed fragment, NO hypotheses. Everything prior bounds height by ε₀ only CONDITIONALLY (orbit bound / per-step increment / graft bound / majorant affine shape). Here `height-<-ε₀ : (x : Gnd) → height ⟦x⟧G < ε₀` is unconditional. Fragment = **single-argument first-order** (extended same session): two mutual sorts `Gnd` (ι: `zeroG`, `appG f x`) and `Fun` (ι⇒ι: `ΩF`, `succF`, `iterF f x`=partial recursor, **`compF f g`**=composition, **`constF x`**=constant/ground-K). Every function is ι⇒ι hence in the SINGLE-ARG affine class `Aff`; the ONLY thing excluded is the `S` DIAGONAL `λa. φ a (γ a)` (which forces the multi-arg joint bound = the persistent wall). So the wall NEVER arises. Closures used: `Aff-Iter-partial` (recursor), `Aff-∘` (comp), `const-Aff` (const), `Aff-app` (appl). Proof: interp `⟦⟧G/⟦⟧F` into dialogue model (`iterF f x`↦`kleisli-extension (iter ⟦f⟧F ⟦x⟧G)`); majorants `μG/μF`; the crux `Aff-μF`/`good-μG` (every fn majorant Aff, every ground majorant <ε₀) — ONE clean mutual induction, recursor discharged by `AffineClosure.Aff-Iter-partial` (partial recursor is affine) + `Aff-app`; correctness `R-G`/`R-F` (height ≤ majorant, logical relation restricted to fragment), recursor case via `Conditional.height-iter-≤-orbit-bound` on orbit sup + termwise `orbit-pt`. `height-recursor-<-ε₀`: recursor with ARBITRARY ground count (e.g. oracle read `appG ΩF zeroG`, unbounded magnitude ⟹ orbit sup genuinely taken) is <ε₀. Exponents finite here (really <ω^ω). **This is the milestone: first unconditional endpoint.** OPEN beyond it: K/S (multi-arg joint bound, the hereditary wall) and the tower to ε₀ proper (higher types). This is the max the affine class alone closes.
  - `…Affine2` (added 2026-07-01, 24 modules, no postulates, EXIT 0, in tour): the **two-argument jointly-affine class — first assault on the S wall.** `Aff2 φ = Σ m d, (SZ≤ι[m])×(d<ε₀)×((s a b)→a≤s→b≤s→ φ a b ≤ (s⊕d)⊗ι[m])` (common-bound joint form, symmetric à la Hereditary). **CRUX `Aff2-diag : Aff2 φ → Aff γ → Aff (λa. φ a (γ a))`** — the ground S-combinator diagonal closes, by the SAME `affine-fold-num` technique as `Aff-∘`, common bound `s=(a⊕e)⊗ι[n]` (dominates a and γa) → `φ a (γ a) ≤ (a⊕(e⊕d))⊗ι[n·m]`. Plus `Aff2-π₁/π₂` (projections), `Aff-to-Aff2-left/right` (lift 1-arg affine to 2-arg ignoring a slot), `Aff2-app`. **HONEST SCOPE:** finite `ι[m]` multiplier only. NOT wired into `Applicative` because finite-mult 2-arg functions are only projections/lifts/sums, whose S-diagonals reduce to already-expressible terms (id, γ, ψ∘γ) — add NOTHING new. The GENUINELY new S-diagonals need `Iter f` as the 2-arg function, which is jointly affine only with an **ω-multiplier** (orbit sup `(a⊕d⊗ω)⊗ω`), beyond the `⊗`-fold (affine-fold fails for limit mult) → needs the natural-sum (OmegaPoly/CNF) route. So this is the finite-multiplier HALF of the ground-S closure. This is the structural heart of S-ground proven at finite altitude — the hereditary predicate's S-ground failure (fixes φ's fn-arg before quantifying x) is sidestepped by diagonalizing the concrete 2-arg φ. **ω-MULTIPLIER HALF (added same session):** the recursor `Iter f` as 2-arg fn is jointly affine only with an ω-mult (orbit-from-start ≤ (a⊕d⊗ω)⊗ω), which `⊗`-fold can't reach (affine-fold false for limit mult). **KEY FINDING: ω-affine functions are NOT closed under composition (two compose to ω²) — this non-closure IS the type-level tower ω↑↑ℓ = the open conjecture, NOT a patchable gap.** So no fixed-multiplier class captures start-varying recursors. What IS achievable = POINTWISE: `Good2 φ := (a b)→a<ε₀→b<ε₀→φ a b<ε₀` (no uniform mult ⟹ no composition demand) holds for recursor (`Good2-Iter` via `Aff-orbit-<-ε₀`/`affine-orbit-<-ε₀`), and `Good2-diag`/`recursor-diag-<-ε₀`: the S-diagonal `λa. Iter f a (γ a)` is <ε₀ at every <ε₀ arg. So recursor S-diagonal is bounded when APPLIED but not in a composable/iterable class — the honest ceiling of the ⊗-affine route, exactly at the tower boundary. **CONCLUSION: the Applicative affine fragment is genuinely MAXIMAL for the clean unconditional <ε₀ proof; past it (start-varying recursor as reusable fn) is tower territory = the open problem. The boundary is now machine-checked & crisp.**
  - `…CNF` (added 2026-07-02, 25 modules, no postulates, EXIT 0, in tour): **Cantor normal forms below ε₀ — the ε₀ generalization of OmegaPoly's ω-polys** (the arithmetic foundation to let composition close past ω^ω, chosen next step). Datatype `CNF` (`𝟎 | ω⟨_⟩+_`, sum of ω-powers whose exponents are recursively CNFs ⟹ all of <ε₀ reachable), denotation `⟦_⟧ : CNF → 𝓑`, headline `cnf-<-ε₀ : ⟦c⟧ < ε₀` (ε₀-closure under ω^ + ⊕). Plus `ωpow` (`⟦ωpow a⟧＝ω^⟦a⟧`, refl), ordinary non-comm sum `_⊕c_` with `⊕c-⟦⟧` (denotes ⊕). **NATURAL-SUM HEART PROVED:** `absorb-ω-succ : A≤B → ω^A ⊕ ω^(S B) ≤ ω^(S B)` (smaller ω-power absorbed on LEFT of larger), via `⊕⊗-succ` (the `Nested.b⊗-succ-swap` shape: b⊕(b⊗ι[n])≤b⊗ι[succ n]) + `ωB⊕ωBω` (limit ω^B⊗ω swallows one more ω^B on left); hence `ωpow-swap-succ` (comparable ω-powers commute up to ≤). **STRENGTHENED TO WHOLE TAILS:** `principal-succ : γ ≤ ω^B⊗ι[k] → γ⊕ω^(S B) ≤ ω^(S B)` (ω^(S B) is additively principal against anything below a finite multiple of ω^B, via `⊗-left-distrib`+`ι-+-homo`) and `absorb-tail : AllExpLeq B p → ⟦p⟧⊕ω^(S B) ≤ ω^(S B)` (a whole CNF with all exponents ≤B absorbed on LEFT of ω^(S B); `AllExpLeq`/`len`/`tail-≤` fold the len-p powers up by `⊕⊗-succ`). This is EXACTLY the merge step (move larger leading power to front past the tail below it). **KEY FINDING (constructive obstacle):** the merge realizing commutative `⊞` must COMPARE exponents; unlike OmegaPoly (exponents = list POSITIONS, freely comparable as ℕ), nested CNF exponents have **NO free constructive total order** (Brouwer-code ≤ undecidable at limits). So the EXACT sorted merge needs a decidable STRUCTURAL order (Brouwer ≤ undecidable at limits). **RESOLVED — SIDESTEP THE SORT (added 2026-07-02):** the sorted natural sum's PURPOSE is keeping leading exponent fixed while coefficients add; `tail-≤` gives that from a COMMON EXPONENT BOUND directly. `common-bound : AllExpLeq m p → AllExpLeq m q → ⟦p⟧⊕⟦q⟧ ≤ ω^m ⊗ ι[len p +ℕ len q]` — SYMMETRIC in p,q (depends only on sizes) so dominates the sum in EITHER order; `common-bound-<-ε₀` gives <ε₀. So the natural-sum-quality COMMUTATIVE bound (leading exponent fixed, coefficients add) is obtained WITHOUT a decidable comparison, by supplying the common exponent bound the affine structure already provides. The exact merge/sort is no longer needed for the composition use. **NEXT:** the CNF-affine class `CAff m d f := ∀ input-bounded-by-m-CNF, f ≤ ω^m⊗finite ⊕ d`-style, composition via common-bound (exponents share bound m = type-level ordinal), reaching ε₀; then hereditary closure. The arithmetic obstacle to composition past ω^ω is now removed. NOTE constructor is `ω⟨_⟩+_` (bracketed, to avoid clash with Epsilon0's `ω^_`).
  - `…CNFAffine` (added 2026-07-02, 26 modules, no postulates, EXIT 0, in tour): **THE AFFINE CLASS THAT COMPOSES PAST ω^ω — payoff of the CNF arithmetic.** PolyAffine's `PAff` scaled input by ω^k (k a NUMERAL, cap ω^ω); here scalar is ω^⟦m⟧ for m an ARBITRARY CNF ⟹ any multiplier <ε₀. `CAff f = Σ m d : CNF, ∀ b p, b≤⟦p⟧ → f b ≤ ⟦ωscale m p ⊕c d⟧`. `ωscale m` = multiply by ω^⟦m⟧ (adds m to every exponent via ⊕c); `ωscale-⟦⟧ : ⟦ωscale m p⟧ ＝ ω^⟦m⟧⊗⟦p⟧` (CNF `shift-pow`, via `ω^⊗`+`⊗-left-distrib`). **CRUX `CAff-∘` (composition closes):** the ⊗-fold `(x⊗M⊕d)≤(x⊕d)⊗M` is FALSE for limit M (the tower wall), but the CNF bound composes by pure algebra — `ωscale-⊕c` (scaling distributes over ⊕c), `ωscale-ωscale` (scalings compose, exponents add by ⊕c), `⊕c-assoc` — **NO fold, NO commutative merge** (composition is single-threaded; merge only for multi-arg joint bound). Base `CAff-id`/`CAff-add`/**`CAff-ωpow`** (multiply-by-ω^α is CAff for EVERY α<ε₀, past ω^ω ceiling); payoff `CAff-<-ε₀`. **KEY INSIGHT: composition needs only ORDINARY ⊕c, not the commutative merge** — so the sort/comparison obstacle was never blocking composition; it only blocks the multi-arg K/S joint bound (handled separately by `common-bound`). So the ARITHMETIC/composition side of the tower is now DONE to ε₀. **ITERATION SIDE TOO (added same session):** `left-mult-orbit-<-ε₀` — LEFT-multiplicative orbit `a(k+1) ≤ ω^m ⊗ a k` (the side ωscale produces, counterpart of MultOrbit's RIGHT engine) has sup `ω^(m⊗ω)⊗a₀ < ε₀` for m<ε₀ (k-th iterate ≤ ω^(m⊗ι[k])⊗a₀, exponent accumulates via `ω^⊗` + `CNF.⊕⊗-succ`); `CAff-ωpow-orbit-<-ε₀` (iterating multiply-by-ω^α from sub-ε₀ start stays <ε₀, step definitional). So BOTH composition AND iteration of ω-power-affine maps closed to ε₀ — arithmetic ceiling FULLY GONE. **ADDITIVE CLASS CLOSED UNDER COMP + ITER (added 2026-07-02) — KEY STRUCTURAL RESULT:** `Add f = Σ c:CNF, ∀ b, f b ≤ b ⊕ ⟦c⟧`; `Add-id`, `Add-∘` (constants add via ⊕c), **`Add-orbit` (ITERATING ADDITIVE → ADDITIVE, constant c ↦ scaleω c)**, `Add-<-ε₀`. Plus enabling arithmetic **`scaleω`** (multiply CNF by ω staying CNF: `scaleω c = ω⟨topexps c ⊕c ι2⟩+𝟎`, `scaleω-bound : ⟦c⟧⊗ω ≤ ⟦scaleω c⟧` via `topexps`/`all-exp-topexps`/`tail-≤`; `AllExpLeq-mono`). **SIGNIFICANCE: the tower's consume-vs-produce shape-mismatch (recursor consumes additive shape, produces degree-raising — no fixed shape closed under both = the tower) is RESOLVED for the additive class by the CNF arithmetic**: iterating additive stays additive (constant grows c→scaleω c but stays CNF<ε₀). So a nested recursor with additive body STAYS additive; single-threaded fragment (comp+iter, no arg duplication) FULLY CLOSED at ε₀. **THE REMAINING WALL IS NOW PURELY the multi-argument (K/S) DUPLICATION** (s⊕s needs a finite multiplier ι[k] alongside; pure additivity can't express it) — genuinely narrowed from "iteration+composition+duplication" to just "duplication". **DUPLICATION ARITHMETIC (added same session):** `nmulc k p` (k copies via ⊕c, `nmulc-≤ : ⟦nmulc k p⟧ ≤ ⟦p⟧⊗ι[k]` via `⊕⊗-succ`; `nmulc-dup : ⟦p⟧⊕⟦p⟧ ≤ ⟦nmulc 2 p⟧`). **KEY FINDING (in the proving): `nmulc` sits on the NON-COMMUTATIVE `⊕c`, so it does NOT distribute over ⊕c ⟹ does NOT itself compose** (the middle-four exchange needs commutativity). So `nmulc` gives the finite multiplicity ι[k] but the COMPOSABLE symmetric duplication bound is `CNF.common-bound` (common exponent bound → ω^m⊗finite, symmetric, no sort — already built). So the joint-bound REORDERING is common-bound's job; nmulc supplies ι[k]. **CONCLUSION: all arithmetic ATOMS for the combined class are now built** (Add-∘/Add-orbit for comp+iter, scaleω for ⊗ω, common-bound for symmetric duplication, nmulc for finite mult). **COHERENT ι⇒ι SHAPE FOUND & CLOSED (added same session): `Add±` (TWO-SIDED additive, `f b ≤ (⟦α⟧⊕b)⊕⟦β⟧`)** — contains the recursor PARTIAL `λν. orbit⊕ν` (LEFT constant; right-additive `Add` MISSES it since ⊕ non-comm, `C⊕ν ⊄ ν⊕C`) via `Add±-const-left`, and every right-additive map via `Add-to-Add±`; **closed under BOTH composition (`Add±-∘`, constants accumulate on their sides via ⊕-assoc) AND iteration (`Add±-orbit`: orbit is Add± again, both constants grown by scaleω, using `⊕⊗-succ` on EACH side; k-th iterate ≤ ((⟦α⟧⊗ι[k])⊕a)⊕(⟦β⟧⊗ι[k]))`**; `Add±-<-ε₀`. **SIGNIFICANCE: one class holds recursor-partials + oracle/relabel + composites + orbits, all <ε₀ — the ι⇒ι shape a hereditary predicate CAN carry through Iter, resolving consume-vs-produce for two-sided-additive bodies.** REMAINING structural residual: (i) multi-arg K/S joint bound (duplication — common-bound + nmulc atoms ready but need assembly into the joint predicate), (ii) ground S-diagonal. **HEREDITARY Iter STEP ASSEMBLED (added same session):** `Add±-app` (apply Add± to CNF-bounded arg → CNF-bounded result) and **`Add±-Iter-partial` : Add± g → x≤⟦px⟧ → Add±(λν. L(λk iter g x k) ⊕ ν)** — THE hereditary Iter step that CNFTransformer's abstract transformer COULDN'T take: the orbit is CNF-bounded at the FIXED start x (via Add±-orbit), fed to Add±-const-left. **So GIVEN Add± shape for the body, the recursor closes hereditarily — the shape-loss wall is GONE.** REMAINING structural residual: ONLY the multi-argument S-diagonal (`λa. φ a (γ a)`, a used twice → needs finite multiplier b-appears-k-times, which Add± (single-b) lacks; and ⊗ι[k] form doesn't compose (right-distrib fails even finite k, needs commutativity), constant-inside Aff form doesn't iterate — the combined comp+iter+dup class needs CNF/natural-sum representation = the persistent hard core). **KEY: the wall is now PINNED to exactly the S-diagonal duplication; iteration, composition, application, and the recursor-partial ALL close via Add± at ε₀.** Next: either (a) logical relation with R(ι⇒ι)=Add± closing first-order EXCEPT S-diagonal, or (b) the combined class for the S-diagonal (the hard core). **PURSUING (b) — TWO KEY LEMMAS BUILT (2026-07-02):** (1) **`CNF.<ε₀-to-cnf : (a) → a<ε₀ → Σ ac:CNF, a ≤ ⟦ac⟧`** — GENERAL BRIDGE: every <ε₀ Brouwer code is CNF-bounded (a<ε₀ ⟹ a≤tower n, tower n IS a CNF via `tower-cnf`/`tower-cnf-eq`, pattern-match the ≤-ℓ). So ALL the Brouwer-code <ε₀ machinery (incl. Aff's constants) lifts to CNF bounds. (2) **`CNFAffine.Aff-orbit-cnf : Aff φ → a≤⟦p⟧ → Σ q:CNF, L(λk iter φ a k) ≤ ⟦q⟧`** — the orbit of a DUPLICATING (finite-mult Aff) body is CNF-BOUNDED (from `Affine.affine-orbit-≤ : L ≤ (a⊕d⊗ω)⊗ω`, the <ε₀ constant d → CNF via <ε₀-to-cnf, both ⊗ω → scaleω, whole orbit lands in q = scaleω(p ⊕c scaleω dc)). **SIGNIFICANCE: the Aff class handles DUPLICATION (Aff2-diag: S-diagonal → Aff) but its orbit escaped to ω-mult (why it didn't iterate); Aff-orbit-cnf shows that's NO LONGER a ceiling — the duplicating body iterated is CNF-bounded <ε₀. So the Aff(duplication) and CNF-orbit(iteration) sides MEET.** The S-diagonal frontier's two lemma-pieces are now built (Aff2-diag for dup, Aff-orbit-cnf for iterating the dup body). **JOINED IN ONE STATEMENT + FEED-FORWARD (2026-07-02, CNFAffine, EXIT 0):** `Sdiag-orbit-cnf : Aff2 φ → Aff γ → a≤⟦p⟧ → Σ q, L(λk iter(λb.φ b(γ b)) a k) ≤ ⟦q⟧` (= `Aff-orbit-cnf ∘ Aff2-diag`: iterating a ground S-diagonal — the one op additive/CAff/Add± can't express — stays CNF-bounded) and `Sdiag-orbit-<-ε₀` (hence <ε₀, via <ε₀-to-cnf/cnf-<-ε₀/≤-S). **AND `Sdiag-nest2`: the CNF-bounded orbit of ONE duplicating recursor IS exactly the start-hypothesis the same lemma consumes**, so finitely-nested ground S-diagonal recursors stay CNF-bounded <ε₀ — the tower for the DUPLICATING case (one bounded exponentiation per nesting level, finitely many below ε₀); n-level = same threading of CNF bounds (the transformer <ε₀-code↦<ε₀-code composed with itself). **HONEST SCOPE: this covers the FINITE-multiplier Aff2 (projections/lifts/sums diagonalised) — the genuinely new S-diagonals with the recursor-as-2-arg-fn are ω-multiplier, NOT Aff2, so Sdiag-orbit-cnf doesn't reach them directly; that (start-varying recursor reused as a 2-arg fn) is where the tower proper lives.** Added Affine2 import to CNFAffine (acyclic). REMAINING for (b): the full hereditary ASSEMBLY — a logical relation / predicate where the recursor body can be an S-diagonal (Aff), using Aff2-diag + Aff-orbit-cnf, closing the FULL first-order fundamental theorem at ε₀ INCLUDING S. The hard combinatorial assembly, but both key lemmas now in hand. REMAINING: wire CAff into the height-transform (the ω-affine recursor majorant IS CAff with m=CNF for ω^k... actually start-varying recursor is ω-affine = CAff with m=ωpow), then the hereditary structural closure (K/S/Iter) over CAff — the persistent structural wall, now with adequate ε₀ arithmetic beneath it.
  - `…CNFTransformer` (added 2026-07-02, 27 modules, no postulates, EXIT 0, in tour): **the ε₀ LIFT of PolyTransformer** — hereditary predicate over a CNF transformer `T : CNF → CNF`. Reruns PolyTransformer's development (`JBpT`/`JointAffpT`/mutual `FunArgs`/`plug`/`𝔅pT`, `JBpT-app-reindex`, `𝔅pT-app/-Zero/-Succ/-Ω/-weaken/-K/-S-higher`) with `Poly ↦ CNF` and poly natural sum `⊞` replaced by ordinary CNF sum `⊕c` — which only needs to be INCREASING on each side (`cnf-≤-⊕c-left/right` via ⊕-increasing-right/left + ⊕c-⟦⟧), NOT commutative (abstract transformer composes by plain function comp). **GAIN: ground extraction `𝔅pTι-to-<ε₀` now lands at ε₀** (every `T 𝟎` is a CNF ⟹ <ε₀ via cnf-<-ε₀), where PolyTransformer capped at ω^ω. So K/S-higher/app/base close the hereditary predicate AT ε₀. Reuses `Hereditary.Maj`/`GroundType`. **RECURSOR ADDITIVE CASE NOW CLOSED (added same session):** `𝔅pT-Iter-additive` — given g additive with fixed CNF increment c (`g b ≤ b ⊕ ⟦c⟧`), `μ-Iter g` is good with CNF transformer `T p = (p ⊕c scaleω c) ⊕c p`. Orbit via `Orbit.orbit-sup-≤` (≤ a ⊕ ⟦c⟧⊗ω), and `CNFAffine.scaleω` (⟦c⟧⊗ω ≤ ⟦scaleω c⟧) makes it a CNF. Where `PolyTransformer.𝔅pT-Iter-affine` gave a degree-raising POLY transformer (cap ω^ω), this gives a genuine CNF one → recursor output reaches ε₀. **New arithmetic `CNFAffine.scaleω`** (multiply CNF by ω, staying CNF): `scaleω c = ω⟨topexps c ⊕c ι2⟩+𝟎`, `scaleω-bound : ⟦c⟧⊗ω ≤ ⟦scaleω c⟧` via `topexps` (ordinary sum of top-level exponents, `all-exp-topexps : AllExpLeq ⟦topexps c⟧ c`) + `tail-≤` (⟦c⟧≤ω^⟦topexps c⟧⊗ι[len]) → ⟦c⟧⊗ω ≤ ω^{topexps c +2}; `AllExpLeq-mono`. **STRUCTURAL RESIDUAL (unchanged, still open):** (i) ground S-diagonal (abstract transformer records fn-arg as single good majorant, not ground-context-dependent transformer); (ii) FULLY HEREDITARY Iter (needs `𝔅pT (ι⇒ι) g` to EXPOSE additive/affine shape, but it gives an abstract transformer — the shape-loss wall). What's removed: the ω^ω ceiling everywhere → ε₀. Ω uses constant CNF `ω⟨𝟎⟩+𝟎` (denotes 1); Succ = identity transformer.
  - `…MultAffine` (added 2026-07-02, 28 modules, no postulates, EXIT 0, in tour): **FIRST CRACK IN THE MULTIPLIER WALL — iterating a LIMIT-multiplier affine body.** The finite-`Aff` class iterates ONLY via `Affine.affine-fold` (`b⊗ι[m]⊕d ≤ (b⊕d)⊗ι[m]`), which is PROVABLY FALSE for a limit multiplier (`1⊗ω⊕1 = ω+1 > ω = 2⊗ω`) — this is EXACTLY why the start-varying recursor (ω-affine majorant `(a⊕d⊗ω)⊗ω`) "could not iterate" = the tower. **KEY IDEA: iterate `mbody c b = (b⊕c)⊗M` WITHOUT affine-fold** — push `⊗M` INWARD via crux `c≤Y → (Y⊕c)⊗M ≤ Y⊗M` (bound `Y⊕c ≤ Y⊕Y = Y⊗ι[2]`, absorb `ι[2]⊗M ≤ M`), for ANY positive `M` absorbing doubling (`ι[2]⊗M≤M`: every principal limit ω, ω⊗2, ω^ω...). Orbit telescopes against powers `Mᵏ` (reuses `MultOrbit.pw`): `iter (mbody c) a k ≤ (a⊕c)⊗pw M k`, sup `(a⊕c)⊗ω^(M⊗ω) < ε₀` (`mult-affine-orbit-<-ε₀`, via `pw-sup-<-ε₀`); `M=ω` instance `mult-ω-orbit-<-ε₀`. **CONNECTED TO THE RECURSOR: `recursor-start-orbit-<-ε₀`** — `Affine.affine-orbit-≤` bounds the start-varying recursor majorant `σ a = L(λj iter φ a j)` by exactly `mbody[ω](d⊗ω) a`, so ITERATING σ (nesting the recursor on its OWN running start = the canonical single-nesting tower term) is `<ε₀`, via `iter-dominate` (σ≤g pointwise, g=mbody monotone) onto the M=ω orbit. Helpers: `ιn⊗ω-≤-ω` (`ι[n]⊗ω ≤ ω`), `iter-dominate` (f≤g pointwise + g monotone ⟹ orbit domination), `pw-pos`. **SIGNIFICANCE: a limit-multiplier iteration is now BOUNDED — the affine-fold-fails-for-ω wall (the reason ω-multiplier bodies "couldn't iterate", cited across Affine/Affine2/CNFAffine) is SIDESTEPPED by inward-multiplication, reaching ω^(ω⊗ω) for the single start-varying recursor nesting.** HONEST SCOPE: closes the limit-multiplier affine BODY (one level); the full recursor S-diagonal's lower-order `⊕γa` terms and FURTHER nesting climb the tower and remain the open core. This is the genuine ω-multiplier-half advance that Affine2's `Aff-orbit-<-ε₀` note said needed the natural-sum route — done here directly in Brouwer-code arithmetic via the doubling-absorption crux instead. **NESTING COMPOSES — TOWER ARITHMETIC CLOSED (added same session 2026-07-02, EXIT 0):** the orbit of an M-body re-enters the mbody class at the RAISED multiplier `M′=ω^(M⊗ω)` — `mbody-orbit-≤ : L(λk iter (λb.(b⊕c)⊗M) a k) ≤ (a⊕c)⊗ω^(M⊗ω)` (via `≤-L-mono orbit-inv` giving `(a⊕c)⊗L(pw M)`, then `MultOrbit.pw-sup-≤`). `M′` is AGAIN a valid multiplier: positive (`one-≤-ω^`), doubling-absorbing (`dbl-ω^ : (X)→ S Z≤X → ι[2]⊗ω^(X⊗ω) ≤ ω^(X⊗ω)`, proved via `ι[2]⊗ω^(X⊗ι[k]) ≤ ω⊗ω^(X⊗ι[k]) = ω^(ι[1]⊕X⊗ι[k]) ≤ ω^(X⊗ι[k+1])` using `ω^⊗`+`ω^-ι1`+`S⊕≤S`, cofinal at index k+1), and `<ε₀` for M<ε₀. So the LIMIT-MULTIPLIER CLASS IS CLOSED UNDER TAKING ORBITS, each recursor-nesting level raising `M↦ω^(M⊗ω)`. Explicit **multiplier tower `Mult`** (`Mult 0=ω`, `Mult(s+1)=ω^(Mult s⊗ω)`) with `Mult-pos`/`Mult-dbl`/`Mult-<-ε₀` (every finite level a valid <ε₀ multiplier) ⟹ `mult-tower-orbit-<-ε₀` (limit-mult orbit at ANY finite tower level is <ε₀), and `nest2-<-ε₀` (composed two-level nesting <ε₀ at M′=ω^(M⊗ω), via `mbody-orbit-≤` domination onto the M′-orbit). **SIGNIFICANCE: the type-level tower ω↑↑ℓ is now an inexhaustible-below-ε₀ sequence of multipliers (one ω-exponentiation per level), all in pure Brouwer-code arithmetic — the tower's ARITHMETIC obstacle (composing/iterating limit multipliers) is CLOSED. The residual is purely STRUCTURAL: that a general first-order term's majorant IS such a finite mbody-nesting, threaded hereditarily (the logical relation over System T), plus the full diagonal's lower-order ⊕γa glue. The difficulty has fully relocated from arithmetic to the hereditary assembly.** NEXT (recommended): the hereditary structural theorem (R(ι⇒ι) = "mbody-dominated", threading `mbody-orbit-≤`/`iter-dominate` through K/S/Iter/app so every first-order majorant is a finite mbody-nesting) — the genuine remaining wall, now with the full limit-multiplier arithmetic beneath it.
  - `…MultDominated` (added 2026-07-02, 29 modules, no postulates, EXIT 0, in tour): **THE HEREDITARY RECURSOR CLOSURE the transformer predicates could NOT take.** Packages MultAffine's orbit-stable shape as `MDom φ = Σ M, ValidMult M × Σ c, c<ε₀ × (∀b φ b ≤ (b⊕c)⊗M)`; `ValidMult M = (S Z≤M)×(ι[2]⊗M≤M)×(M<ε₀)` (positive, doubling-absorbing, <ε₀), closed under `⊗` (`validMult-⊗`, via ⊗-assoc+dbl — for composition) and orbit-raise `M↦ω^(M⊗ω)` (`validMult-orbit`, via dbl-ω^ — for iteration). Leaves `MDom-additive` (id/succ/oracle-relabel at mult ω). **PAYOFF `MDom-orbit` : MDom φ → MDom (λ a → L(λk iter φ a k))** — the start-varying recursor majorant of an MDom body is MDom AGAIN at raised mult ω^(M⊗ω), same const, a 2-line consequence of `iter-dominate`+`mbody-orbit-≤` (shape survives its own orbit). **THIS IS EXACTLY the wall Hereditary/PolyHereditary/PolyTransformer/CNFTransformer ALL hit** (their ι⇒ι predicate either DROPPED the iterable shape (abstract transformer) or was NOT orbit-closed (finite affine → ω-mult escape)); MDom is orbit-closed by construction (MultAffine). Plus `MDom-app` (apply to <ε₀ → <ε₀), `MDom-orbit-<-ε₀` (recursor orbit <ε₀ for MDom body from <ε₀ start). **So the RECURSOR HALF of the first-order fundamental theorem is closed for the multiplier-dominated shape — the previously-blocked half.** **HONEST RESIDUAL (the composition/S half, now the SOLE pinned frontier):** (i) COMPOSITION `φ∘ψ` needs right-sub-distributivity `(X⊕E)⊗M ≤ X⊗M⊕E⊗M` (a genuine ordinal theorem — "blocked ≥ interleaved" — but the NAIVE induction on M FAILS at the successor step, which reduces to the false swap `E⊗γ'⊕X ⊄ X⊕E⊗γ'`; needs a cleverer/CNF proof) PLUS an output-constant extension of the shape `(b⊕c)⊗M⊕D`; (ii) the ground S-diagonal. NOTE: pure mbody does NOT compose VIA DISTRIBUTION (the affine-fold wall: `X⊗M⊕c ≤ (X⊕c)⊗M` is FALSE for limit M, e.g. ω+1>ω). **BUT COMPOSITION + S-DIAGONAL CLOSE ANYWAY (added same session 2026-07-02, EXIT 0) — right-sub-distributivity NOT needed:** the trick is `crux` (`c≤Y → (Y⊕c)⊗M ≤ Y⊗M`) + **CONSTANT ENLARGEMENT**. `MDom-∘` (composition): enlarge inner const to `c_ψ⊕c_φ`, then `c_φ ≤ (b⊕(c_ψ⊕c_φ))⊗M_ψ` so crux absorbs the OUTER const → composite at mult `M_ψ⊗M_φ` (validMult-⊗), NO output constant, NO distribution. `MDom2` (jointly-dominated 2-arg, common-bound form `(s a b)→a≤s→b≤s→ F a b≤(s⊕c)⊗M`); `MDom2-diag` (GROUND S-DIAGONAL `λx.F x(G x)`): common bound `s=(x⊕c*)⊗M_G`, enlarge+crux → mult `M_G⊗M`. `MDom2-Iter` (partial recursor `F a ν=orbit(a)⊕ν` is jointly-dominated at mult `M′⊗ι[2]`, via MDom-orbit + `A⊕A=A⊗ι[2]`; `ι2-valid-mult : ValidMult M′→ValidMult(M′⊗ι[2])`). Hence `MDom-recursor-diag` : the RECURSOR S-DIAGONAL `λx. Iter φ x (G x)` (canonical tower-driving term) is MDom. **SO MDom IS CLOSED UNDER COMPOSITION + ITERATION (recursor orbit) + DUPLICATION (S-diagonal) SIMULTANEOUSLY, ALL <ε₀ — the three ops that jointly define the tower.** **CONCEPTUAL CORRECTION (understanding>truth): the earlier claim "no fixed class closed under dup+iter+comp below ε₀ = the tower = open" was TOO PESSIMISTIC.** MDom IS such a class — the resolution is a PER-FUNCTION multiplier M<ε₀ that CLIMBS with each op (comp: M_ψ⊗M_φ; orbit: ω^(M⊗ω); diag: M_G⊗M) but every FINITE composite stays <ε₀ (no uniform multiplier; sup over the class = ε₀). The old "impossibility" was about a FIXED/uniform multiplier; the growing-but-<ε₀ multiplier dodges it. **THIS IS THE ARITHMETIC/MAJORANT-LEVEL HEART OF THE FIRST-ORDER THEOREM, COMPLETE — every closure that blocked Hereditary/PolyHereditary/PolyTransformer/CNFTransformer is discharged.** HONEST SCOPE (conjecture NOT proved): MDom is the MAJORANT class (𝓑→𝓑); REMAINING = the STRUCTURAL assembly — type-indexed hereditary predicate over ALL first-order types (function args, K/S at higher types, FunArgs/plug machinery à la PolyTransformer) + connection to actual dialogue height (height ⟦t⟧ ≤ majorant, R-relation, fundamental theorem over real syntax). The remaining work is type-structural PLUMBING with every arithmetic closure now available — NOT an arithmetic wall. NEXT: build the hereditary predicate `R`/`𝔅` with `R(ι⇒ι)=MDom`, discharge K (weaken), S (MDom-∘/MDom2-diag), Iter (MDom-orbit/MDom2-Iter), app (MDom-app), base (MDom-additive) — then the height connection.
  - `…MultApplicative` (added 2026-07-02, 30 modules, no postulates, EXIT 0, in tour): **the MDom closures CASHED IN on real dialogue trees — an unconditional `height<ε₀` for a fragment INCLUDING the recursor S-diagonal.** The MDom analogue of `Applicative` (22): SAME skeleton (concrete two-sort syntax `Gnd`/`Fun`, interp into dialogue model, height majorants `μG`/`μF`, logical relation `R-G`/`R-F`, capstone `height-<-ε₀`) but `Aff`↦`MDom` and a NEW constructor `iterDiagF φ G` = the recursor S-diagonal `λx. iter' φ x (G x)` (start = the bound variable, count = `G` of it), interp `λd. kleisli-extension (iter ⟦φ⟧ d)(⟦G⟧ d)`, majorant `λa. L(λk iter(μF φ)a k)⊕μF G a` = exactly `MDom-recursor-diag`'s function. **This is the term `Applicative` had to EXCLUDE** (the S-diagonal wall). Each `MDom-μF` case = ONE MDom closure: ΩF↦MDom-succ, succF↦MDom-id, iterF (count-varying, fixed start) ↦`MDom-const-left orbit (MDom-orbit-<-ε₀ …)`, compF↦MDom-∘, constF↦MDom-const, iterDiagF↦`MDom-recursor-diag (MDom-μF φ)(MDom-μF G)`. The R-F iterDiagF case is structurally the iterF case with start/count roles SWAPPED: orbit over the argument `d` (orbit-pt base = the hypothesis `h : height d ≤ a`, NOT R-G x), count = ⟦G⟧d (bounded by R-F G), closed by `Conditional.height-iter-≤-orbit-bound ⟦φ⟧ d orbit uniform (⟦G⟧d)` + `⊕-mono-right orbit (R-F G d a h)`. Payoff `height-recursor-diag-<-ε₀`: the tower-driving diagonal driven by an ARBITRARY ground count (oracle read `appG ΩF zeroG` ⟹ orbit sup genuinely taken) is `<ε₀`, UNCONDITIONAL. **Added two leaves to MultDominated: `MDom-const : C<ε₀→MDom(λ_.C)`** (`C≤b⊕C≤(b⊕C)⊗ω`) **and `MDom-const-left : C<ε₀→MDom(λν. C⊕ν)`** (the LEFT-additive partial-recursor shape MDom-additive misses since ⊕ non-comm; `C⊕ν≤(ν⊕C)⊕(ν⊕C)=(ν⊕C)⊗ι[2]≤(ν⊕C)⊗ω`, via Z-left-unit + ≤-L-upper-bound at 2). **SIGNIFICANCE: first UNCONDITIONAL `height<ε₀` on genuine dialogue trees that BREACHES the S-diagonal wall — strictly larger than Applicative's fragment (adds the previously-walled tower-driving recursor-S-diagonal), every majorant climbing the multiplier tower yet <ε₀.** HONEST SCOPE (conjecture NOT proved): still a FRAGMENT — the functions MDom is closed under, and the specific S-diagonal whose start is the bound variable (not a general 2-arg φ). REMAINING for full first-order: the type-indexed hereditary assembly over ALL first-order types (function-arg majorization à la Howard, K/S at higher types, FunArgs/plug) with R(ι⇒ι)=MDom — structural plumbing, every arithmetic closure now available.
  - `…MultApplicativeT` (added 2026-07-02, 31 modules, no postulates, EXIT 0, in tour): **REALITY-CHECK (done at user's request before the hereditary assembly) — the MultApplicative fragment EMBEDS into genuine first-order System T.** MultApplicative's bespoke `Gnd`/`Fun` syntax + interp `⟦⟧G`/`⟦⟧F` are certified to denote actual `Majorant.T₁` terms (real `Ω/Zero/Succ/Iter/K/S/·`) via translation `⌜_⌝G:Gnd→T₁ ι`, `⌜_⌝F:Fun→T₁(ι⇒ι)` + agreement `agreeG/agreeF : ⟦ ⌜x⌝ ⟧₁ ＝ ⟦x⟧`. Agreement is DEFINITIONAL at every node (checked in MFPS-XXIX/Combinators: `zero'=η 0`, `succ'=B-functor succ`, `iter' f x=kleisli-extension(iter f x)`, `Ķ x y=x`, `Ş f g x=f x(g x)`), so proof = refl glued by ap/happly/dfunext through the applications; iter cases via `ap₂ (λ g y→kleisli-extension(iter g y))`. **KEY CONFIRMATIONS: `compF f g` ↦ `S(K f)g` (standard combinator ∘), and the tower-driving `iterDiagF φ G` ↦ `S (Iter φ) G` — a GENUINE use of the S combinator with `Iter φ` as the shared-arg function.** So the recursor-S-diagonal really IS a System T term, and payoff `height-systemT-<-ε₀ : (x:Gnd) → height ⟦ ⌜x⌝G ⟧₁ < ε₀` states MultApplicative's unconditional bound DIRECTLY about System T dialogue trees (transport height-<-ε₀ along agreeG). Does NOT enlarge what's proved — CERTIFIES SCOPE: the fragment is a bona fide sub-language of T₁, not an ad-hoc calculus. Imports Majorant (T₁/⟦⟧₁), MultApplicative, UF.Base (ap₂/happly). **NEXT (the deferred hard core): the type-indexed hereditary predicate `𝔅 σ` over ALL first-order types with R(ι⇒ι)=MDom, reusing Hereditary FunArgs/plug — discharge base+app (cheap, always carried), then Iter via MDom-orbit (the payoff/real test: if it needs the arg's MDom-data in the result multiplier, promote R(ι⇒ι) to MDom-TRANSFORMER à la PolyTransformer), then K/S.**
  - `…MultHereditary` (added 2026-07-02, 32 modules, no postulates, EXIT 0, in tour): **THE HEREDITARY PREDICATE WHOSE RECURSOR CLOSES — the deferred hard core, and the KEY QUESTION RESOLVED: the FIXED-MDom predicate suffices, NO transformer needed.** Runs the Hereditary/CNFTransformer skeleton (GroundType, FunArgs, plug, mutual `𝔅M`) with the ground joint bound `JBM ι a s c M = a ≤ (s⊕c)⊗M` for a `ValidMult M` (in place of finite affine `(s⊕d)⊗ι[m]`); `JointM σ φ = Σ M, ValidMult M × Σ c, c<ε₀ × (∀s→JBM σ φ s c M)`. Ground extraction `𝔅Mι-to-<ε₀`/`<ε₀-to-𝔅Mι` at ε₀ (s=Z ⟹ c⊗M<ε₀). App (`𝔅M-app`, absorb ground arg into constant via `JBM-shift`=⊕-assoc), base (Zero/Succ/Ω), weaken, K (`𝔅M-K`), S-higher (`𝔅M-S-higher`, three-apps sharing fun arg) ALL PORT VERBATIM from Hereditary/CNFTransformer (ι[m]↦M). **NEW CONTENT `𝔅M-Iter` (the case Hereditary/PolyHereditary/PolyTransformer/CNFTransformer ALL left OPEN): `𝔅M((ι⇒ι)⇒ι⇒ι⇒ι) μ-Iter` — ONE LINE**: at fun-arg g, `𝔅M(ι⇒ι) g` hands back (via `JointM1→MDom`) EXACTLY the MDom data `MDom2-Iter` consumes → JointM(ι⇒ι⇒ι)(λ a ν→L(λk iter g a k)⊕ν). Compiled FIRST real attempt. **This CONFIRMS the design hypothesis I flagged as the decisive uncertainty: a FIXED MDom majorant at ι⇒ι (NOT an MDom-transformer) closes hereditary Iter — because MDom is orbit-closed (MDom2-Iter), the shape survives its own orbit, so no PolyTransformer-style abstract transformer (which drops the shape) is needed.** ALSO `𝔅M-S-ground-ι` (ground S-diagonal AT ι, `λ a→φ a(γ a)`, φ:ι⇒ι⇒ι, γ:ι⇒ι) closes via `MDom2-diag`. Small conversions `JointM1→MDom`/`MDom→JointM1`/`MDom2→JointM2`/`JointM2→MDom2` witness common-bound-form ⟺ per-arg MDom/MDom2 (coincide at arity 1,2: instantiate s=arg forward, monotonicity backward). **SIGNIFICANCE: STRICT ADVANCE over CNFTransformer — closes app+K+S-higher+base+RECURSOR+ground-S-at-ι, i.e. everything CNFTransformer had PLUS the previously-hard recursor half.** HONEST SCOPE (conjecture NOT proved, NO fundamental theorem over all T₁ yet): the SOLE remaining gap is the GENERAL ground S-diagonal (shared arg ι, but the OTHER TWO args at HIGHER types — `𝔅M-S-ground-ι` only does σ=τ=ι) — the same combinatorial case open throughout, now ISOLATED as the only gap with the recursor discharged. NEXT: attack general ground-S (the plug/GroundType combinatorics for `λa.φ a(γ a)` at higher σ,τ), or assemble a fundamental theorem `𝔅M`-over-`T₁` modulo that gap.
  - `…MultHereditaryT` (added 2026-07-02, 33 modules, no postulates, EXIT 0, in tour): **A FUNDAMENTAL THEOREM connecting `𝔅M` to REAL dialogue height — unconditional `height<ε₀` for first-order System T with the RECURSOR + `S` (except ground-S with function-typed components).** KEY OBSERVATION that makes a COMBINATORY fund. thm possible: the `S` COMBINATOR's goodness `𝔅M(S-type) μ-S` CLOSES at exactly the two instance families MultHereditary handles — (i) at `((ι⇒ι⇒ι)⇒(ι⇒ι)⇒ι⇒ι)` (ground-S at ι): both non-shared args AND the shared arg land in FunArgs (since ι⇒ι⇒ι, ι⇒ι are function types ⟹ GroundType peels them), leaving the ground diagonal = `𝔅M-S-ground-ι`; (ii) at any `(((ρ₁⇒ρ₂)⇒σ⇒τ)⇒((ρ₁⇒ρ₂)⇒σ)⇒(ρ₁⇒ρ₂)⇒τ)` (shared arg a FUNCTION type): all three in FunArgs, = `𝔅M-S-higher`. Lemmas `𝔅M-Sg`/`𝔅M-Sh` = ONE-LINE peels of the FunArgs tuple. Fragment `T★` = {Ω,Zero,Succ,Iter,K,·} + `Sg★`(ground-S@ι) + `Sh★`(S-higher); embeds into Majorant.T₁ via `e` (both S↦S₁). `goodμ:(t:T★ σ)→𝔅M σ(μ(e t))` = one MultHereditary closure per case (K★/Sh★ need EXPLICIT type-implicits or metas go unsolved). Then `μ(e t)<ε₀` (𝔅Mι-to-<ε₀) + `height ⟦e t⟧₁ ≤ μ(e t)` (Majorant.height-≤-μ) ⟹ `height-<-ε₀:(t:T★ ι)→height ⟦e t⟧₁<ε₀` and `dialogue-height-<-ε₀:(t:T★((ι⇒ι)⇒ι))→height(⟦e t⟧₁ generic)<ε₀`, UNCONDITIONAL. **FIRST fundamental theorem to include the RECURSOR *and* S-at-higher-types at ε₀ — the recursor being the previously-open half.** REQUIRED CHANGE: MultHereditary switched its `Maj` import from Hereditary to Majorant (the two identical `Maj` defs didn't defeq-unify during μ-S implicit inference; unifying the source fixed it). **HONEST SCOPE (conjecture NOT proved): still NOT all of T₁ — the SOLE excluded combinator is ground-S with a FUNCTION-typed σ/τ (shared arg ι, another arg higher), the persistent structural wall (FunArgs fixes fn-args before quantifying the ground arg, but there the fn-arg `γ a` VARIES with the ground a).** This is genuinely the only remaining gap; recursor + all other S + K + app + base all close. NEXT: the general ground-S wall needs a ground-context-DEPENDENT function-argument predicate (a redesign of FunArgs/plug so fn-args can depend on ground args) — the deep structural open problem, now precisely isolated.
  - `…MultHereditaryB` (added 2026-07-02, 34 modules, no postulates, EXIT 0, in tour): **DESIGN B — the dual `s`-relative logical relation, CLOSING ground-S at a FUNCTION-TYPED argument (the exact case Design A/MultHereditary could NOT reach).** Built in NEW files (existing work untouched, at user's request). Where Design A records fn-args as GLOBAL majorants (FunArgs, quantified before common bound s) — closing app-everywhere+Iter+K+S-higher+ground-S-at-ι but NOT ground-S-at-function (no global majorant of the varying γ a; φ probes outside [0,s]) — Design B uses `Bnd σ s c M φ`: ground args measured vs common s with UNCHANGED data (c,M); FUNCTION args carry their OWN data (cg,Mg) threading multiplicatively to M⊗Mg (as MDom-∘). `Good σ φ = Σ M, ValidMult M × Σ c, c<ε₀ × (∀s→Bnd σ s c M φ)`. Bnd cases: ι→a≤(s⊕c)⊗M; ι⇒τ→(x)x≤s→Bnd τ s c M(φ x); (σ⇒σ')⇒τ→(g cg Mg valid cg<ε₀)→Bnd(σ⇒σ')s cg Mg g→Bnd τ s(c⊕cg)(M⊗Mg)(φ g). **HEADLINE `Good-S-fun`: ground-S `λa.φ a(γ a)` with γ a FUNCTION-valued CLOSES** — γ a fed to φ a as fn-arg with γ's own (cγ,Mγ), shared ground a≤s serves both, multiplier Mφ⊗Mγ, **NO monotonicity hypothesis needed** (the s-relative relation tracks the ACTUAL arg's bound, not a majorant). Compiled FIRST attempt. Also `Good-app-fun` (fn-arg application, threads M_F⊗M_G), ground extraction `Good-ι-to-<ε₀`/`<ε₀-to-Good-ι` at ε₀ (s=Z→c⊗M<ε₀), base Zero/Succ/Ω, `Good-app-fun-<ε₀`. **HONEST DUAL SCOPE: Bnd's fn-arg bound holds only for inputs ≤s, so Design B canNOT bound anything probing a fn-arg OUTSIDE [0,s]: (i) the RECURSOR (iterating g runs g past s — Bnd(ι⇒ι) s cg Mg g only bounds g on [0,s], can't get MDom g for the orbit), (ii) GROUND-arg application before a fn-arg (re-index up to bigger s' then need fn-arg good-at-s' but caller gives good-at-s; Bnd ANTITONE in s at fn-arg slots). These are EXACTLY the cases Design A closes.** **KEY FINDING: Design A and Design B are strictly COMPLEMENTARY/DUAL — each closes precisely what the other cannot; neither alone is a full fundamental theorem. A=global-majorant closes app-everywhere+Iter+ground-S-at-ι, fails ground-S-at-function; B=s-relative closes ground-S-at-function+fn-app, fails Iter+ground-arg-app+ground-S-at-ι.** The SHARED obstruction reconciling them = ONE relation simultaneously s-relative AND order-insensitive AND allowing fn-arg bounds to depend on preceding ground context = the genuine Howard–Bezem STRONG-majorizability step (self-monotone majorants + transformer-in-s bounds), now pinned FROM BOTH SIDES. This is the honest state of the deep open structural problem; the arithmetic (MDom closures) is complete beneath it. **SHARPENED CHARACTERIZATION (2026-07-02, after testing 5 linear designs A/B + 3 hybrids, all walling at the SAME point): the hereditary predicate's MULTIPLIER MUST GROW WITH THE ARGUMENT WEIGHT — the tower is INTRINSIC to the hereditary structure, not factorable into a fixed multiplier class the way pure MDom arithmetic was.** Concrete witness: φ a f = f(f a), γ a = λx. a⊗x ⟹ ground-S diagonal φ a(γ a) = a⊗(a⊗a) = a³ (super-linear at infinite a: a=ω→ω³ needs mult ≥ω²; a=ω^ω needs ≥(ω^ω)²; no fixed M<ε₀ works over all a<ε₀). So NO bound of form (weight⊕c)⊗M with FIXED M can hold — the multiplier M must be a weight-dependent climbing function. This is WHY every linear (weight⊕c)⊗M design fails ground-S-at-function. The genuine solution = a predicate whose bound is a TRANSFORMER T(weight) that is simultaneously orbit-closed (for Iter) + composition-closed (for app) + super-linear/weight-dependent-multiplier (for ground-S-at-function) + self-monotone (Bezem strong majorizability). Combining all four = the full open problem; each existing module closes at most 2-3. Held at verified-clean checkpoint (34 modules, full rebuild EXIT 0); reframe deferred as deliberate research, not attempted as an unsupervised scaffold (would hide the hard cases).
  - `…MultHereditaryE` (added 2026-07-02, 35 modules, no postulates, EXIT 0, in tour): **DESIGN E — transformer-valued bounds; ground-S at a function type for SUPER-LINEAR companions, strictly beyond Design B.** Attempted the reframe (user-authorized). Result: B's fixed multiplier `(w⊕c)⊗M` (linear) can't even EXPRESS a functional like `γ a=λx.a⊗x` (ground-S diagonal = a⊗(a⊗a)=a³, super-linear) — such γ isn't "Good" in B's sense (no fixed M bounds a⊗x ∀a). Design E replaces the fixed multiplier by a MONOTONE `<ε₀`-PRESERVING TRANSFORMER `T:𝓑→𝓑`: `ValidT T = (∀w, w<ε₀→T w<ε₀) × (∀w w', w≤w'→T w≤T w')` (NON-circular — no orbit-closure asserted). Bnd ι w T a = a≤T w; fn-arg carries own Tg, result transformer = COMPOSITION T∘Tg (ValidT-∘). Exhibits `ValidT-sq : ValidT(λw.w⊗w)` (the shape B couldn't carry). CLOSES: ground extraction (T Z<ε₀), base (Zero/Succ/Ω), `Good-app-fun` (result T∘Tg), **headline `Good-S-fun` + `Good-S-fun-<ε₀` — ground-S at function type for ARBITRARY ValidT companion incl. super-linear** (diagonal transformer Tφ∘Tγ). Compiled clean. **KEY FINDING (definitive after ~8 designs): TRANSFORMERS FIX THE BOUND SHAPE (super-linear), NOT THE QUANTIFICATION ORDER. The A/B fork is IRREDUCIBLE: fn-args good at SAME weight w (B/E — ground-S closes, RECURSOR does NOT: iterating g runs past w) vs GLOBALLY ∀w' (A — recursor closes, ground-S does NOT: γ a good only at weights ≥a). `γ a` is PROVABLY good only for w'≥a; NO transformer bridges w'<a.** So Design E = "B, super-linear" — widens ground-S/app reach to super-linear bounds but INHERITS B's recursor gap. ValidT DELIBERATELY OMITS orbit-closure — that omission IS the recursor's residual. **CONCLUSION: the reframe genuinely requires the ORDER-FREE, ground-context-DEPENDENT Howard–Bezem strong-majorizability predicate — no fixed-quantification-order design (same-w OR global) can close both recursor and ground-S-at-function, proven exhaustively. This is the honest floor of the open structural problem.** 35 modules, full rebuild EXIT 0.
  - **DESIGN CONVERGED (2026-07-03): the reframe's majorant is a TYPE-INDEXED HIGHER-ORDER TRANSFORMER** `𝕋 ι = 𝓑→𝓑`, `𝕋(σ⇒τ)=𝕋σ→𝕋τ`, with the fn-arg result being COMPOSITION (for app/S) or ORBIT (for Iter) — the exact Howard–Bezem shape, now concrete. Validated: ground-S-at-function closes via composition transformer, Iter via orbit transformer. The one ARITHMETIC sub-residual: the base transformer class must be ORBIT-CLOSED (sub-exponential — must EXCLUDE `ω^` itself, whose orbit IS ε₀).
  - `…MultSquareOrbit` (added 2026-07-03, 36 modules, no postulates, EXIT 0, in tour): **SUPER-LINEAR ORBIT-CLOSURE — orbit of squaring `b↦b⊗b` is `<ε₀`.** The concrete arithmetic core of the reframe's recursor residual (base transformer class must be orbit-closed AND super-linear). `Affine.double-orbit-<-ε₀` = LINEAR doubling `b⊕b`(=b⊗ι[2]); this = DEGREE-2 `b⊗b`. **`sq-orbit-<-ε₀ : w<ε₀ → L(λk iter (λb.b⊗b) w k) < ε₀`**, via: squaring adds ONE level to the ω-exponent per step — `w≤ω^b ⇒ iter sq w k ≤ ω^(b⊗ι[2^k])` (reuses `Affine.pow2`; step uses `ω^⊗` exponent-homo ω^x⊗ω^x=ω^(x⊕x) + ⊗-left-distrib + ι-+-homo, SAME template as double-orbit but INSIDE ω^), so sup=ω^(b⊗ω)<ε₀. Helpers: `≤-S-self:(a)→a≤S a` (structural rec, L-case via ≤-trans through f n), `expo`/`tower-≤-ω^expo`/`w-≤-ω^:(w)→w<ε₀→Σ b, b<ε₀ × w≤ω^b`. **KEY: makes the ARITHMETIC BOUNDARY concrete — squaring's orbit stays <ε₀ (bounded exponent multiple) while ω-EXPONENTIATION's orbit REACHES ε₀ (moves up a tower level: iter(ω^)(1)=tower, sup=ε₀). So the reframe's base transformer class CAN contain squaring/super-linear but MUST EXCLUDE ω^.** With MultHereditaryE (super-linear on the S side) this shows BOTH arithmetic pieces of a super-linear transformer class are tractable ⟹ evidence the reframe's residual is genuinely ARITHMETIC (ordinal analysis of the sub-exponential orbit-closed class), NOT another structural wall. 36 modules, full rebuild EXIT 0.
  - `…MultHereditaryF` (added 2026-07-03, 37 modules, no postulates, EXIT 0, in tour): **THE STRUCTURAL A/B FORK DISSOLVED — the type-indexed higher-order transformer. This is the major structural advance of the whole project.** The wall (open all along): a fn-arg had to be good either at the current weight (ground-S closes, recursor fails) OR globally (recursor closes, ground-S fails), γ a good only at weights ≥a. **RESOLUTION: every argument carries its OWN transformer, itself globally valid (`GoodT`).** Transformers TYPE-INDEXED: `𝕋 ι=𝓑→𝓑`, `𝕋(σ⇒τ)=𝕋σ→𝕋τ` (functionals on transformers). `GoodT` = logical relation (ground: mono + <ε₀-pres; higher: maps GoodT→GoodT). `Bnd ι w T a = a≤T w`; `Bnd(σ⇒τ) w T φ = (g Tg)GoodT Tg→(∀w' Bnd σ w' Tg g)→Bnd τ w(T Tg)(φ g)` — arg fed with its transformer at EVERY weight, result = higher-order app `T Tg`. `Good σ φ = Σ T, GoodT σ T × ∀w Bnd σ w T φ`. **KEY MECHANISM: because arg-transformer Ta is globally valid, feeding (a,Ta) to Good γ AT EVERY WEIGHT makes γ a globally good with transformer Tγ Ta — the "good only at ≥a" obstruction VANISHES.** So **`Good-S-diag : (σ τ)→Good(ι⇒σ⇒τ)φ→Good(ι⇒σ)γ→Good(ι⇒τ)(λa→φa(γa))` closes for ARBITRARY σ,τ INCLUDING function-typed σ (the case blocked THROUGHOUT)** — transformer `λTa→(Tφ Ta)(Tγ Ta)`. Plus `Good-app` (result TF TG), `Good-ι-to-<ε₀`, base (Zero=id transformer, Succ=identity functional λTg.Tg, Ω=λTg w.S(Tg w)). **`Good-Iter` ALSO closes**: transformer = FUNCTIONAL ORBIT `Torbit Tg Ta=λw.L(λk iter Tg Ta k w)` (Tg iterated on Ta); pointwise bound `orbit-pt : iter g a k≤iter Tg Ta k w'` by induction (global, uses GoodT preserved along Tg via `GoodT-iter`), so Bnd closes UNCONDITIONALLY, and ONLY the GoodT component needs Torbit<ε₀ — taken as explicit hypothesis `orbit-good` (transformer-orbit-closure). Helpers `GoodT-iter`, `GoodT-⊕`. **ALSO `Good-K` (weaken, transformer λTa Tb.Ta) and the FULL `Good-S` (general ρ σ τ, shared arg possibly FUNCTION-typed, transformer λTφ Tγ Ta.(Tφ Ta)(Tγ Ta)) close UNCONDITIONALLY** — so EVERY combinator except the recursor is unconditional; app+base+K+S(full)+ground-S-diag all no-hypothesis. **SIGNIFICANCE: the type-structural obstruction that stood the ENTIRE project (ground-S at function-typed args + recursor in ONE predicate) is GONE. Design F reconciles them: args-carry-globally-valid-transformers + higher-order-application.** **CRITICAL HONESTY CAVEAT (verified counterexample): `orbit-good` is FALSE for the full `GoodT` — the exponentiation functional `Tg T=λw.ω^(T w)` IS GoodT(ι⇒ι) (mono + <ε₀-pres), yet Torbit Tg id 1 = sup_k(k-fold ω^)(1) = ε₀, NOT <ε₀. So `Good-Iter`'s hypothesis CANNOT be discharged blanket; the full GoodT is TOO PERMISSIVE (includes ω^, whose orbit hits ε₀). The residual is to CUT GoodT down to a sub-exponential orbit-closed subclass `𝕄T⊂GoodT` (containing ⊕,⊗,squaring[sq-orbit] but EXCLUDING ω^) on which orbit-good holds, AND land the base combinators in 𝕄T. `Good-Iter` is stated over full GoodT only to EXPOSE the reduction cleanly — NOT a claim that orbit-good is provable.** So do NOT build a "fundamental theorem conditional on orbit-good" — it would be conditional on a FALSE hypothesis (vacuous/misleading). HONEST SCOPE (conjecture NOT proved): remaining = (i) define the sub-exponential class 𝕄T + prove its orbit-closure (arithmetic; sq-orbit is a piece; the boundary is exactly "exclude ω^" per sq-orbit note), (ii) verify base combinators land in 𝕄T, (iii) height connection (height ⟦t⟧≤μ t via Majorant.height-≤-μ, then Good-ι-to-<ε₀). **The type-structural wall is GONE (Good-S-diag/Good-S unconditional witness this); what remains is ordinal ARITHMETIC (the sub-exponential class) + assembly.** This supersedes the earlier "no fixed-quantification-order design works" floor — resolution = NOT fix quantification order but give each arg its OWN globally-valid transformer (Howard-Bezem strong majorizability, concretely realized). 37 modules, full rebuild EXIT 0.
  - `…MultHereditaryFLin` (added 2026-07-03, 38 modules, no postulates, EXIT 0, in tour): **DISCHARGES MultHereditaryF's recursor hypothesis `orbit-good` on the LINEAR class — concrete proof the residual is ARITHMETIC.** Since orbit-good is FALSE for full GoodT (ω^), it must be discharged on a sub-exponential subclass. Here: `LinBounded Tg = Σ c M, c<ε₀ × ValidMult M × (∀ T w → Tg T w ≤ (T w⊕c)⊗M)` (functional post-composes with an MDom step — the shape a recursor with a LINEAR body produces: id/succ/oracle-relabel + composites). **`orbit-good-lin : GoodT(ι⇒ι) Tg → LinBounded Tg → GoodT ι Ta → GoodT ι(Torbit Tg Ta)`** — iterating Tg on Ta is dominated pointwise by iterating MDom body `b↦(b⊕c)⊗M` on Ta w (`iter-bound`), so `MultAffine.mbody-orbit-≤` bounds orbit by `(Ta w⊕c)⊗ω^(M⊗ω)<ε₀`; mono via `GoodT-iter`. So the recursor orbit-closure is PROVABLE on a real class from library arithmetic. **`LinBounded-orbit : LinBounded Tg → LinBounded(λTa.Torbit Tg Ta)`** — the linear class is CLOSED UNDER THE RECURSOR ORBIT (multiplier climbs M↦ω^(M⊗ω) via validMult-orbit, same constant; NO GoodT needed) ⟹ NESTED recursors with linear inner functions STAY linear. This is the structural backbone for a linear-fragment fundamental theorem. Compiled clean. Super-linear case reaches via `MultSquareOrbit` in place of mbody-orbit-≤. **STATE OF PLAY (2026-07-03, end of session batch 36-38): the STRUCTURAL WALL IS GONE (MultHereditaryF: ground-S-at-function + recursor reconciled, all combinators but Iter unconditional). orbit-good dischargeable on linear (orbit-good-lin) + linear class orbit-closed (LinBounded-orbit) + super-linear orbit arithmetic (sq-orbit). REMAINING for first-order fundamental theorem: (i) a Design-F predicate variant carrying LinBounded (or a sub-exponential class) on ι⇒ι transformers so Good-Iter uses orbit-good-lin (needs re-proving F's combinators with the class tracked — the ASSEMBLY), (ii) super-linear ground-S companions need the super-linear class (sq-orbit) threaded too, (iii) height connection via Majorant.height-≤-μ + Good-ι-to-<ε₀. This is ASSEMBLY + finishing the sub-exponential class, NOT a structural wall. NEXT concrete step: either the LinBounded-tracking predicate (linear-fragment fund. thm, strictly beyond MultHereditaryT since it adds function-typed ground-S) OR a concrete-syntax Design-F fragment (à la MultApplicativeT) with explicit LinBounded transformers giving an unconditional height<ε₀.** 38 modules, full rebuild EXIT 0.
    - **(added 2026-07-03, same module) THE LINEAR CLASS IS A COMPLETE ALGEBRA: `LinBounded-id` (λT.T, data Z/ω), `LinBounded-S` (λT w.S(T w), data ι[1]/ω), `LinBounded-∘` (composition λT.Tf(Tg T), via MDom-∘ enlarge-then-crux: constants accumulate cg⊕cf, multipliers compose Mg⊗Mf), `LinBounded-orbit` (already had).** So the linear ι⇒ι transformers are closed under id, successor, composition, and the recursor orbit — exactly what a linear first-order term builds. Compiled clean.
    - **KEY SUBTLETY FOUND (2026-07-03) toward the fundamental theorem: `K` breaks pure `LinBounded`.** K·a = the CONSTANT function λb.a; its transformer λTb.Ta IGNORES its argument Tb, so it is NOT LinBounded (LinBounded bounds output by (arg⊕c)⊗M — but a constant ignores the arg, and for a small arg the bound would be small while Ta w grows). YET its orbit is trivially fine (iterating a constant = constant). So the fundamental-theorem class must be **LINEAR + CONSTANT**, not just LinBounded. Options: (a) a sum class `CLin = LinBounded ⊎ Constant` (Constant Tg = Σ Ta, GoodT Ta × (Tg ＝ λ_.Ta)) with orbit-good + closure proved per branch (comp: anything∘constant=constant, constant∘anything=constant; linear∘linear=linear); (b) an affine-with-D bound `Tg T w ≤ (T w⊕c)⊗M ⊕ D w` (D a GoodT transformer, D=Z for linear, M=ω/c=Z/D=Ta for constant) — orbit-closed but needs the (mbody⊕D)-orbit arithmetic. **CONSTANT DISCHARGE NOW BUILT: `orbit-good-const : GoodT Ta → GoodT Ta0 → GoodT(Torbit(λ_.Ta) Ta0)`** — iter(λ_.Ta) Ta0 k = Ta0 (k=0) or Ta (k≥1), so orbit ≤ Ta0 w⊕Ta w <ε₀; clean, no new imports. So the recursor orbit-closure is now discharged for BOTH linear (orbit-good-lin) AND constant (orbit-good-const) functionals — the full linear+constant class the combinators generate (super-linear via sq-orbit separately). **THIRD SHAPE ALSO BUILT: `orbit-good-add : GoodT C → GoodT Ta0 → GoodT(Torbit (λTν w. C w⊕Tν w) Ta0)`** — the PARTIAL-RECURSOR's own transformer `λTν.λw.C w⊕Tν w` (C added on LEFT), which is what `Iter f x : ι⇒ι` denotes and what a NESTED recursor iterates; left-additive orbit via `Nested.orbit-left-sup-≤` (bound `(C w⊗ω)⊕Ta0 w<ε₀`). So the recursor orbit-closure is discharged for the THREE basic first-order body shapes: **lin (mbody), const (K), add (nested recursor)**. **CLASS-STRUCTURE FINDING (2026-07-03): the composition CROSS-CASES (lin∘add, add∘lin, etc.) produce a TWO-SIDED AFFINE shape `Tg T w ≤ D₁ w ⊕ (T w⊕c)⊗M ⊕ D₂ w` (D₁,D₂ GoodT transformers, c<ε₀, M ValidMult) — the unified class covering lin (D₁=D₂=Z), const (D=Ta), add (D₁=C). So the fundamental-theorem transformer class = TWO-SIDED-AFFINE-WITH-MULTIPLIER (= `Add±` + ValidMult multiplier), which must be closed under composition (NON-COMMUTATIVE affine arithmetic — the D's on the left get trapped inside ⊗M after composition, needing a crux-style absorption like MDom-∘) and orbit. This is the SUBSTANTIAL remaining ARITHMETIC for the fundamental theorem — well-defined, all per-shape orbit-good lemmas in hand, but the two-sided affine composition-closure is a real development.** STATUS: structural wall GONE (Design F), recursor orbit-good discharged per-shape (lin/const/add/sq), LinBounded a complete algebra (id/S/∘/orbit); REMAINING = (i) the two-sided-affine class + its ∘/orbit closure, (ii) the GoodL/BndL fundamental theorem over a CONCRETE syntax (avoids the polymorphic-T₁ type-case obstacle: LG's ι⇒ι special-case doesn't reduce for abstract types), (iii) height connection. 38 modules, full rebuild EXIT 0, nothing committed. **ALSO FOUND: `LG→GoodT` FAILS (contravariance at fn-arg types) and `GoodT→LG` fails (can't manufacture LinBounded at ι⇒ι), so the fundamental theorem needs a PARALLEL `BndL` (Bnd with LG-args) not a reuse of F's Bnd — but the combinator proofs PORT VERBATIM from F (LG for GoodT, BndL for Bnd) and Iter becomes UNCONDITIONAL (LG(ι⇒ι) carries LinBounded → orbit-good-lin discharges).** So the fund.-thm design is: `ExtraLin(ι⇒ι)=CLin, else 𝟙`; `LG ι=GoodT ι`, `LG(σ⇒τ)=(logical-relation)×ExtraLin`; `BndL`=Bnd-with-LG-args; `GoodL=Σ T, LG T × ∀w BndL`; combinators port from F, Iter via orbit-good-(lin/const), then height via Majorant.height-≤-μ. This is the NEXT module (the linear-fragment fundamental theorem — strictly beyond MultHereditaryT: adds function-typed ground-S). The K-subtlety (broaden to CLin) is the only real new content beyond porting. 38 modules, full rebuild EXIT 0, nothing committed.
  - `…MultHereditaryFAff` (added 2026-07-03, 39 modules, no postulates, EXIT 0, in tour): **THE TWO-SIDED-AFFINE CLASS BUILT — the complete ι⇒ι transformer algebra of the first-order fragment. The "substantial remaining arithmetic" of the 38-entry is DONE, and it turned out clean, not hard.** KEY MOVE: don't fight the trapping — since a left summand D genuinely CANNOT move out of ⊗M (⊕ non-commutative, ⊗ not left-distributive), DEFINE the class with D INSIDE: **`AffBounded Tg = Σ D c M, GoodT ι D × c<ε₀ × ValidMult M × (∀T w → Tg T w ≤ (D w ⊕ (T w ⊕ c))⊗M)` — the INNER-AFFINE functionals** (NOT the two-sided `D₁⊕(T⊕c)⊗M⊕D₂` form previously predicted — inner-affine is strictly simpler and suffices; the predicted form was never needed). Then NOTHING ever escapes a multiplier. PROVED: embeddings `AffBounded-lin/const/add/id/S` (lin: D=Z; const: D=Ta, the argument-slot ignored; add: D=C,c=Z,M=ω); **`AffBounded-∘`** (composition: enlarge inner zone to Y′=((Df⊕Dg)⊕(T⊕(cg⊕cf)))⊗Mg, then Df,Y,cf all ≤ Y′ and the TRIPLE ABSORPTION `⊕-trip-≤-⊗ω : x⊕(x⊕x) ≤ x⊗ω` packs them — **replaces crux entirely at the price of one ω factor in the multiplier** Mg⊗(ω⊗Mf); also `⊕-dup-≤-⊗ω : x⊕x≤x⊗ω`); **`aff-orbit-≤`** (iteration dominated by MDom body b↦(b⊕c)⊗(ω⊗M) from start D w⊕T w, INVARIANT: majorant stays ≥ D w so the inner D can be replaced by the majorant and the doubled zone absorbed by the extra ω; no GoodT needed for the bound); **`orbit-good-aff`** (GoodT + AffBounded ⟹ Torbit GoodT — the recursor hypothesis holds UNIFORMLY on the whole class, subsuming orbit-good-lin/const/add); **`AffBounded-orbit`** (orbit stays inner-affine, SAME D and c, multiplier raised to ω^((ω⊗M)⊗ω), still ValidMult via validMult-orbit ⟹ arbitrary finite recursor nesting stays in class, one ω-exponentiation per level). Compiled clean FIRST TRY. **STATUS AFTER 39: the transformer-class ARITHMETIC for the first-order fragment is COMPLETE (one class, closed under id/S/∘/orbit, contains all combinator shapes, orbit-good uniform). REMAINING for the fragment's fundamental theorem is PLUMBING not arithmetic: (ii) the GoodL/BndL relation over a CONCRETE first-order syntax carrying AffBounded at ι⇒ι (combinators port from F; Iter discharged by orbit-good-aff), (iii) height connection via Majorant.height-≤-μ + Good-ι-to-<ε₀. Conjecture for FULL System T still open: super-linear transformers (squaring, ω^) lie outside AffBounded; sq-orbit bounds them individually; no closed class containing them known.** 39 modules, full rebuild pending final check, nothing committed.
  - `…MultHereditaryFT` (added 2026-07-03, 40 modules, no postulates, EXIT 0, in tour): **THE DESIGN-F FUNDAMENTAL THEOREM — unconditional `dialogue-height-<-ε₀` for fragment T⁺, including a previously-blocked ground-S family. First-try compile.** Design: `ExtraA σ τ : 𝕋(σ⇒τ)→𝓤₀` with `ExtraA _ (arrow) = 𝟙` (MATCH CODOMAIN FIRST — this is what makes K/S obligations REDUCE at abstract types, dissolving the "polymorphic type-case obstacle" WITHOUT concrete-syntax restrictions), `ExtraA ι ι = AffBounded`, `ExtraA (arrow) ι = 𝟙`. `LA ι = GoodT ι`, `LA (σ⇒τ) T = (maps LA→LA) × ExtraA σ τ T` (note `LA(ι⇒ι) T` DEFINITIONALLY `= GoodT(ι⇒ι) T × AffBounded T`). `BndA`/`GoodA` = F's Bnd/Good with LA for GoodT; structural proofs port VERBATIM. Refinement paid at exactly 4 spots (all module-39 lemmas): Succ↦AffBounded-id, Ω↦AffBounded-S, K-inner-constant↦AffBounded-const (via case-split helper `extraA-K`), Iter-partial-app↦AffBounded-add + orbit's GoodT by `orbit-good-aff` ⟹ **RECURSOR UNCONDITIONAL IN THE PREDICATE** (the iterated arg's own LA supplies AffBounded — no hypothesis). Fragment `T⁺`: Ω/Zero/Succ/Iter/K/· + S at `Sh⁺` (shared arg function-typed, as T★) + **NEW `Sf⁺`: shared arg ι, FUNCTION-TYPED RESULT τ, arbitrary middle σ** — in the blocked family ("ground S with function-typed σ or τ"), closes here because ALL its ExtraA obligations reduce to 𝟙 (codomain-first) and Design F's transformer mechanism does the structure. Embeds into T₁ (both S ↦ S₁), `goodμ : (t : T⁺ σ) → GoodA σ (μ (e t))`, payoffs `μ-<-ε₀`/`height-<-ε₀`/`dialogue-height-<-ε₀ : (t : T⁺ ((ι⇒ι)⇒ι)) → height (⟦e t⟧₁ generic) < ε₀` — ALL UNCONDITIONAL. **HONEST SCOPE: T⁺ ⊉ T★ — missing (a) all-ground `Sg★` (ρ=σ=τ=ι): diagonal needs a JOINT binary affine bound Aff₂ (T Ta Tν w ≤ (D⊕(Ta w⊕(Tν w⊕c)))⊗M); analysis shows Aff₂ closes it (zone-substitution + quad absorption + y-≤-⊗; note ⊕ is quasi-commutative up to ⊗ω: b⊕a ≤ (a⊕b)⊗ω) BUT consistency then demands Affₙ for ALL all-ground arities (S at τ=ι⇒ι forces Aff₃ on φ, etc.) = the n-ary joint refinement over all-ground types — a real but well-defined development (JBnd-style recursion, zone accumulated left, c at the end via ⊕-assoc); (b) ground-S with function-typed MIDDLE σ and ground result: affine data of the diagonal must DEPEND on the fn-arg's data (Howard's transformer-of-data tower) — genuinely beyond any fixed-data ExtraA. UNIFYING T★ and T⁺ = the Affₙ upgrade (next step candidate). Full conjecture OPEN.** 40 modules, full rebuild EXIT 0, nothing committed.
  - `…MultHereditaryFAff2` (added 2026-07-03, 41 modules, EXIT 0, in tour, first-try compile): **the BINARY JOINT affine class `Aff₂`** — two ground transformer args inside ONE multiplied zone: `Aff₂ T = Σ D c M, GoodT D × c<ε₀ × ValidMult M × (∀T₁ T₂ w → T T₁ T₂ w ≤ (D w⊕(T₁ w⊕(T₂ w⊕c)))⊗M)` (Hereditary's joint-bound move at the transformer level). Zone is QUASI-COMMUTATIVE (reorder/duplicate costs ⊗ω: new `⊕-quad-≤-⊗ω : x⊕(x⊕(x⊕x)) ≤ x⊗ω`, `S-≤-⊗ω : S Z≤M → S M ≤ M⊗ω`). PROVED: `Aff₂-apply` (currying: fix T₁ ⟹ AffBounded at summand D⊕T₁, pure ⊕-assoc), `Aff₂-K`, `Aff₂-insert` (constant-in-first-arg from AffBounded — what K{ι⇒ι}{ι}·g needs), `Aff₂-Iter` (recursor's start+count JOINTLY, from AffBounded-orbit + S-≤-⊗ω absorption), and payoff **`AffBounded-Sg-diag : Aff₂ Tφ → AffBounded Tγ → AffBounded(λTa → Tφ Ta (Tγ Ta))`** (substitute γ's bound into φ's zone via zone-mono, enlarge to Y′=base⊗(ω⊗Mγ), quad-absorb; multiplier ((ω⊗Mγ)⊗ω)⊗Mφ) — THE ALL-GROUND S DIAGONAL ARITHMETIC. Honest: capping at 2 args is real; all-ground binary results demand Aff₃; arities ladder with no uniform top.
  - `…MultHereditaryFJT` (added 2026-07-03, 42 modules, EXIT 0, in tour): **THE JOINT FUNDAMENTAL THEOREM — unconditional height<ε₀ for fragment `T₂ ⊇ T★` (strictly), the LARGEST fragment with the ε₀ bound in the development.** Extra extended one arity rung: `Extra ι ι = AffBounded`, `Extra ι (ι⇒ι) = Aff₂`, else 𝟙 — now matching DOMAIN FIRST (arrow domain ⟹ 𝟙 reduces at abstract codomains; price: ground domain must see codomain shape ⟹ FT's Sf⁺ splits in two: `Sfh₂` result middle function-typed, `Sfr₂` result ι⇒κ₁⇒κ₂). LJ/BndJ/GoodJ port from F/FT. Joint obligations paid by FAff2: K-outer↦Aff₂-K, K at σ=ι⇒ι↦Aff₂-insert (case-split helpers extraJ-K-outer/inner; NOTE Agda exact-split warning if last clause is catch-all `σ (τ₁⇒τ₂)` — split all 4 σ-cases explicitly), Iter's two ground args↦Aff₂-Iter, **all-ground S diagonal↦AffBounded-Sg-diag ⟹ BOTH Iter AND Sg₂ UNCONDITIONAL in the predicate**. Fragment `T₂`: Ω/Zero/Succ/Iter/K/· + 4 S families (`Sg₂`=Sg★, `Sh₂`=Sh★, `Sfh₂`, `Sfr₂`). `emb★ : T★ → T₂` + `emb★-coherent` (needs `ap₂` from UF.Base, NOT in MLTT.Spartan) witness T₂ ⊇ T★ over the T₁ semantics; T₂ ⊋ T★ via Sfh₂/Sfr₂. Payoffs μ-<-ε₀/height-<-ε₀/`dialogue-height-<-ε₀ : (t : T₂ ((ι⇒ι)⇒ι)) → height(⟦e t⟧₁ generic) < ε₀`. **HONEST SCOPE: T₂ misses exactly ONE FT instance family — Sf⁺ at result ι⇒ι (diagonal = all-ground BINARY fn; its Aff₂ obligation needs TERNARY joint data on φ:ι⇒σ⇒ι⇒ι⇒ι which Extra doesn't carry) = the Aff₃ rung; the n-ary zone calculus is the visible next step (same arithmetic one arity up, but ladder has no uniform top — a UNIFORM Affₙ/JBnd-style recursion over all-ground types would subsume the ladder). Still outside: ground-S w/ function-typed middle + ground result (transformer-dependent data, Howard tower), super-linear transformers. Conjecture OPEN.** NOTE: agda interface files live in `_build/2.9.0/agda/source/...` (NOT next to sources) — for a true clean rebuild `rm -rf _build/2.9.0/agda/source/EffectfulForcing/DialogueTreeHeight`. 42 modules, clean rebuild verified (42 checked, 0 warnings), nothing committed.
  - `…MultHereditaryFAffN` (added 2026-07-03, 43 modules, EXIT 0, in tour): **THE TYPE-LEVEL ZONE CALCULUS — the whole Affₙ arity ladder in ONE recursion on the type.** `BoundT τ T D c M`: ground arg accumulated into left summand D (`BoundT (ι⇒τ) T D c M = ∀T₁ → BoundT τ (T T₁) (D⊕T₁) c M` — CURRYING IS DEFINITIONAL), ground value ≤ (D w⊕c)⊗M, **collapses to 𝟙 at any function-typed argument** (`BoundT ((σ₁⇒σ₂)⇒τ) _ = 𝟙` — no joint data across fn args = the Howard-tower boundary made definitional). `Collapses τ` (some arg of τ is fn-typed) + `BoundT-collapse` ⟹ trivial discharge past the all-ground prefix; `JAff τ T = Σ D c M, GoodT D × c<ε₀ × ValidMult M × BoundT`. Toolkit: `S-⊕-ι : S a⊕ι[m] ＝ S(a⊕ι[m])` (numerals have no limit stage), `⊕ι-≤-⊗ι/⊕ι-≤-⊗ω : S Z≤K → K⊕ι[m] ≤ K⊗ι[m+1] ≤ K⊗ω`, `⊕-insert-mid`, `BoundT-mono-D/M`, `JAff-collapse/id/S/const/add/K-outer/insert/Iter`, `JAff-to-AffBounded` (assoc transport). **ENGINE `BoundT-absorb`**: rebase accumulated summand A→B given ∀w A w ≤ (B w⊕c′)⊗K and c≤c′, cost = ONE SUCCESSOR OF K PER REMAINING GROUND ARG (definitional X⊗S K = X⊗K⊕X!), total multiplier (K⊕ι[dp τ+1])⊗M, weakened to (K⊗ω)⊗M. **PAYOFF `JAff-Sg-diag` : (τ)(Tφ Tγ) → JAff(ι⇒ι⇒τ) Tφ → JAff(ι⇒ι) Tγ → JAff(ι⇒τ)(λTa→Tφ Ta(Tγ Ta)) — the ground-S diagonal for ANY result type τ**, same multiplier ((Mγ⊗ω)⊗ω)⊗Mφ as Aff2's τ=ι case. GOTCHA fixed: JAff-K-outer at τ=τ₁⇒τ₂: BoundT((τ₁⇒τ₂)⇒ι)=𝟙 REGARDLESS of τ₁ (second arg is an arrow) — don't case-split τ₁.
  - `…MultHereditaryFNT` (added 2026-07-03, 44 modules, EXIT 0, in tour, first-try compile): **THE ALL-GROUND-S-COMPLETE FUNDAMENTAL THEOREM — fragment `T₃` with ground-S at EVERY result type, unconditional height<ε₀.** ExtraN is ONE clause pair: `ExtraN ι τ = JAff(ι⇒τ)`, arrow domain 𝟙 — JAff's own collapse makes the uniform predicate viable (no per-type case analysis as in FT/FJT). LN/BndN/GoodN port from F. Obligations: Succ/Ω↦JAff-id/S; K outer↦JAff-K-outer, inner by σ-shape (JAff-const σ=ι / JAff-insert σ=ι⇒σ′ pulling pr₂ lTa=JAff σ Ta / JAff-collapse σ=(fn)⇒σ′); Iter↦JAff-Iter+JAff-add+orbit-good-aff via JAff-to-AffBounded — UNCONDITIONAL. **Fragment T₃: Ω/Zero/Succ/Iter/K/· + THREE S families: `Sg₃ {τ}` (shared ι, middle ι, ANY τ — τ=ι is Sg★, τ=ι⇒ι is FJT's missing Aff₃ family, all arities + mixed τ), `Sh₃` (shared fn-typed), `Sx₃ {σ}{τ}` (shared ι, ANY middle σ, `Collapses τ` WITNESS as constructor argument).** T₃ ⊇ T★ strictly (emb★+coherence). Payoffs μ/height/dialogue-height-<-ε₀ unconditional. **STRUCTURAL FINDING — THE FRAGMENTS ARE GENUINELY INCOMPARABLE AT THE FRONTIER: the remaining S family (shared ι, fn-typed middle σ, all-ground τ) is contained partially in T⁺/T₂ (whose COARSER predicates demand no data at the diagonal's type) but NOT in T₃ (whose FINER predicate — which is what buys every all-ground S — demands data there that can't be produced). NO single fixed-data predicate can have both: more data ⟹ more diagonals provable but more obligations elsewhere. The JOIN of the fragment lattice = data depending on the function argument's data = HOWARD'S TRANSFORMER TOWER — now the precisely-pinned single remaining structural step for full first-order+S coverage (super-linear transformers the remaining arithmetic one).** STATE AFTER 44: Design-F line complete through the zone calculus; fragments T★ ⊂ T₂, T★ ⊂ T₃ (T⁺,T₂,T₃ pairwise incomparable at the tower frontier); everything --safe --without-K, no postulates; 44 modules clean rebuild 0 warnings; NOTHING COMMITTED; conjecture OPEN.
  - `…MultHereditaryG` (added 2026-07-03, 45 modules, EXIT 0, in tour, first-try compile): **DESIGN G — THE TRANSFORMER TOWER'S GROUND LAYER (Howard data maps over Design-F transformers).** Mirrors Design F one level up: `𝔻 ι = GFun = Σ B, GoodT ι B` (ground transformers ARE their own data, `selfD` = pairing, `selfD-bnd` = ≤-refl), `𝔻(σ⇒τ) = 𝔻σ→𝔻τ`; `BndD ι a d = ∀w → a w ≤ pr₁ d w`, `BndD (σ⇒τ) T F = ∀ g dg → BndD σ g dg → BndD τ (T g) (F dg)` (pure Howard shape, NO accumulator — accumulator-free is what makes it work; an accumulator version FAILS mono-A at fn-args by contravariance). **PAYOFF: the STRUCTURAL half is FREE — `BndD-S` closes for ALL ρ,σ,τ in ONE LINE** (diagonal data = application `(dφ da)(dγ da)`), `BndD-K` one line, app literal ⟹ ALL the instance-splitting of FT/FJT/FNT was an artifact of fixed data. **RESIDUAL = Iter, exactly as F's orbit-good one level up: arbitrary data maps have unbounded orbits (λb→ω^∘b legal), so maps must be `Tracked`** = dominated by inner-affine `Taff D c M = λT w → (D w⊕(T w⊕c))⊗M`; the whole FAff calculus TRANSFERS BY REUSE: `Tracked-∘` = AffBounded-∘ on dominating transformers (via `Taff-aff` = ≤-refl data + `Taff-mono`), `BndD-orbit` = aff-orbit-≤ on Taff (chain₁: BndD along iterates; chain₂: bnd-fns dominated by iter Taff), `dOrbit`/`Tracked-dOrbit` (orbit map tracked at ω^((ω⊗M)⊗ω) — nesting stays in class), `BndD-Iter` assembled; `Tracked-id/const/dΩ` (NOTE: dΩ must output the NORMALIZED triple λw→S(pr₁ b w) — an (D,Sc,M⊗ω)-triple-map is NOT tracked since output multiplier would depend on input's). **KEY STRUCTURAL FINDING — THE TOWER IS SELF-SIMILAR (the honest discovery of the design pass):** a fundamental theorem must carry Tracked hereditarily (𝕂 on data); 𝕂's own S-diagonal at fn-typed middle demands tracking-of-tracking — each storey's diagonal needs the NEXT storey, because a term's data map has the SAME combinator structure as the term. Any FIXED number of storeys reproduces the fragment lattice shifted a level; T₁-completeness needs the whole ω-tower. TWO PINNED ROUTES: (i) ℕ-indexed tower predicate 𝕂 n (𝕂 0 = 𝟙, 𝕂(n+1) = maps × zone × 𝕂 n-components) — each term needs only its own DEPTH many storeys ⟹ level-polymorphic fundamental theorem `∀t → Σ n, tracked-to-depth n`; levels ≥2 look structurally identical (all affine-data maps) so storeys may COLLAPSE to one self-referential level; (ii) Howard's actual route: syntactic ordinal ASSIGNMENT (not a logical relation). 45 modules, clean rebuild 0 warnings, nothing committed, conjecture OPEN.
  - **HOWARD-TOWER DESIGN DECISION (2026-07-03, user steer: "I think we should trust Howard's insights"; design note `dialogue-tree-height-howard-tower.md` added to the project dir):** Analysis of the two Howard readings: **(a) operational** (height ≤ symbolic head-reduction length of t·generic + Howard's ordinal-decrease) — mathematically classical but needs infrastructure the repo LACKS (operational semantics for combinatory T with oracle, adequacy vs the dialogue denotational semantics, decrease lemma; and the recursor-unfolding consumes the COUNT'S VALUE so the TwoComponent (A)/(B) magnitude split reappears inside it) — a large separate development; **(b) structural (ADOPTED)**: Howard's vectors have length = TYPE LEVEL ⟹ the G-regress is not infinite: the tracking tower is an **ℕ-indexed hierarchy `Trk n σ d` with depth bounded by the term's type level** — Howard's vectors AS a logical relation. Storey 0 trivial; storey n+1 at arrows = (maps preserving storey-(n+1)) × (JAff-style affine zone whose cross-diagonal obligations consume storey n); storey 1 = MultHereditaryG.Tracked (already proven, calculus by reuse). Fundamental theorem shape: `∀ t : T₁ σ → tracked-to-storey (level of t)` — LEVEL-POLYMORPHIC, no S/K instance restrictions ⟹ target = T₁-COMPLETE (full combinatory T with ground recursor). Simplification to try first: storeys ≥2 look structurally identical (affine-data maps over affine data) ⟹ hierarchy may COLLAPSE to 2 distinct storeys + indexed repetition = single new predicate. WATCH: storey-(n+1) S-diagonal at fn middle (consumption of storey n must be ARITHMETIC à la JAff-Sg-diag, not another map); Iter × storeys ≥2 (nesting raises multiplier one ω-exponent/level — the per-level ω-exponent is exactly the room). Also noted: ANY compositional bound relation with quantitative sub-ε₀ ground data reproduces itself one level up — the regress is intrinsic to the relation approach, and the level-indexed bound is what terminates it (= "one ω per type level", ε₀ = sup ω↑↑ℓ — the shape the conjecture always had). Even with T₁ complete, higher-type recursor (super-linear transformers) stays open. NEXT SESSION: build `Trk` (try the 2-storey collapse first), then the level-polymorphic fundamental theorem.
  - `…MultBump` (added 2026-07-03, 46 modules, EXIT 0, first-try compile, in tour): **the per-storey multiplier arithmetic of the Howard tower.** `bump M = ω^((ω⊗M)⊗ω)` (= the orbit raise of AffBounded-orbit/Tracked-dOrbit DEFINITIONALLY — `orbit-raise-is-bump = refl`), `bumpk k` its iterates. Laws: `bump/bumpk-valid` (ValidMult), `bump/bumpk-mono`, `≤-bump` (inflationary, via Epsilon0's `≤-ω^ : a ≤ ω^ a` + y-≤-⊗/x-≤-x⊗), `bumpk-≤-succ`, `bump/bumpk-<-ε₀`, **`⊗-≤-bump : X⊗X ≤ bump X`** (inflationarity + OmegaPoly's `ω^⊗ : ω^a⊗ω^b ＝ ω^(a⊕b)` + X⊕X ≤ (ω⊗X)⊗ω), **`bumpk-⊗-absorb : bumpk k M ⊗ bumpk k N ≤ bumpk (succ k)(M⊕N)`** (combine two fn-args' multipliers, each finitely reused, at cost ONE storey). Storey accounting: composition free, products one bump, Iter-nesting k↦succ k — one ω-exponent per storey, ε₀ the join.
  - `…MultHereditaryG2` (added 2026-07-03, 47 modules, EXIT 0, in tour): **storey 2's first consumption point — the data-level ground-S diagonal.** `Tracked₂` (Aff₂-shape on bound fns) + algebra (`Tracked₂-apply` currying via ⊕-assoc transport ⁻¹, joins fixed arg's bound into summand; `Tracked₂-K`; `Tracked₂-insert`) and **`Tracked-diag : Tracked₂ F → Tracked G → Tracked (λ b → F b (G b))`** — by PURE REUSE (the Tracked-∘ trick): dominating transformers `Taff₂`/`Taff` are Aff₂/AffBounded by ≤-refl, `AffBounded-Sg-diag` closes their diagonal, data bound rides by `Taff₂-mono₂`. **The ARITHMETIC for the two-storey collapse is now complete (MultBump + G2 + G); the remaining construction is the tower predicate `Trk` itself (mutual with a zone-bound recursion over types; DESIGN WORKED OUT this session: fn-arg positions contribute (i) their additive part (Dh⊕ch) into the zone accumulator LIKE A GROUND ARG and (ii) their multiplier Mh through bumpk; the Σ(D,c,M,k) sits OUTSIDE ground-arg quantifiers (joint zone) and the fn-arg quantifiers come FIRST (hoisted, Hereditary-FunArgs-style OR accumulator-threaded); KEY INVARIANT making the frontier diagonal close: outputs' multipliers are FIXED (rigid) per subterm, only additive parts flow affinely in ground inputs — an invariant HELD BY THE SHAPE of the zone predicate itself) and the level-polymorphic fundamental theorem over full T₁.** 47 modules, clean rebuild 0 warnings, nothing committed, conjecture OPEN.
  - `…MultHereditaryH` (added 2026-07-03, 48 modules, EXIT 0, FIRST-TRY COMPILE incl. termination of the mutual recursion, in tour): **DESIGN H STAGE 1 — THE HOWARD TOWER PREDICATE DEFINED.** `𝕂 ι = GFun`; `𝕂 (σ⇒τ) = Σ F : 𝕂σ→𝕂τ, ZPack σ τ F` where ZPack = Σ (D,c,M,j), GoodT D × c<ε₀ × ValidMult M × M<ε₀ × ZApply-from-empty-accumulators. THREE ACCUMULATORS threaded along the argument spine: A : 𝓑→𝓑 (additive zone: ground args contribute pr₁ b, fn args contribute `kadd` = their pack's λw→D w⊕c), MH : 𝓑 (multiplier pool: `kmult` = pack's M), JH : ℕ (bump budget: `kbudget` = pack's j; local `_+ℕ_` defined to avoid coproduct clash). Ground bound: `pr₁ k w ≤ ((D w ⊕ (A w ⊕ c)) ⊗ bumpk (j +ℕ JH) (M ⊕ MH))` — MultBump's absorptions service the pool. **THE DECISIVE CLAUSE: `PackDom` — ZCont at arrow types constrains the PARTIAL APPLICATION'S OWN PACK (kadd ≤ current zone-value-form; kmult ≤ bumpk (j+JH)(M⊕MH); budget in SEMANTIC form ∀P → bumpk jk P ≤ bumpk (j+JH) P, avoiding ℕ-order imports). This is the rigid-multiplier invariant HELD BY THE SHAPE — it terminates the module-45 self-similar regress: γ a's pack is FORCED affine-in-a with fixed multiplier data, so the fn-middle ground-S diagonal assembles packs arithmetically (JAff-Sg-diag/Tracked-diag pattern), no next storey.** Structure: mutual 𝕂/ZPack/ZApply/ZCont/PackDom + kadd/kmult/kbudget — termination via ZApply-consumes-one-arg / ZCont-continues (arg and result types proper subterms; size-change accepted). Toolkit: `zone-mono-A`, mutual `ZCont-mono-A`/`ZApply-mono-A` (NO contravariance — argument data ∀-quantified, PackDom covariant in A). **STAGE 2 REMAINING (the last construction for the T₁-complete theorem): (i) combinator packs — dK (project+insert), dS (application at data + PackDom-driven diagonal pack assembly), base (id/Ω), dIter (consume 𝕂(ι⇒ι)'s pack ⟹ Tracked ⟹ MultHereditaryG.dOrbit/BndD-orbit; orbit-raise = ONE bump so j↦j+1); (ii) the bound relation BndH σ (T : 𝕋 σ) (k : 𝕂 σ) (Howard-shaped, mirrors G.BndD with 𝕂); (iii) LT σ T = GoodT-maps × Σ k, BndH (LT ι = GoodT ι); GoodH = Σ T, LT × ∀w Bnd (F-layer verbatim); fundamental theorem over FULL T₁ (no instance restrictions) + height payoffs. NOTE for stage 2: AffBounded-on-good-inputs variant of orbit-good-aff needed (BndH gives Tg T w ≤ pack-form only for GOOD T; orbit-good-aff demands ∀T — reprove with aff-good hypothesis, internals only use it at good iterates via GoodT-iter).** 48 modules, clean rebuild 0 warnings, nothing committed, conjecture OPEN.
  - `…MultHereditaryH2` + `…MultHereditaryH3` (added 2026-07-03, 50 modules, EXIT 0, in tour): **DESIGN H STAGES 2a+2b — THE REBASE, the tower's load-bearing lemma, PROVED.** H2 (arithmetic): ℕ-budget lemmas (+ℕ-zero-right/succ-right/comm/assoc/shuffle), bumpk calculus (`bumpk-+ : bumpk (a+b) P ＝ bumpk a (bumpk b P)`, `≤-bumpk` inflationary, `bumpk-pad`, `ω-≤-bump` via SZ-⊗, `B-step : bumpk k P ⊗ ω ≤ bumpk (succ k) P` given ω≤P), **`zdom-ext`** (same summand X to both accumulators keeps base-domination at cost ⊗ω: H≤R⊗B via ⊕-increasing-right H c THEN hyp — c also ≤ R⊗B via increasing-left; three summands → ⊕-trip-≤-⊗ω; zones reassociated by ⊕-assoc chains), **`pool-ext`** (same mh to both pools costs one slack: dup→B-step), `rbb : type → ℕ → ℕ → ℕ` (spine budget: ι↦succ(u+e), ground-arg↦(succ u,e), fn-arg↦(succ u,succ e)). **KEY DESIGN POINT: pool-domination hypothesis on the RAW pool with explicit slack e — a bumped-pool hypothesis causes bumps-of-bumps EXPLOSION; raw+slack makes pool substitution an ADDITIVE budget shift** (`mult-rebase`: I3 then bumpk-mono I2 then bumpk-+ ⁻¹ then +ℕ-shuffle). H3 (the rebase): `ZCont-rebase`/`ZApply-rebase` mutual on the type, invariants: I1 base-dom at budget (u+e)+JH′; I2 raw-pool at slack e; I3 semantic budget-dom ∀P → bumpk (j+JH) P ≤ bumpk (u+JH′) P; I4 ω-floor ω ≤ (M′⊕MH′). Steps: ground-arg (u↦succ u; zdom-ext + B-step; **I3 ALSO weakens by one ≤-bump — forgetting this was the one compile bug**), fn-arg (additionally e↦succ e via pool-ext with ⊕-assoc transports both sides; I3 re-derived via bumpk-+ + +ℕ-assoc chains; I1 index-eq E built from succ-right/assoc aps), ground merges via mult-rebase + ⊗-≤-bump. Conclusion budget rbb τ u e — **rbb's clauses make recursive calls match DEFINITIONALLY**. PackDom components rebase by same chains + padding via `rbb-split : rbb τ u e ＝ succ(u+e)+d` / `rbb-split-u : ＝ u+d` (Σ-witnessed, avoiding ℕ-order). **REMAINING (stage 2c/2d, now pure assembly): combinator packs — each = instantiate the arg's pack then ONE rebase (Zero/Succ/Ω trivial packs; K = project+insert; S = feed (Fγ b) into Fφ's fn-slot — PackDom of (Fγ b) supplies I1/I2/I3 EXACTLY (that's the design's payoff); Iter = 𝕂(ι⇒ι)-pack → Tracked → G.dOrbit/BndD-orbit, raise = one bump); BndH σ (T : 𝕋σ)(k : 𝕂σ) Howard-shaped; LT σ T = GoodT-maps × Σ k, BndH (LT ι = GoodT ι); GoodH = Σ T, LT × ∀w Bnd; goodμ over FULL T₁; payoffs via height-≤-μ. Plus orbit-good-aff-on-good-inputs variant.** 50 modules, clean rebuild 0 warnings, nothing committed, conjecture OPEN.
  - `…MultHereditaryH4` (added 2026-07-03, 51 modules, EXIT 0, in tour): **DESIGN H STAGE 2c — THE FUNDAMENTAL-THEOREM SKELETON.** Three-layer predicate: `BndL σ w T φ` (F's Bnd with LT-args), `LT ι = GoodT ι`, `LT (σ⇒τ) T = maps × Σ k ꞉ 𝕂(σ⇒τ), BndH (σ⇒τ) T k`, `BndH ι T k = ∀w → T w ≤ pr₁ k w`, `BndH (σ⇒τ) T k = ∀ g kg → BndH σ g kg → BndH τ (T g) (pr₁ k kg)` (Howard-shaped), `GoodH σ φ = Σ T, LT × ∀w BndL`, `GoodH-ι-to-<ε₀` ✓. PROVED: GoodH-Zero/Succ/Ω (packs (Z,Z/ι[1],ω,0), one increasing-chain each — empty accumulators/pools reduce DEFINITIONALLY: (Z⊕prb)⊕Z ≡ Z⊕prb, ω⊕Z ≡ ω, bumpk 0 = id; WHERE-CLAUSES for pack tuples, inline Σ-nesting parens are error-prone), GoodH-app (maps only), **`pack-to-Tracked : ZPack ι ι F → Tracked F`** (the recursor bridge: `⊕-Z-left : Z⊕x ＝ x` [induction, dfunext fe at L], budget folds into multiplier bumpk (j+ℕ0) (M⊕Z), bumpk-valid ✓) — **and BndH (ι⇒ι) T k ≡ BndD (ι⇒ι) T (pr₁ k) DEFINITIONALLY (𝕂 ι = GFun = 𝔻 ι) ⟹ module-45 orbit engine (BndD-orbit/BndD-Iter/dOrbit/Tracked-dOrbit) applies DIRECTLY to the recursor's argument.** REMAINING FOR THE T₁-COMPLETE THEOREM (stage 2d, all patterns established): (i) GoodH-K {σ τ}: partial λkb→ka packs via kadd/inner rebase (case σ: D:=pr₁ ka for σ=ι / D:=kadd ka + ZApply-rebase of ka's pack for σ arrow; ALWAYS choose consumer pack-M with ω-FLOOR e.g. M⊗ω or ω, since rebase I4 needs ω ≤ pool); (ii) GoodH-S {ρ σ τ}: three nested partial packs; diagonal pack: instantiate kφ's pack ZApply with kh := (Fγ b)-outputs — **PackDom (Fγ b) supplies rebase I1/I2/I3 EXACTLY (the (48)-design payoff)** — then ONE ZCont-rebase; (iii) GoodH-Iter: data λkg → dsum∘dOrbit(pr₁ kg)(pack-to-Tracked …)-partials, packs from trk-components (outer pack (Z-ish, Z, ω, ~3), partial multipliers bump(bumpk jg Mg)⊗ω ≤ bumpk (2+jg) Mg ≤ pool-bumps, ground bound = trip-absorb of (D̂,ĉ ∈ kadd kg ∈ accumulator)); need orbit GoodT via orbit-good-aff-ON-GOOD-INPUTS variant (BndH+selfD gives AffBounded only at good T — reprove FAff.orbit-good-aff with aff-good hypothesis, internals only use good iterates via GoodT-iter); (iv) goodμ : (t : T₁ σ) → GoodH σ (μ (e? no — full T₁ directly, no embedding needed since all combinators close) + μ-<-ε₀/height-<-ε₀/dialogue-height-<-ε₀ via Majorant.height-≤-μ. **This yields THE COMPLETE FIRST-ORDER FUNDAMENTAL THEOREM: height(dialogue-tree t) < ε₀ for ALL combinatory System T with ground recursor — no instance restrictions.** 51 modules, clean rebuild 0 warnings, nothing committed, conjecture OPEN (higher-type recursion remains).
  - `…MultHereditaryH5` (added 2026-07-03, 52 modules, EXIT 0, in tour): **stage 2d(i) — the recursor's GoodT side over the tower, COMPLETE.** `orbit-goodT : (Tg)(gTg : GoodT(ι⇒ι) Tg)(D c M)(GoodT D)(c<ε₀)(ValidMult M)(M<ε₀)(aff-good : ∀T → GoodT ι T → ∀w → Tg T w ≤ ((D w⊕(T w⊕c))⊗M)) → ∀Ta → GoodT ι Ta → GoodT ι (Torbit Tg Ta)` — mirror of FAff.aff-orbit-≤/orbit-good-aff with GoodT-iter supplying goodness at each aff application. `BndH-to-aff-good : (Tg kg) → BndH (ι⇒ι) Tg kg → Σ D c M, sides×(good-inputs affine bound)` — destructure kg's pack, instantiate bndg at selfD (T,gT) with ≤-refl bound, dominate via the inlined pack-to-Tracked transport (⊕-Z-left), multiplier bumpk (j+ℕ0)(M⊕Z) with bumpk-valid/bumpk-<-ε₀. Chained: full recursor GoodT-side from any tower argument. **STAGE 2d REMAINING — THE ITER PACK BLUEPRINT (fully worked out, all chains verified on paper): GoodH-Iter: kI = (FI, packI); FI kg = (FI2 kg, pack2 kg); FI2 kg ka = (FI3 kg ka, pack3 kg ka); FI3 kg ka kν = G.dsum (G.dOrbit (pr₁ kg) (pack-to-Tracked …) ka) kν. BndH-Iter = G.BndD-Iter DIRECTLY (BndH (ι⇒ι) T k ≡ BndD (ι⇒ι) T (pr₁ k) definitionally; the pr₁-projection chains through the 𝕂-pairs match BndD's F-applications). trk-components D̂ ≡ Dg, ĉ ≡ cg DEFINITIONALLY (pack-to-Tracked pattern-matches, Σ-eta), M̂ ≡ bumpk (jg+ℕ0)(Mg⊕Z); pr₁ (dOrbit …) w ≡ ((D̂ w⊕pr₁ ka w)⊕ĉ)⊗bump M̂ (orbit-raise-is-bump refl). PACKS: pack3 (D₃ := pr₁ orbitb [GoodT = pr₂ orbitb], Z, ω, 0): ob⊕ν ≤ (ob⊕(Z⊕ν))⊗ω one chain. pack2 (D₂ := λw→D̂ w⊕ĉ [GoodT: pres needs ĉε ✓], Z, M₂ := bumpk 3 M̂, 0): PackDom-pd: trip-absorb of D̂,ka,ĉ ≤ zone → zone⊗ω⊗bump M̂ ≤ zone⊗bump²M̂ ≤ bumpk 3; pm: ω ≤ bumpk 3 M̂ [ω-≤-bump chains]; pj trivial; inner ground: ob⊕ν: dup-absorb → zone⊗bump³M̂ = M₂ ✓. packI (DI := λ_→Z, Z, MI := ω, jI := 3): kadd kg = Dg⊕cg ≡ D̂⊕ĉ IS the accumulator entry (increasing chains); pm: bumpk 3 M̂ = bumpk (3+(jg+0))(Mg⊕Z) ＝[zero-right ap] bumpk (3+jg) Mg ≤ bumpk (jI+(0+ℕjg))(MI⊕(Z⊕Mg)) with jI = 3, 0+jg ≡ jg DEF ⟹ REFL index match [Mg ≤ Z⊕Mg ≤ pool mono]; inner levels: same chains with ka,ν in accumulator, slack in the 3. LT-maps assembly: level-2 k₂ := (FI2 kg, pack2) from lTg's Σ-kg; level-3 k₃ with ka := selfD (Ta,gTa); GoodT-side via BndH-to-aff-good + orbit-goodT + GoodT-⊕; BndL = FNT's GoodN-Iter bnd verbatim (orbit-pt + GoodT-iter). THEN: GoodH-K (helpers: `reflect : (σ)(T)(LT σ T) → Σ k, BndH` [ι: selfD+≤-refl; arrow: the Σ-component directly!]; const-packs via kD σ k [ι: pr₁ k; arrow: kadd] + ZApply-rebase of ka's pack, I2 e:=0, I4 via pack-M-ω-floor convention M := Ma⊕ω-style; outer j := rbc-based: **define `rbc : type → ℕ → ℕ` u-INDEPENDENT split constant with `rbb-＝ : rbb τ u e ＝ u +ℕ rbc τ e`** [rbc ι e = succ e; rbc (ι⇒τ) e = succ (rbc τ e); rbc (fn⇒τ) e = succ (rbc τ (succ e))] — needed since pack budgets must be chosen BEFORE args arrive); GoodH-S (three nested partials; diagonal: instantiate kφ's ZApply at kh := Fγ b — **PackDom (Fγ b) delivers I1/I2/I3 exactly** — one ZCont-rebase); goodμ : (t : T₁ σ) → GoodH σ (μ t) [T₁ from Majorant, no embedding needed]; μ-<-ε₀/height-<-ε₀/dialogue-height-<-ε₀ via height-≤-μ + GoodH-ι-to-<ε₀.** 52 modules, clean rebuild 0 warnings, nothing committed, conjecture OPEN.
  - `…MultHereditaryH6` (added 2026-07-03, 53 modules, EXIT 0, in tour): **STAGE 2d(ii) — `GoodH-Iter`, THE RECURSOR OVER THE HOWARD TOWER, PROVED.** Structure: parameterized `module IterPack (kg : 𝕂 (ι⇒ι))` with deep projections (Fg/Dg/cg/Mg/jg/gDg/cgε/vMg/Mgε), trkg = pack-to-Tracked, M̂ = bumpk (jg+ℕ0)(Mg⊕Z), P = bump M̂, lemmas lemA : ω⊗P ≤ bumpk 2 M̂, lemA3, lemB : (ω⊗P)⊗ω ≤ bumpk 3 M̂ = M₂; data maps FI3 ka kν = dsum (dOrbit Fg trkg ka) kν, FI2, FI; `trip3 : a,b,c ≤ z → (a⊕b)⊕c ≤ z⊗ω` helper. PACKS: pack3 (pr₁(ob ka), Z, ω, 0) one chain; pack2 (D₂ = λw→Dg w⊕cg, Z, bumpk 3 M̂, 0) — PackDom-pd via trip3+lemA3, ground via dup-absorb+lemB; packI ((λ_→Z), Z, ω, 3) — **pm₂ = the BUDGET IDENTITY bumpk 3 M̂ ＝ bumpk (3+jg) Mg via bumpk-+ ⁻¹ + ap (+ℕ-zero-right): the recursor costs the argument's budget PLUS THREE, Howard's accounting literal**; inner levels via zoneOf/z-in/sum-Dg/sum-cg/obzB generalized chains. bndHI = BndD-Iter DIRECTLY (definitional); maps: orbitG via BndH-to-aff-good + orbit-goodT (deep pr-projections of the Σ); maps₂ reuses FI2 kg (Ta,gTa) + bndH₂ at selfD with ≤-refl; bndL = F's orbit-pt verbatim. COMPILE NOTES: transport directions for ⊗-assoc need ⁻¹ when moving subject from (a⊗b)⊗c-form; explicit `tail`-bindings needed where ⊕-increasing-right's second arg is ambiguous (unsolved metas otherwise). **REMAINING FOR THE T₁-COMPLETE FUNDAMENTAL THEOREM: GoodH-K (reflect-helper + const-packs + one ZApply-rebase; rbc/rbb-＝ u-independent budget constant), GoodH-S (three nested partials, diagonal = instantiate kφ's ZApply at kh := Fγ b, PackDom (Fγ b) supplies rebase I1/I2/I3, one ZCont-rebase), then goodμ : (t : T₁ σ) → GoodH σ (μ t) + payoffs (Zero/Succ/Ω/Iter/app DONE — only K,S remain!).** 53 modules, clean rebuild 0 warnings, nothing committed, conjecture OPEN.
  - `…MultHereditaryHT` (added 2026-07-03, 54 modules, EXIT 0, in tour): **THE ITERATOR-FRAGMENT FUNDAMENTAL THEOREM OVER THE HOWARD TOWER — the tower validated END-TO-END.** Fragment `TI` = Ω/Zero/Succ/Iter/app (arbitrarily nested oracle-driven ground recursion, counts computed by dialogue). `goodμ : (t : TI σ) → GoodH σ (μ (e t))` = a FOUR-LINE induction (all closures proven in H4/H6); payoffs `μ-<-ε₀`, `height-<-ε₀`, **`dialogue-height-<-ε₀ : (t : TI ((ι⇒ι)⇒ι)) → height (⟦ e t ⟧₁ generic) < ε₀` — UNCONDITIONAL**, heights climbing exactly the ω-exponent tower (3 bumps per recursor nesting). **FINAL REMAINING for T₁-complete: GoodH-K + GoodH-S (blueprints in the H5/H6 entries: reflect-helper `reflect : (σ)(T)(LT σ T) → Σ k, BndH σ T k` [ι: selfD+refl; arrow: the Σ-component]; const-packs via k-cont with per-σ rebase, pack budgets as rbb-values, rbc u-independent constant; S-diagonal: instantiate kφ's ZApply at kh := Fγ b, PackDom (Fγ b) supplies I1/I2/I3, one ZCont-rebase) + goodμ over full T₁.** 54 modules, clean rebuild 0 warnings, nothing committed, conjecture OPEN (higher-type recursion beyond).
  - `…MultHereditaryH7` (added 2026-07-04, 55 modules, EXIT 0, in tour): **`GoodH-K` FULLY POLYMORPHIC — six of seven T₁ constructs closed in the final predicate.** Content: `rbc : type → ℕ → ℕ` + `rbb-＝ : rbb τ u e ＝ u +ℕ rbc τ e` (u-independent split constant; proofs via +ℕ-succ-right ⁻¹ chains); `bumpk-pad-left`; `reflect : (σ)(T)(LT σ T) → Σ k, BndH σ T k` (ι: selfD+refl; arrow: pr₂ lT); constant-pack data `kD/kM/kJ` (ι: pr₁ k/ω/0; arrow: kadd/kmult⊗ω/rbb (σ₁⇒σ₂) ja 0) + sides (kM = Ma⊗ω NOT Ma⊕ω — **ValidMult is NOT ⊕-closed**, use ⊗ω for the floor); `ccore` (constant continuation at own-data pack: PackDom by increasing chains, behaviour by ZApply-rebase at u=ja,e=0 — concluding budget = pack budget LITERALLY); `ocore-ι`/`ocore-fn` (argument under outer trivial pack (Z,Z,ω,rbb σ 0 0), rebase at u=e=0); `ktail-ι/ktail-fn` (consume-and-forget the τ-arg BY CASES ON τ AS AN ARGUMENT — **Agda cannot `with` on a parent-clause/module variable**); `cpack`; `packK` (outer, by cases on σ; pm at σ-arrow: Ma⊗ω ≤ pool⊗pool ≤ bump pool then pad via rbb-split; pj = the transported ≤-refl along E : rbb σ ja 0 ＝ rbb σ 0 0 +ℕ ja from rbb-＝ + comm — **K IS BUDGET-NEUTRAL**); `GoodH-K` assembled (maps via reflect; BndH-K = λ…→bnda). COMPILE NOTES: where-clauses do NOT scope over a preceding λ's binder (make the binder a clause/where-fn parameter); x-≤-x⊗'s bump-index needs explicit filling (unsolved metas otherwise). **REMAINING FOR T₁-COMPLETE: GoodH-S ONLY (three nested partial packs + the diagonal: instantiate kφ's ZApply at kh := (Fγ-applied), PackDom supplies rebase I1/I2/I3 — the (48) payoff) + goodμ over T₁ + payoffs.** 55 modules, clean rebuild 0 warnings, nothing committed, conjecture OPEN.
  - **GOODH-S: COMPLETE PAPER DERIVATION (2026-07-04, fully worked, transcription pending — read this before building):** Structure: kδ = (Fδ, packδ) with Fδ ka = pr₁ (Fφ ka) (Fγ ka); BndH₃ = λ a ka bnda → bndφ a ka bnda (Tγ a) (Fγ ka) (bndγ a ka bnda) — ONE LINE; BndL = F's Good-S verbatim; maps thread pr₁'s. THE PACKS: **(1) TWO DESIGN FINDINGS: (a) ρ-ARROW (Sh-family, fn-typed SHARED arg) IS OBSTRUCTED in the current H-predicate: the diagonal uses ka twice, so the source budget contains kbudget ka TWICE (once via φ's JH, once inside kbudget (Fγ ka) ≤ jγ+JHₐ), but the pack-form allows JHₐ only once — bumpk(2·JHₐ) ≰ bumpk(jδ+JHₐ) for variable JHₐ. PIN: Sh needs a budget-form refinement (e.g. pool-entries pre-bumped/budget folded into multipliers at 𝕂-level). BUILD ρ=ι ONLY (JHₐ=0, MHₐ=Z, accₐ = λw→Z⊕pr₁ ka w): this is THE historically-blocked family (Sg★ + fn-middle-σ + all τ) — fragment T₄ = Ω/Zero/Succ/Iter/K-full/S-at-ρ=ι/app strictly beyond TI and all F-line fragments (misses only Sh). (b) γ's BUDGET MUST BE ABSORBED INTO THE DIAGONAL'S MULTIPLIER, NOT ITS BUDGET: naive e := succ(jγ) makes jδ = rbb τ u e contain jγ via rbc τ e (rbc is e-linear), which pack₂ cannot absorb (jγ arrives with kγ). FIX: Mδ := (Mφ⊗ω)⊗(Bγ⊗ω) where Bγ := bumpk (jγ+ℕ0)(Mγ⊕Z) — Bγ AS A POOL ELEMENT; then u := jφ +ℕ C₁, e ∈ {0,1} FIXED, jδ = rbb τ u e is jφ-linear/jγ-FREE.** (2) packδ (per σ-case): consume ka; instantiate packφ's ZApply at the γ-value (σ=ι: b := Fγ ka gives bγa : ∀w → pr₁(Fγ ka) w ≤ ((Dγ⊕(accₐ⊕cγ))⊗Bγ) from packγ-consumption; σ-arrow: kh := Fγ ka, PackDom of ZCont σ (Fγ ka) gives pdγ/pmγ/pjγ — THE (48) PAYOFF); then ONE ZCont-rebase to (accₐ, Z, 0, Dδ, cδ, Mδ, jδ) with Dδ := λw → Dφ w ⊕ (Dγ w ⊕ cγ), cδ := cγ⊕cφ: I1: 4-summand quad-absorb (Dφ,accₐ,X,cφ each ≤ Q := Y⊗(ω⊗Bγ'-with-pool-mono); reassoc a⊕((b⊕c)⊕d)＝a⊕(b⊕(c⊕d)) then ⊕-quad-≤-⊗ω; X-bound: γ-zone Dγ⊕(accₐ⊕cγ) ≤ Y⊗ω by trip3-reassoc since Dγ,cγ ≤ Dγ⊕cγ ∈ Dδ); multiplier factors ≤ pool-δ'-products since Bγ ≤ Mδ RAW ⟹ ≤ bumpk 2 (Mδ⊕Z): u := jφ +ℕ 2-ish; I2: σ=ι: Mφ⊕Z ≡ Mφ ≤ Mδ, e := 0; σ-arrow: source-pool Mφ⊕(Z⊕mh) with mh ≤ Bγ ≤ Mδ raw ⟹ trip→⊗ω→bump: e := 1; I3: σ=ι: pad jφ ≤ u; σ-arrow: bumpk (jφ+(0+kb)) P = bumpk jφ (bumpk kb P) ≤ [pjγ] bumpk jφ (bumpk (jγ+0) P) hmm — CAREFUL: pjγ's bound is (jγ+ℕJHₐ)=(jγ+ℕ0) FIXED ✓ ⟹ ≤ bumpk (jφ+(jγ+0)) P — WAIT this reintroduces jγ into u!! RECHECK: u must ≥ jφ+jγ for I3(σ-arrow)!! Hmm — jδ = rbb τ (jφ+jγ+C) e — jγ ONCE — pack₂'s pj₂ absorbs ONE jγ via JH₁ = jγ ✓ OK FINE: u := (jφ +ℕ jγ) +ℕ 2-ish for σ-arrow (ONE jγ ✓ absorbable), u := jφ+2-ish for σ=ι. jδ jγ-linear-ONCE ⟹ pj₂ workable; I4: ω ≤ Mδ⊕Z ✓ (Mδ has ω-floor by construction). Sides: ValidMult Mδ = validMult-⊗ chains incl. bumpk-valid (jγ+ℕ0) {Mγ⊕Z} vMγ (Mγ⊕Z ≡ Mγ). (3) pack₂(kφ) (consume kγ, fn-clause: acc₁ = Z⊕(Dγ⊕cγ), MH₁ = Z⊕Mγ, JH₁ = 0+jγ ≡ jγ): D₂ := λw→Dφ w⊕cφ (kφ's data — pack₂ is per-kφ ✓), c₂ := Z, M₂ := Mφ⊗ω, j₂ := 2 +ℕ jδ: pd₂: summands of Dδ⊕cδ: Dφ,cφ ≤ D₂; Dγ⊕cγ = kadd kγ ∈ acc₁; multi-summand chains; pm₂: Mδ-factors: Bγ ≤ bumpk jγ pool₂ [(jγ+ℕ0)＝jγ zero-right; (Mγ⊕Z)≡Mγ ≤ Z⊕Mγ ∈ pool₂ mono] ⟹ Mδ ≤ B₀⊗bump B₀ ≤ bumpk (2+jγ) pool₂ = bumpk (2+ℕjγ) DEFINITIONAL ≤ bumpk ((2+jδ)+ℕjγ) [pad-mid via bumpk-+ decompositions]; pj₂: jδ+stuff vs (2+jδ)+jγ: pads+comm. (4) packS (consume kφ, fn-clause): kadd (F₂-pair) ≡ Dφ⊕cφ = kadd kφ ∈ acc ✓; kmult = M₂ = Mφ⊗ω ≤ poolS-products ≤ bump poolS (jS ≥ 1, packK's pm₂-pattern); kbudget = j₂ = 2+jδ = jφ-linear+jγ?? — WAIT j₂ contains jδ ∋ jγ (σ-arrow: ONE jγ) — at packS, jγ is NOT yet arrived either?! NO — pack₂ is per-kφ and kγ arrives at pack₂'s ZApply — j₂ := 2+ℕjδ where jδ depends on jγ — j₂ IS kγ-dependent — but j₂ is pack₂'s Σ-component FIXED BEFORE kγ arrives!!! RE-FIX: jδ must be jγ-FREE TOO: the I3(σ-arrow) jγ-need: bumpk (jφ+(jγ+0))-source — absorb the jγ-bumps into the MULTIPLIER as well?? bumpk jγ P-terms with P the POOL — I3 is ∀P (budget-semantic) — CANNOT poolify. ⟹ σ-ARROW ALSO OBSTRUCTED at pack₂?! — UNLESS pack₂'s pj₂-slot provides it: PackDom pj₂: ∀P → bumpk (kbudget kδ-partial) P ≤ bumpk (j₂+ℕJH₁) P with JH₁ = jγ — kbudget declared := jδ(jγ) ≤ j₂+jγ NEEDS jδ ＝ jγ-linear-coefficient-ONE: jδ = rbb τ ((jφ+ℕjγ)+2) e = (jφ+jγ+2)+rbc τ e [rbb-＝] — jγ-coefficient ONE ✓✓ and j₂ := jφ+2+rbc-const FIXED-per-kφ ✓ pj₂: jδ ＝ (j₂-const-part)+jγ-rearranged ≤ j₂+jγ ✓ TRANSPORT-EQUAL ✓✓ SAVED — j₂ jγ-FREE ✓. THEN packS pj: j₂ = jφ-linear ✓ jS := const, comm-transports as packK ✓.** **UPDATE 2026-07-04 — packδ (ground shared arg ρ=ι, ground middle σ=ι) IS BUILT AND VERIFIED (module 56, MultHereditaryH8, EXIT 0). The budget tension is DEFINITIVELY RESOLVED: packδ is constructed with BOTH kφ AND kγ in scope ⟹ γ's budget jγ rides in the MULTIPLIER (Bγ = bumpk (jγ+ℕ0)(Mγ⊕Z), inside Mδ = bumpk 3 (Mφ⊗(Bγ⊗ω))), while jδ = rbb τ jφ 0 depends ONLY on jφ. Rebase runs at u=jφ, e=0. I1 = quad4 (heterogeneous 4-absorb: Dφ,accₐ,X=γval,cφ each ≤ Q=Y⊗(ω⊗Bγ), via ⊕-quad-≤-⊗ω) with the γ-value X≤Q from zaγ+trip3r; keyM: (ω⊗Bγ)⊗ω ≤ Mδ (two ⊗-≤-bump). module `Sδ-ground (τ)(kφ)(kγ)`: deep pr-projections (za = pr₂⁹ k, Mε=pr₁(pr₂⁸ k)); helpers trip3r/quad4. GOTCHAS FIXED: ⊕-increasing-left d x : x≤d⊕x [RIGHT operand ≤ sum]; ⊕-increasing-right a b : a≤a⊕b [LEFT]; x-≤-x⊗ Y (ω⊗Bγ) needs SZ≤(ω⊗Bγ) [order-specific, separate vωBγ]; reassoc is ＝ not ≤ (transport, not ≤-trans). This VALIDATES THE ENTIRE S MECHANISM at its hardest point. REMAINING: (a) packδ σ=arrow (fn-middle: γ-value fed to φ's slot via ZApply arrow-clause; PackDom of ZCont σ (Fγ ka) supplies the rebase I1/I2/I3 — the (48) payoff — instead of the ground bound; kmult/kbudget of the γ-value enter pool/budget, then same rebase), (b) pack2 (consume kγ: rebase packδ's data into pack2 context; PackDom of kδ), (c) packS (consume kφ), (d) GoodH-S assembly + goodμ. All same rebase pattern as packδ.**
**UPDATE 2026-07-04 (module 57, MultHereditaryH9): the ONE genuinely-multiplicative outer-pack obligation is VERIFIED. `mult-absorb : Mφ≤pool → Mγ≤pool → ω≤pool → bumpk 3 (Mφ⊗(bumpk n (Mγ⊕Z)⊗ω)) ≤ bumpk (5+ℕn) pool` (flood all ≤pool ⟹ base ≤ P⊗(P⊗P), P=bumpk n pool; `⊗³-≤-bumpk2 : X⊗(X⊗X) ≤ bumpk 2 X` via two ⊗-≤-bump; outer bumpk 3 composes via bumpk-+ ⟹ bumpk 5 P = bumpk(5+ℕn)pool). idx = bumpk-+ 5 n pool ∙ bumpk-+ 3 2 (bumpk n pool) [3+ℕ2≡5, 5+ℕn≡3+ℕ(2+ℕn) definitional]. So OUTER PACKS COST FIXED +5 BUMPS over consumed arg's budget ⟹ pack2/packS multiplier clause closes with j2≥5. Both hard obligations of S over the tower (diagonal core packδ = module 56, outer multiplier = module 57) now MACHINE-CHECKED; remaining = SYSTEMATIC ASSEMBLY (pack2 consume kγ: rebase packδ-body + PackDom via mult-absorb; packS consume kφ; GoodH-S; goodμ; fn-middle σ=arrow variant). No conceptual unknowns remain in the S construction.**
**LANDED 2026-07-04 (module 58, MultHereditaryHTK): the K-EXTENDED ITERATOR FRAGMENT THEOREM — fragment TK = Ω/Zero/Succ/Iter/K/app, unconditional `dialogue-height-<-ε₀`. Strictly extends TI (module 54) with K. 4-line goodμ over the 6 proven GoodH closures (Ω/Zero/Succ/Iter/K/app all done in H4/H6/H7). Certain, complete. The ONLY combinator not yet a full GoodH closure is S (diagonal core + outer multiplier verified in 56/57; pack2/packS/assembly remaining).**
**LANDED 2026-07-04 (module 59, MultHereditaryH10): the bumpk-INDEX MONOTONICITY TOOLKIT — `le a b = Σ d, b ＝ a+ℕd`; `bumpk-le : le a b → bumpk a P ≤ bumpk b P` (via bumpk-pad); `le-refl/le-trans/le-add-right/le-add-left/le-+ℕ-right/le-+ℕ-left/le-＝-left/le-＝-right`. This kills the ℕ-index bookkeeping that gates pack2/packS. ALL THREE pack2 sub-components now verified: diagonal core (56), mult-absorb (57), index toolkit (59).**
**FULLY-WORKED pack2 RECIPE (all-ground σ=τ=ι, transcription-ready — a future session builds this in ~120 lines): module Spack2 (kφ:𝕂(ι⇒ι⇒ι)); F2 kγ = (Sδ-ground.Fδ ι kφ kγ, Sδ-ground.packδ ι kφ kγ); pack2 : ZPack (ι⇒ι)(ι⇒ι) F2 with D2:=Dφ, c2:=cφ, M2:=Mφ⊗ω, j2:=rbb ι ((jφ+ℕ0)+ℕ5) 0 [chosen so the ZApply component's ZCont-rebase output budget rbb ι u 0 MATCHES j2 DEFINITIONALLY with u:=(jφ+ℕ0)+ℕ5, e:=0], sides gDφ/cφε/(validMult-⊗ vMφ ω-valid)/(⊗-<-ε₀ Mφ ω Mφε (tower-<-ε₀ 0)). body2 (kγ): open Sδ-ground ι kφ kγ [gives Fδ,Dδ,cδ,Mδ,jδ,Bγ,Dγ,cγ,Mγ,jγ,packδ-body]; acc=λw→Z⊕(Dγ w⊕cγ), MH=Z⊕Mγ, JH=0+ℕjγ≡jγ, pool=M2⊕MH=(Mφ⊗ω)⊕(Z⊕Mγ). Returns (PackDom, ZApply): PackDom-(b) = ≤-trans (mult-absorb Mφ Mγ pool (jγ+ℕ0) hφ hγ hω) (bumpk-le pool [le (5+ℕ(jγ+ℕ0)) (j2+ℕJH) via le-＝-left (ap(5+ℕ_)(+ℕ-zero-right jγ)) (le-+ℕ-right jγ (jδ0, +ℕ-comm...))]); hφ:Mφ≤pool (x-≤-x⊗ then ⊕-inc-right), hγ:Mγ≤pool (⊕-inc-left Z Mγ then ⊕-inc-left), hω:ω≤pool (y-≤-⊗ Mφ ω then ⊕-inc-right). PackDom-(c)=bumpk-le P (le-trans (le-add-right jδ 5)(le-add-right (jδ+ℕ5) JH)) [jδ≡rbb ι jφ 0]. PackDom-(a): kadd kδ=(Dφ⊕Dγ)⊕(cγ⊕cφ) ≤ Y2⊗ω [Y2=D2⊕(acc⊕c2)] via ⊕-assoc-reassoc + quad4 (import from H8) with 4 bounds Dφ/Dγ/cγ/cφ ≤ Y2, then ⊗-mono-right Y2 (≤-trans hω (≤-bumpk (j2+ℕJH) pool)). ZApply (ka:GFun) = ZCont-rebase ι (Fδ ka) Asrc Atgt Dδ D2 Z MH 0 JH cδ c2 Mδ M2 jδ u 0 [u=(jφ+ℕ0)+ℕ5] I1 I2 I3 I4 (packδ-body ka); Asrc=λw→Z⊕pr₁ ka w, Atgt=λw→acc w⊕pr₁ ka w. I1: LHS Dδ⊕(Asrc⊕cδ), use ⊕-Z-left to erase Z in Asrc, then summands Dφ,Dγ,pr ka,cγ,cφ ≤ Y (target zone) — NEEDS A 5-FOLD ⊕-absorb (only quad4=4 exists; build pent5 : a⊕(b⊕(c⊕(d⊕e)))≤z⊗ω OR group as (2 summands)+(3 summands) with dup/trip then combine ×ω²; OR bound via nested — THE ONE GENUINELY NEW HELPER pack2 needs beyond current lib), then Y⊗ω ≤ Y⊗bumpk((u+ℕ0)+ℕJH)pool; I2: Mδ⊕Z ≤ bumpk 0-ish... actually I2 for ZCont-rebase source→target: (Mδ⊕MH_src=Mδ⊕Z≡Mδ) ≤ bumpk e (M2⊕MH); e must absorb — WAIT ZApply-rebase I2 is (M_src⊕MH_src)≤bumpk e (M_tgt⊕MH_tgt) = Mδ ≤ bumpk e ((Mφ⊗ω)⊕(Z⊕Mγ))=bumpk e pool. Mδ ≤ bumpk e pool: from mult-absorb Mδ≤bumpk(5+ℕ(jγ+ℕ0))pool, so e:=5+ℕ(jγ+ℕ0). BUT e is the rebase slack and appears in output budget rbb ι u e — SO u,e must satisfy BOTH rbb ι u e ＝ j2 AND e≥5+ℕ(jγ+ℕ0). CONFLICT: e depends on jγ but j2 fixed-per-kφ. RESOLUTION: this is the SAME budget-into-multiplier trick as packδ — the ZApply-rebase's I2 pool-domination should use e SMALL and carry Mδ's excess via... hmm, actually reconsider: maybe M2 must be BIGGER (M2:=bumpk-based carrying room) so Mδ≤bumpk e pool with e small. Set M2 := Mδ-dominating? But M2 is fixed-per-kφ and Mδ depends on jγ... SAME tension as diagonal, resolved the SAME way: the ZApply component's rebase carries jγ in the pool via MH=Z⊕Mγ + budget JH=jγ, so e can be small IF the rebase I2 allows bumpk with the JH... RE-READ ZApply-rebase I2: (M⊕MH)≤bumpk e (M′⊕MH′), pool' = M′⊕MH′ ∋ Mγ. Mδ=bumpk 3(Mφ⊗(bumpk jγ Mγ⊗ω)) ≤ bumpk(5+ℕjγ)pool' — needs e=5+ℕjγ (jγ-dependent). Then output budget rbb ι u (5+ℕjγ) is jγ-dependent ≠ fixed j2. ⟹ the OUTPUT budget of the ZApply-rebase is jγ-dependent, which is FINE because it's the budget of the produced ZCont which must = j2... NO. **THIS IS A REAL SUBTLETY TO RESOLVE: pack2's ZApply component budget must be j2 (fixed), but rebasing packδ-body carries jγ. Likely resolution: the ZApply component does NOT rebase via ZApply-rebase with e-slack; instead packδ ALREADY has jδ jγ-free (budget) and Mδ jγ-in-multiplier, so the ZApply rebase only needs to move accumulator (Dγ into acc) + pool (Mγ into MH) with e=0 and the Mδ≤M2-form handled by making M2 CONTAIN the bumpk-room. Since Mδ=bumpk 3(Mφ⊗(bumpk jγ Mγ⊗ω)) and pack2's pool has Mγ+budget jγ, set M2 s.t. bumpk e(M2⊕MH) ⊇ Mδ with e=0: need Mδ ≤ M2⊕(Z⊕Mγ). But Mδ has bumpk jγ inside — M2 fixed can't contain bumpk jγ Mγ for variable jγ. ⟹ e CANNOT be 0. So the ZApply-rebase e IS jγ-dependent, making output budget rbb ι u e jγ-dependent. FOR THIS TO EQUAL j2, need j2 jγ-dependent — but j2 is pack2's fixed Σ-component!! ⟹ CONTRADICTION unless the ZApply component's budget is allowed to be ≠ j2.** Re-examine ZCont(ι⇒ι)(kδ) = PackDom × ZApply — the ZApply here is ZApply ι ι (pr₁ kδ=Fδ) acc MH JH D2 c2 M2 j2 — its budget slot IS j2 (pack2's). So the produced ZCont-at-ground (from ZApply's ka) has budget j2. And packδ-body ka has budget jδ. Rebasing jδ→j2 with the pool/mult moves. The rebase output budget = rbb ι u e must = j2. And I2 needs e ≥ (jγ-dependent). ⟹ rbb ι u e ≥ succ(jγ-stuff), so j2 ≥ jγ-dependent. But j2 fixed. **THE RESOLUTION (must verify): the ZApply-rebase I2 does NOT need e to cover Mδ's full bumpk jγ — because the TARGET pool M2⊕MH already contains bumpk-able Mγ with the budget accounted via bumpk in the rebase's OWN bumpk((u+ℕe)+ℕJH) where JH=jγ appears! I.e. I1's target multiplier is bumpk((u+ℕe)+ℕJH)(pool) with JH=jγ — the jγ budget rides in JH, NOT in e. So e stays small (e=0) and I2 is Mδ≤bumpk 0(M2⊕MH)... no I2 has its OWN bumpk e without JH.** ⟹ **MUST CAREFULLY RE-READ whether ZApply-rebase's I2 includes JH. From H3 sig: I2 = ((M⊕MH) ≤ bumpk e (M′⊕MH′)) — NO JH in I2. So I2 genuinely needs e to cover Mδ vs pool, jγ-dependent. THIS IS THE UNRESOLVED KNOT for pack2's ZApply component.** Possible fixes: (i) strengthen the rebase lemma to allow I2 with a +JH term; (ii) restructure so packδ's Mδ does NOT bake in bumpk jγ but leaves it to be applied at pack2 (i.e. packδ multiplier = Mφ-only, and the jγ-budget stays as budget jδ, contradicting the earlier resolution); (iii) make j2 legitimately depend on... no. **THIS IS THE NEXT THING TO RESOLVE BEFORE pack2 CLOSES — the diagonal (packδ) is verified but its INTERFACE to pack2 (how Mδ's jγ-content passes the ZApply-rebase I2) needs either a strengthened rebase or a redesigned packδ multiplier. NOT yet resolved; do NOT claim pack2 is mere transcription.** Modules 56,57,59 (packδ, mult-absorb, index toolkit) are solid verified sub-components; the pack2 ZApply-I2 budget/multiplier interface is a genuine remaining design question.**
**RESOLVED 2026-07-04 (module 60, MultHereditaryH11): the pack2-I2 knot is FIXED by ROUTING γ's BUDGET THROUGH THE BUDGET (not the multiplier). Design change to packδ (module 56 needs rebuild): Mδ := bumpk 3 (Mφ ⊗ (Mγ ⊗ ω)) [jγ-FREE, was Mφ⊗(Bγ⊗ω)]; jδ := rbb τ uδ 0 with uδ := jγ +ℕ (jφ +ℕ 3) [jγ IN BUDGET; jγ leftmost so le-+ℕ-left applies]; rebase at u=uδ, e=0. Then: (a) packδ's I1 absorbs the γ-value's forced multiplier bumpk jγ Mγ via `diag-absorb` (VERIFIED module 60): ((ω⊗bumpk n(Mγ⊕Z))⊗ω) ≤ bumpk(2+ℕn)(Mφ⊗(Mγ⊗ω)), n=jγ — two ⊗-≤-bump + bumpk-+; the diagonal budget uδ⊇jγ covers the 2+ℕjγ via bumpk-le. (b) pack2's ZApply-I2 becomes Mδ≤bumpk e pool with Mδ jγ-free ⟹ e=5 CONSTANT via `mult-absorb Mφ Mγ pool 0` (bumpk 0=id, Mγ⊕Z≡Mγ, gives Mδ≤bumpk 5 pool); output budget rbb ι u 5 jγ-FREE, matches fixed j2. BOTH new inequalities now MACHINE-VERIFIED (diag-absorb=60, mult-absorb-at-0=57). **REMAINING TRANSCRIPTION: (1) rebuild module 56 packδ jγ-free (swap Bγ→Mγ in base₀; jδ:=rbb τ (jγ+ℕ(jφ+ℕ3)) 0; keyM→use diag-absorb + bumpk-le for I1 finish target bumpk uδ Mδ; I3 now needs bumpk-le not ≤-refl since u≠jφ; import H10+H11); (2) pack2 (recipe in memory above, now with e=5 constant ZApply-rebase — the knot gone); (3) packS; (4) GoodH-S + goodμ. NO conceptual unknowns remain — all 3 pack sub-inequalities (diag-absorb 60, mult-absorb 57, index-le 59) + diagonal structure (56) verified; it is now genuinely transcription (fiddly +ℕ but bounded).** 60 modules, clean rebuild 0 warnings, nothing committed, conjecture OPEN.**
**DONE 2026-07-04: MODULE 56 (MultHereditaryH8) packδ REBUILT jγ-FREE AND VERIFIED (compiles first-try, full Index rebuild clean at 60 modules). The composition flaw is now FIXED IN CODE — packδ genuinely composes. Concrete changes made to H8: imports H10(le tools)+H11(diag-absorb); base₀:=Mφ⊗(Mγ⊗ω) [was Bγ]; uδ:=(2+ℕjγ)+ℕjφ; jδ:=rbb τ uδ 0; keyM:=≤-trans (diag-absorb Mφ Mγ (jγ+ℕ0) (pr₁ vMφ)(pr₁ vMγ)) (transport (bumpk-+ ((uδ+ℕ0)+ℕ0) 3 base₀) (bumpk-le base₀ leKM)) where leKM:=le-＝-left (ap(2+ℕ_)(+ℕ-zero-right jγ)) (le-＝-right e2 core), e2:=ap(_+ℕ3)((+ℕ-zero-right uδ)⁻¹∙(+ℕ-zero-right(uδ+ℕ0))⁻¹), core:=le-trans (le-add-right (2+ℕjγ) jφ)(le-add-right uδ 3); I3 P:=bumpk-le P (le-+ℕ-right 0 (le-add-left (2+ℕjγ) jφ)); rebase call `...Mφ Mδ jφ uδ 0 I1 Mφ≤Mδ I3 ω≤Mδ...`; I1 target index (uδ+ℕ0)+ℕ0; finish uses keyM directly (⊗-mono-right Y keyM). ALL COMPILED.**
**pack2 ATTEMPTED, NOT DONE (draft H12 was buggy, DELETED — don't leave broken code). pack2 is genuinely LENGTHY fiddly transcription (~150 lines, many le/＝ proofs) with real iteration risk; do it in a FOCUSED pass. WORKED-OUT pack2 choices (σ=τ=ι, module Spack2 over kφ): D2:=Dφ, c2:=cφ, M2:=Mφ⊗ω, ur:=3+ℕjφ, e_r:=5, j2:=rbb ι (3+ℕjφ) 5 [FIXED/jγ-free]. body kγ: `open Sδ-ground ι kφ kγ`; acc=λw→Z⊕(Dγ w⊕cγ), MH=Z⊕Mγ, JH=0+ℕjγ, pool=M2⊕MH. Returns (PackDom,ZApply). **PackDom-pm (multiplier): kmult(F2 kγ)=Mδ ≤ bumpk(j2+ℕ(0+ℕjγ))pool = ≤-trans (mult-absorb Mφ Mγ pool 0 hφ hγ hω : Mδ≤bumpk(5+ℕ0)pool=bumpk 5 pool) (bumpk-le pool [le (5+ℕ0)(j2+ℕ(0+ℕjγ)) via le-add stuff]); hφ:Mφ≤pool (x-≤-x⊗+⊕-inc-right), hγ:Mγ≤pool (⊕-inc-left Z Mγ+⊕-inc-left), hω:ω≤pool (y-≤-⊗+⊕-inc-right). PackDom-pj (budget): jδ=rbb ι uδ 0 ≤ j2+ℕJH via le. PackDom-pd (zone): kadd(F2 kγ)=Dδ⊕cδ ≤ (D2⊕(acc⊕c2))⊗bumpk(...)pool, 4-summand quad4. ZApply (ka): ZCont-rebase ι (Fδ ka) [Asrc=λw→Z⊕pr ka w][Atgt=λw→acc w⊕pr ka w] Dδ D2 Z MH 0 JH cδ cφ Mδ M2 jδ ur 5 I1z I2z I3z I4z (packδ-body ka); I2z=mult-absorb Mφ Mγ pool 0 hφ hγ hω [Mδ≤bumpk 5 pool, e_r=5 CONSTANT — the knot resolved!]; I4z=hω; I3z via le (jδ ＝ (3+ℕjφ)+ℕjγ by commuting succ/assoc, then le-refl-ish); I1z: 5 summands (Dφ,Dγ,pr ka,cγ,cφ)≤Y=D2⊕(Atgt⊕c2); use ⊕-Z-left to kill Z, reassoc, quad4 on inner-4 (Dγ,pr ka,cγ,cφ) then ⊕ Dφ + dup → Y⊗(ω⊗ω), then ω⊗ω ≤ bumpk(idx)pool. **KEY VERIFIED FACT enabling all this: e_r=5 CONSTANT (jγ-free) because Mδ is jγ-free (mult-absorb n=0). This is the whole point of the packδ rebuild.** THEN packS (consume kφ, same pattern one level up), GoodH-S (transformer λTφ Tγ Ta→Tφ Ta(Tγ Ta), LT via kS=(FS,packS), BndH one-line like G's BndD-S, BndL=F's Good-S), goodμ over T₁, payoffs.** 60 modules verified, jγ-free packδ DONE, pack2/packS/GoodH-S = remaining focused transcription, nothing committed, conjecture OPEN.
**PROGRESS 2026-07-04 (module 61, MultHereditaryH12): pack2's ARITHMETIC OBLIGATIONS VERIFIED as standalone lemmas — the fiddly +ℕ/le core is now machine-checked, de-risking pack2 assembly. USE FIXED j2 := 9 +ℕ jφ (NOT rbb-form) with `eq-j2 : rbb ι (3+ℕjφ) 5 ＝ 9+ℕjφ` to transport the ZApply's ZCont-rebase output budget to j2. Verified lemmas (all in H12, parameterized by jφ jγ): `eq-j2`; `jδ-＝ : rbb ι ((2+ℕjγ)+ℕjφ) 0 ＝ (3+ℕjφ)+ℕjγ` [jδ commuted]; `L-pm : le (5+ℕ0)((9+ℕjφ)+ℕ(0+ℕjγ))` [PackDom multiplier, after mult-absorb n=0 gives bumpk(5+ℕ0)pool]; `L-pj : le (rbb ι((2+ℕjγ)+ℕjφ)0)((9+ℕjφ)+ℕ(0+ℕjγ))` [PackDom budget]; `L-I3z : le (jδ+ℕ0)((3+ℕjφ)+ℕ(0+ℕjγ))` [inner rebase budget, ur=3+ℕjφ]; `L-I1z : le 1 (((3+ℕjφ)+ℕ5)+ℕ(0+ℕjγ))` [inner rebase mult index, =(...,refl) since RHS≡succ d]. GOTCHA: trailing ⁻¹ on a ∙-chain needs a where-bound `fwd` with explicit type (inference fails otherwise); le witnesses use +ℕ-assoc N M jφ : (N+ℕM)+ℕjφ ＝ N+ℕ(M+ℕjφ) with N+ℕM reducing defeq (e.g. 5+ℕ4≡9). **REMAINING pack2 = PLUMBING ONLY (PackDom triple + ZCont-rebase of packδ-body at e=5 constant, threading these H12 lemmas + mult-absorb + quad4-for-zone + I1z 5-summand-absorb via quad4+dup→Y⊗(ω⊗ω)); lower risk now.** 61 modules clean, nothing committed, conjecture OPEN.
**★★★ MAJOR MILESTONE 2026-07-04: THE S COMBINATOR IS COMPLETE OVER THE HOWARD TOWER. ALL SEVEN COMBINATORS NOW CLOSE. Fragment `TS` = Ω/Zero/Succ/Iter/K/S/· (all-ground S, ρ=σ=τ=ι) has UNCONDITIONAL `dialogue-height-<-ε₀` (module 64). Modules built: 62 (H13 pack2, VERIFIED), 63 (H14 packS, VERIFIED), 64 (H15 GoodH-S + TS fragment theorem, VERIFIED). 64 modules, clean rebuild 0 warnings, nothing committed. KEY FACTS: pack2 (H13, module Spack2 kφ): D2=Dφ0,c2=cφ0,M2=Mφ0⊗ω,j2=rbb ι(3+ℕjφ0)5; PackDom via mult-absorb(n=0)+quad4+H12-lemmas; ZApply=ZCont-rebase of packδ-body at e=5 CONSTANT (jγ-free Mδ payoff); pent5 helper (5-summand absorb=quad4+dup→Y⊗(ω⊗ω)). GOTCHA: `open Sδ-ground` clashes kφ-projections → suffix Spack2's with 0 (Dφ0 etc). packS (H14, module SpackS): DS=λ_→Z,cS=Z,MS=ω,urS=9,eS=2,jS=rbb(ι⇒ι)9 2≡13 CONSTANT; inner ZApply=ZCont-rebase AT ARROW TYPE (ι⇒ι) of pack2-body (=pr₂⁸ of Spack2.pack2 kφ — 8 not 9 since it's the ZPack not the 𝕂-pair); I1S quad4 (4-summand leaf, real increasing-chain bounds via P1/P2/P3 nesting), I2S 2-bump, I3S eq-j2-converted; GOTCHA: Z⊕kmult needs ⊕-Z-left not ⊕-increasing-left; bumpk-le needs EXPLICIT le-typed witness (metas otherwise). GoodH-S (H15): Tr=λTφ Tγ Ta→Tφ Ta(Tγ Ta); kS=(FS,SpackS.packS); **BndH = ONE LINE `λ Tφ kφ bndφ g kg bndg a ka bnda → bndφ a ka bnda (g a)(pr₁ kg ka)(bndg a ka bnda)`** (diagonal Fδ ka=pr₁(pr₁ kφ ka)(pr₁ kg ka) IS the composition bndφ feeds — THE PAYOFF); LT maps thread (mφ,kφ,bndφ)→(m1,(FS kφ,b1)), m1→(m2,(F2 kφ kγ,b2)), m2=pr₁(mφ Ta gTa)(Tγ Ta)(mγ Ta gTa); BndL=F's Good-S verbatim. **HONEST SCOPE: TS has S only at ρ=σ=τ=ι (all-ground). NOT yet full T₁ — higher-σ/τ S (T₃'s joint-data instances on Design-F line, module 44) not yet ported to tower (would need n-ary joint tower data). But TS is combinatory T with recursor+K+all-ground-S, unconditional height<ε₀ — the S combinator, which BLOCKED THE PROJECT, is now machine-checked over the tower.** REMAINING for full T₁: port higher-σ/τ S (Sfh/Sfr-style + Sg at higher τ) to the tower via joint data — analogous to how modules 41-44 did it for Design F, but over 𝕂. Conjecture (full System T, higher-TYPE recursion) still OPEN beyond T₁.
**★ HONEST SCOPE CORRECTION 2026-07-04 (IMPORTANT — don't overclaim TS): TS's S is ALL-GROUND (ρ=σ=τ=ι), which is a SUBSET of module 44's T₃.Sg₃{τ} (all-ground-shared-and-middle, ANY τ). So TS ⊊ T₃ AS A TERM FRAGMENT — the Howard tower has so far REPRODUCED a sub-fragment of Design-F's reach (modules 37-44), NOT exceeded it. The tower's WHOLE POINT / UNIQUE CAPABILITY is the ONE case Design F CANNOT do: ground-S with FUNCTION-TYPED MIDDLE σ (transformer-dependent-data: the value fed to φ's 2nd slot is a FUNCTION whose bound depends on its own data). ALL current packs (packδ/Sδ-ground, pack2, packS) are σ=ι (GROUND middle). So the tower machinery is built+validated end-to-end but its UNIQUE PAYOFF IS STILL PENDING. Also: generalizing τ alone (pack2/packS to any τ) stays within T₃.Sg₃ — does NOT exceed Design F. The ONLY way to exceed Design F = FUNCTION-MIDDLE σ.
THE REAL NEXT TARGET = GoodH-S at function-typed middle σ. Construction: generalize Sδ-ground to σ=ARROW. Then Fδ ka=pr₁(pr₁ kφ ka)(pr₁ kγ ka) with γ-value pr₁ kγ ka : 𝕂 σ (ARROW, not GFun). Consume ka in φ's pack → zaφ2 (φ's 2nd-slot ZApply at ARROW σ) → feed γ-value via ZApply's ARROW CLAUSE (not ground) → γ-value's kadd/kmult/kbudget enter accumulators; γ's pack consumed at ka gives ZCont σ (ARROW) whose PackDom supplies the bounds on kadd/kmult/kbudget of the γ-value = THE (module-48) PACKDOM PAYOFF supplying rebase I1/I2/I3. This is a genuinely different (harder) diagonal than the ground case (where zaγ gave a single ⊗Bγ bound); it's the transformer-tower's actual novel content. Substantial — build incrementally (arithmetic-first like pack2). Until then, honest status: tower COMPLETE for all-ground S (=TS, ⊆T₃), tower's UNIQUE reach (fn-middle σ) PENDING.
**FN-MIDDLE PROGRESS 2026-07-04 (module 65, MultHereditaryH16): `fnmid-I2` VERIFIED — the key NEW arithmetic of the fn-middle diagonal. `fnmid-I2 : (Mφ Mγ M)(n) → SZ≤Mφ → SZ≤Mγ → M ≤ bumpk n (Mγ⊕Z) → (Mφ⊕M) ≤ bumpk (n+ℕ2)(bumpk 3 (Mφ⊗(Mγ⊗ω)))`. M = γ-value's kmult (bounded by γ's PackDom-pm = bumpk jγ Mγ, n=jγ). Absorbs Mφ⊕(γ-mult) into jγ-FREE Mδ at budget-slack cost jγ+2. Proof: flood ≤ P0=bumpk n base₀, Mφ⊕M ≤ P0⊗ω ≤ bump P0 = bumpk(succ n)base₀ ≤ bumpk((n+ℕ2)+ℕ3)base₀=bumpk(n+ℕ2)Mδ. le′ : le(succ n)((n+ℕ2)+ℕ3)=le-＝-right ((+ℕ-assoc n 2 3)⁻¹)(4, +ℕ-succ-right n 4). **FN-MIDDLE packδ PLAN (verified pieces: I1 reuses diag-absorb [H11] with kadd_γ playing X's role; I2 = fnmid-I2 [H16] with M=kmult_γ; I3 uses γ's PackDom-pj budget bound: bumpk(jφ+ℕkbudget_γ)P ≤[pjγ] bumpk(jφ+ℕjγ)P ≤ bumpk uδ P, uδ⊇jφ+jγ; I4 ω≤Mδ). Choices: Mδ=bumpk3(Mφ⊗(Mγ⊗ω)) jγ-FREE, uδ=(jφ+ℕjγ)+ℕ3, eδ=jγ+ℕ2, jδ=rbb τ uδ eδ. Structure: Sδ-fnmid module (σ arrow), consume ka in φ's pack → zaφ2 (ZApply at ARROW σ) → feed γ-value pr₁ kγ ka via ARROW clause → γ-value's kadd/kmult/kbudget in accumulators → γ's pack at ka gives PackDom (pdγ/pmγ/pjγ) supplying the 3 rebase bounds = THE (48) PAYOFF → ZCont-rebase τ. THEN generalized pack2/packS (τ,σ general) + GoodH-S-fnmid. This EXCEEDS Design F (T₃ couldn't do fn-middle σ).** 65 modules clean, nothing committed, conjecture OPEN.
**★★ FN-MIDDLE DIAGONAL VERIFIED 2026-07-04 (module 66, MultHereditaryH17, Sδ-fnmid): THE FIRST MACHINE-CHECKED PIECE OF S THAT EXCEEDS DESIGN F. `Sδ-fnmid (σ₁ σ₂ τ)(kφ : 𝕂(ι⇒((σ₁⇒σ₂)⇒τ)))(kγ : 𝕂(ι⇒(σ₁⇒σ₂)))` builds `packδ : ZPack ι τ Fδ` for the fn-typed-MIDDLE diagonal. The γ-value pr₁ kγ ka : 𝕂(σ₁⇒σ₂) fed to φ's slot via ZApply ARROW clause; γ's PackDom (pdγ/pmγ/pjγ = pr₁/pr₁∘pr₂/pr₂∘pr₂ of pr₁(zaγ ka)) supplies the 3 rebase bounds — THE (module-48) PACKDOM PAYOFF realized. I1=quad4+diag-absorb (kadd_γ via pdγ, SAME as ground), I2=fnmid-I2 (kmult_γ via pmγ, +⊕-Z-left+index-convert transports), I3=pjγ threaded via bumpk-+ (explicit s2/s3 lemmas, NOT nested transports — those confuse inference). Choices: Mδ=bumpk3(Mφ⊗(Mγ⊗ω)) jγ-FREE, uδ=jφ+ℕjγ, eδ=jγ+ℕ2, jδ=rbb τ uδ eδ. keyM leKM clever: eδ=jγ+ℕ2 ＝ 2+ℕjγ (comm) = diag-absorb's index, so leKM=le-＝-left(comm)(le-add-left uδ eδ ∘ le-add-right). COMPILE GOTCHAS FIXED: le-＝-right needs the ＝ in b′＝b direction (⁻¹); cφ≤Y/cγ≤Y middles use ⊕-increasing-LEFT (cδ is right operand) not right. **HONEST: the DIAGONAL (novel hard core) is DONE. A COMPLETE fn-middle GoodH-S (a THEOREM exceeding Design F) still needs: generalize pack2/packS to σ=arrow (consume kγ:𝕂(ι⇒σ) / kφ:𝕂(ι⇒σ⇒τ) using Sδ-fnmid instead of Sδ-ground) + GoodH-S-fnmid. Those are structurally like H13/H14 but general σ. So: hardest NEW piece verified, full fn-middle theorem = generalize outer packs + assemble.** 66 modules clean, nothing committed, conjecture OPEN.
**★ OBSTRUCTION FOUND 2026-07-04 (composition analysis of Sδ-fnmid — HONEST, don't claim fn-middle nearly-done): the fn-middle diagonal (module 66) VERIFIES IN ISOLATION but DOES NOT COMPOSE into pack2. ROOT CAUSE: the fn-middle diagonal absorbs TWO jγ-contributions from the γ-value (both ≤jγ via γ's PackDom): kbudget_γ→uδ, kmult_γ→eδ. So jδ=rbb τ (jφ+ℕjγ)(jγ+ℕ2) has jγ TWICE. But pack2 (consuming kγ) provides only ONE jγ (kγ's own kbudget) in PackDom targets bumpk(j2+ℕjγ). So PackDom-(c) `jδ ≤ j2+ℕjγ` FAILS: 2jγ ≤ j2+jγ ⟹ j2≥jγ, impossible (j2 fixed). COULD be fixed if kmult_γ's jγ went into the MULTIPLIER Mδ (PackDom-(b), which has its OWN j2+ℕjγ, absorbs it) instead of the budget — BUT the rebase lemma's I2 (pool: (M⊕MH)≤bumpk e(M'⊕MH')) has NO +JH' term (unlike I3 budget which does), so it FORCES Mδ jγ-free, pushing the jγ into the budget. This is the SAME I2/I3 asymmetry dodged for ground (jγ-free Mδ); for fn-middle it's GENUINELY BINDING (2 jγ's, only 1 dodgeable). **FIX = re-architect the rebase lemma (module 50, H3) so I2 carries JH' symmetrically with I3. But naive strengthening (I2 → bumpk(e+ℕJH')) OVER-COUNTS JH' in mult-rebase (gives bumpk(u+ℕe+ℕ2·JH') vs conclusion bumpk((u+ℕe)+ℕJH')). Clean fix needs reworking mult-rebase + ZCont's index accounting + ALL rebase callers (packδ/pack2/packS/K) — SUBSTANTIAL, touches the tower's core lemma.** HONEST STATUS: fn-middle DIAGONAL verified in isolation (module 66, real hard novel piece); fn-middle THEOREM blocked by this rebase-I2 asymmetry. The all-ground tower (TS, ⊆T₃) stands complete; exceeding Design F (fn-middle) needs the rebase re-architecting. This is a precisely-characterized genuine obstruction, NOT mere plumbing. 66 modules clean, nothing committed, conjecture OPEN.
**★★ OBSTRUCTION RE-DIAGNOSED — CORRECTED & DEEPENED 2026-07-08 (SUPERSEDES the "re-architect rebase I2" fix above, which was the WRONG diagnosis): the fn-middle obstruction is NOT a rebase-lemma artifact and CANNOT be fixed by making rebase I2 carry JH'. It is a STRUCTURAL deficit of the flat-pool tower predicate. WHAT I DID: rebuilt module 66's diagonal to route γ's budget correctly — Mδ = bumpk (jγ +ℕ 3)(Mφ⊗(Mγ⊗ω)) (carries the multiplier's jγ, was bumpk 3, jγ-free), eδ = 2 (constant, was jγ+ℕ2), so jδ = rbb τ (jφ+ℕjγ) 2 carries jγ ONCE (rbb τ u e = u +ℕ (u-free overhead); the `rbb-shift` fact rbb τ (u+ℕv) e ＝ rbb τ u e +ℕ v). REGROUPING IS FREE: Mδ⊕Z reduces to Mδ definitionally (⊕ recurses on right, a⊕Z=a), so fnmid-I2's output index (total jγ+ℕ5 over base₀) re-splits as eδ=2 slack + bumpk(jγ+ℕ3) multiplier — SAME fnmid-I2, unchanged. PAYOFF: pack2's SCALAR PackDom clauses now BOTH compose — pm `Mδ ≤ bumpk(j2+ℕjγ)pool` via bumpk jγ (mult-absorb) [one jγ, matched by JH=jγ]; pj `jδ ≤ j2+ℕjγ` [one jγ each side]. Module 66 recompiles EXIT 0 (isolation), DialogueTreeHeight Index EXIT 0. **BUT THE za-REBASE STILL FAILS, STRUCTURALLY: the fn-middle diagonal's GROUND LEAF multiplier is bumpk(jδ)(Mδ) = bumpk(jφ +ℕ 2·jγ +ℕ 6) base₀ — jγ TWICE — because feeding the function-value γa to φ (ZApply arrow clause, MultHereditaryH.lagda:108-114) charges BOTH kbudget(γa)→JH AND kmult(γa)≤bumpk jγ Mγ→MH, and the flat leaf pool bumpk(j+ℕJH)(M⊕MH) composes budget-over-pool MULTIPLICATIVELY (bumpk of the bump-count over the pool). pack2's target leaf is bumpk(j2+ℕjγ)(pool) with j2 jγ-FREE (chosen before kγ) — jγ ONCE. Via mult-absorb (base₀→pool costs a CONSTANT), source needs j2 ≥ jφ+jγ+8 — IMPOSSIBLE. NO choice of Mδ/eδ/j2 and NO strengthening of rebase I2 manufactures the 2nd jγ the target simply does not carry.** This 2·jγ-vs-1·jγ LEAF DEFICIT is the genuine ordinal cost of feeding a budget-bumped function-value; it is close to the HEART of the open conjecture (higher-type composition compounding the budget). NOTE even pool-PRE-BUMPING (fold kbudget into the pool element, bumpk(jkg)(Mkg)) does NOT obviously fix it: the γ-VALUE γa is ALREADY a jγ-budgeted object (partial app of γ), so its kmult already has jγ bumps, and pre-bumping by its own budget compounds to 2jγ again. THE REAL NEXT STEP is a predicate where a function-arg's budget is NOT separately charged on top of a value that already carries it — think fresh about the accounting, don't just add JH' to I2. Module 66 KEPT in the improved form (cleaner: jδ carries jγ once, scalar clauses compose, obstruction isolated to za); its prose header honestly states all this. 66 modules clean, nothing committed, conjecture OPEN.
**★★★ REFRAMED 2026-07-08 (corrects the "near the HEART of the conjecture / needs pool-prebumping" pessimism in the note just above — that was TOO pessimistic on the ε₀/multiplier side): the fn-middle obstruction is PURELY BUDGET-BOOKKEEPING COMPOSITION, and budget is <ε₀-FREE. KEY FACT: bumpk (any finite n) preserves <ε₀ (ε₀ closed under ω^,⊗), so the leaf zone⊗bumpk(j+ℕJH)(M⊕MH) is <ε₀ for ANY finite budget — the budget's VALUE is irrelevant to <ε₀; it only governs whether the PackDom/rebase INEQUALITIES compose. Mδ=bumpk(jγ+3)base₀ and everything ε₀-side are FINE (all <ε₀; ValidMult preserved by bumpk). So the ONLY barrier is bookkeeping: the predicate charges a function-arg's use at 1× kbudget (ZApply arrow: JH +ℕ kbudget kh), but feeding a budget-bumped fn-value in the S-diagonal costs 2× (once into jδ-budget, once as kbudget-worth-of-bumps in Mδ). LEAF ARITHMETIC (SOLID): source bumpk(jδ)Mδ = bumpk(jφ+ℕ2jγ+6)base₀ ≤ target bumpk(j2+ℕJH_tgt)pool holds IFF JH_tgt ≥ 2jγ (bumpk jγ (mult-absorb), j2 jγ-free). So a predicate charging fn-args ≥2× (JH +ℕ 2·kbudget, or type-structured) MEETS the leaf requirement and stays <ε₀ for free. WRINKLE (the real work): current mult-rebase (H3) OVER-COUNTS — I3(budget)∘I2(pool) each carry JH_tgt → 2·JH_tgt; must rework to bound the combined budget+multiplier leaf DIRECTLY (bumpk(jδ+jγ+3)base₀ ≤ bumpk(j2+2jγ)pool) not by composing I2∘I3. CONCRETE CANDIDATE FIX (substantial, NOT "heart of conjecture"): (1) charge fn-args ≥2× budget in MultHereditaryH ZApply-arrow; (2) rework mult-rebase/ZCont-rebase (H3) to avoid the over-count; (3) re-verify H2–H17 (all budget-insensitive for <ε₀ ⟹ should re-close with adjusted constants). RISK: all-or-nothing on core H + H3, cascades through the H-tower; deeper fn-middle (σ₂ a function) may need >2× (type-structured budget). HONEST STATUS: leaf arithmetic + <ε₀-freeness SOLID; end-to-end composition is a CANDIDATE validated only at the leaf, not a done proof. This is the concrete next MAJOR effort — a focused core-predicate rebuild, best done in a dedicated fresh pass, not tacked onto a long session. [Refactor 2026-07-08: the pure arithmetic (mult-absorb=BrouwerOrdinals.MultBump/…, bumpk, ε₀ closures) now lives in BrouwerOrdinals/; the tower predicate H/H2–H17 stays in DialogueTreeHeight/.]
**★★ PROTOTYPE VALIDATED 2026-07-08 (module `MultHereditaryH18Proto`, EXIT 0, standalone, NOT in Index, nothing depends on it): the reworked-rebase arithmetic CORE is machine-checked. Two lemmas: (1) `mult-rebase-merged (base base′)(a b d Jt) : bumpk a base ≤ bumpk b base′ → le (d+ℕb) Jt → bumpk (d+ℕa) base ≤ bumpk Jt base′` — the MERGED-REPRESENTATION rebase core (multiplier-as-base, ALL bumps counted in the budget index d): a SINGLE bumpk-index chain resting on ONE pool-domination fact (the mult-absorb shape), NO two-hypothesis composition ⟹ structurally NO over-count (this is exactly what the old H3 mult-rebase, which composes I2-pool ∘ I3-budget each carrying JH′, could NOT avoid). (2) `fnmid-leaf (Mφ Mγ M2)(jφ jγ) : Mφ≤pool → Mγ≤pool → ω≤pool → bumpk (jφ+ℕ2jγ+6) base₀ ≤ bumpk ((jφ+ℕ8)+ℕ(jγ+ℕjγ)) pool` (base₀=Mφ⊗(Mγ⊗ω), pool=M2⊕Mγ) — the EXACT fn-middle τ=ι leaf that FAILED under 1× budget now CLOSES: output budget jφ+ℕ8 is jγ-FREE, arg budget credited 2×=jγ+ℕjγ. Proof = mult-rebase-merged (a=3,b=5 from mult-absorb n=0) + one le. GENERALIZED to `fnmid-leaf-gen (Mφ Mγ M2)(s j2 jγ) : le (s+ℕ5) j2 → bumpk (((s+ℕjγ)+ℕjγ)+ℕ3) base₀ ≤ bumpk (j2+ℕ(jγ+ℕjγ)) pool` — for ANY jγ-FREE output budget j2 covering s+ℕ5, closes; s absorbs jφ AND the rbb-τ spine overhead (jγ-free for every first-order τ), so this validates the WHOLE first-order fn-middle-S family, not just τ=ι (τ=ι = `fnmid-leaf-ι`, s=jφ+3, j2=jφ+8). CONFIRMS the doubling is ESSENTIAL: with 1× the target would be j2+ℕjγ, forcing jγ into the jγ-free j2 — impossible; with 2× the output budget stays jγ-free. So the arithmetic core of the fix (2× arg budget + merged rep) is SOUND & machine-checked. STILL UNVALIDATED (the larger rebuild): recursion-preservation of the merged invariants through ZApply/ZCont-rebase; re-closing K/Iter/ground-S under 2×; deeper fn-middle (fn RESULT type) possibly needing >2× (type-structured budget). Next major step = rebuild MultHereditaryH (charge fn-args 2×) + H3 (mult-rebase→merged form) + re-verify H2–H17.
**★ PARALLEL LINE STARTED 2026-07-08 (per user: this is ACADEMIC RESEARCH — keep the record of significant attempts, don't modify the working tower in place; COPY the radically-changing modules and change the copies). New `HM*` series = the merged/doubled-budget Howard-tower fork, ALONGSIDE the untouched flat-pool `H`/`H2–H17` (the record of the attempt that hits the fn-middle wall). FOUNDATION DONE: `MultHereditaryHM` (EXIT 0) = exact copy of MultHereditaryH's predicate (𝕂/ZPack/ZApply/ZCont/PackDom/kadd/kmult/kbudget + zone-mono-A/ZCont-mono-A/ZApply-mono-A) with ONE change — ZApply's ARROW clause credits a consumed function-arg 2×: `JH +ℕ (kbudget +ℕ kbudget)` (was 1×), in BOTH the predicate and ZApply-mono-A. HM.𝕂 ≠ H.𝕂 (independent predicate). REUSE (budget-agnostic, no copy): H2 arithmetic (rbb/bumpk-+/+ℕ/zdom-ext/pool-ext/B-step), H9 mult-absorb, H10 le-toolkit, H11 diag-absorb, H12, H16 fnmid-I2, BrouwerOrdinals. NEXT `HM*` modules to build (incrementally, verify each): HM3 (rebase), HM4 (predicate layer BndH/LT/BndL/GoodH), HM K/Iter/ground-S packs (re-close under 2× — budget-insensitive, mechanical), then HM fn-middle-S packs (packδ/pack2/packS/GoodH-S-fnmid) — the payoff. Prototype H18Proto (`mult-rebase-merged`, `fnmid-leaf-gen`) is the validated arithmetic core HM3 wraps. Nothing committed.
**★ HM3 DONE 2026-07-08 (EXIT 0, first-try compile for BOTH halves): `MultHereditaryHM3` = the rebase for the HM predicate. TWO parts: (1) STANDARD rebase (ZCont-rebase/ZApply-rebase mutual recursion + mult-rebase + ground-rebase + rbb-split/rbb-split-u + bumpk-pad-shuffle) = H3 VERBATIM retargeted to HM, EXCEPT ZApply-rebase ARROW clause threads the DOUBLED budget `kbudget +ℕ kbudget` (in the recursive-call JH/JH′ args AND I1kh/I3kh types+proofs). KEY FINDING: the doubling is a MECHANICAL substitution — kb enters I1kh/I3kh's index arithmetic (eq-l/eq-r/E via +ℕ-assoc/+ℕ-succ-right/bumpk-+/bumpk-pad) only OPAQUELY, so redefining `kb = kbudget+ℕkbudget` makes the SAME proofs go through (I2kh/I4kh have no kb, unchanged; ι-case & ZCont-arrow-case unchanged — they don't consume fn-args). This is why HM3 compiled first-try. (2) MERGED `ground-rebase-merged (A A′ D D′)(c c′ Msrc Mtgt)(jsrc J) : (∀w zone dom at bumpk J Mtgt) → (bumpk jsrc Msrc ≤ bumpk J Mtgt) → V ≤ zone_src⊗bumpk jsrc Msrc → V ≤ zone_tgt⊗bumpk(succ J)Mtgt` — the leaf rebase taking the COMBINED multiplier bound DIRECTLY (caller proves via mult-rebase-merged+mult-absorb), for the fn-middle pack2 leaf (Msrc=Mδ has hidden bumps, raw I2 can't absorb at constant slack). Same ⊗-≤-bump merge as ground-rebase, no I2, no over-count. IMPORT NOTE: HM3 imports _+ℕ_ from MultHereditaryH (shared with H2's rbb/bumpk-+), 𝕂/ZApply/ZCont/PackDom/kadd/kmult/kbudget from MultHereditaryHM. Parallel line state: HM ✓, HM3 ✓ (both foundational radical-change modules done). NEXT: HM4 (predicate layer) then the packs.
**★ HM4 + HM7 DONE 2026-07-08 (both EXIT 0): `MultHereditaryHM4` (predicate layer BndH/LT/BndL/GoodH + GoodH-ι-to-<ε₀ + base combinators GoodH-Zero/Succ/Ω + GoodH-app + pack-to-Tracked + ⊕-Z-left) = H4 VERBATIM retargeted (imports: _+ℕ_ from H, 𝕂/ZPack/ZApply/ZCont from HM). Ports unchanged because the predicate is structural over 𝕂 and the base packs (packSucc/packΩ) + pack-to-Tracked touch ONLY the GROUND ZApply clause (unchanged by the fork; doubling is in the ARROW clause). So the 2× fork AGREES with the original tower on everything at ground arg type. `MultHereditaryHM7` (K pack GoodH-K) = H7 retargeted (imports HM/HM3/HM4), needed the doubled-budget threading: ~8 accumulator spots `kbudget X Y kh` → `(kbudget X Y kh +ℕ kbudget X Y kh)` (cpack fn-clause JH, ktail-ι/ktail-fn fn-clauses JH+pad, body(σ₁⇒σ₂) PackDom targets+tail via local `kb2 ka`), AND TWO proofs that were EQUALITIES under 1× became PADDED under 2×: (a) body pj (kJ ka=rbb(kbudget)0 ≤ bumpk(rbb00+ℕkb2)P: was transport-E-refl, now ≤-trans[transport E][bumpk-pad (rbb00+ja′) ja′ + +ℕ-assoc]), (b) tail's hj (was ≤-refl(bumpk kbudget P), now bumpk-pad kbudget kbudget P). body-ι case unchanged (ground arg). CONFIRMS the "budget-insensitive for <ε₀, mechanical" prediction — K re-closes under 2×, the doubling is free. Parallel line: HM ✓ HM3 ✓ HM4 ✓ HM7(K) ✓. NEXT: HM6 (Iter — consumes fn-arg, similar doubling), HM5 if needed, then the fn-middle S packs (the payoff). Nothing committed.
**★ HM5 + HM6 DONE 2026-07-08 (both EXIT 0): `MultHereditaryHM5` (good-inputs orbit orbit-goodT/BndH-to-aff-good) = H5 VERBATIM retargeted (0 kbudget/ZApply refs — pure structural over 𝕂/BndH at ι⇒ι). `MultHereditaryHM6` (recursor GoodH-Iter) = H6 retargeted (imports HM/HM4/HM5, +bumpk-pad/+ℕ-assoc from H2), doubled-budget spots (7, all as predicted): body-type JH `0+ℕkbudget`→`0+ℕ(kbudget+ℕkbudget)`; outer-pack index BI `bumpk(3+ℕjg)`→`bumpk(3+ℕ(jg+ℕjg))`; M₂BI gained ONE bumpk-pad jg (M₂=bumpk(3+ℕjg)(Mg⊕Z) ≤ bumpk(3+ℕjg)(ω⊕Z⊕Mg) ≤[pad+ +ℕ-assoc 3 jg jg] BI); ωBI `≤-bumpk(3+ℕ(jg+ℕjg))`; pj₂/pj₃ `bumpk(3+ℕ(jg+ℕjg))Q`; inner-type JH `jg`→`jg+ℕjg`. KEY DISTINCTION preserved: `jg` as kg's INTRINSIC budget (M̂=bumpk(jg+ℕ0)(Mg⊕Z), M₂=bumpk 3 M̂) is UNCHANGED; only `jg` as the ACCUMULATOR budget (after consuming kg) doubles to jg+ℕjg. Everything else (pd/pm/ground) uses BI/M₂BI/ωBI as opaque tools → unchanged. **Parallel line COMPLETE through the recursor+K+base: HM ✓ HM3(rebase+merged) ✓ HM4(pred layer+Zero/Succ/Ω/app) ✓ HM5 ✓ HM6(Iter) ✓ HM7(K) ✓ — 6 modules, all EXIT 0, all budget-insensitive-for-<ε₀ confirmed.** The ground-arg machinery ports verbatim; fn-arg-consuming packs need mechanical 2× threading + occasional refl→pad. NEXT & CLIMACTIC: the fn-middle S packs — Sδ-fnmid (module 66/H17 retargeted, using HM3), pack2-fnmid (consuming kγ via ground-rebase-merged — THE PAYOFF spot), packS-fnmid, GoodH-S-fnmid. This is the novel part that failed in the H line; the HM foundation is built to make it compose. Nothing committed.
**★★★ CRITICAL CORRECTION 2026-07-08 (SUPERSEDES the "2× budget closes fn-middle S" / "prototype validated the fix" claims of THIS session — those were WRONG; the H18Proto prototype was FLAWED): the 2× budget approach does NOT close the fn-middle S obstruction. The deficit is SCALING-INVARIANT. CAREFUL LEAF RECOUNT (τ=ι): feeding the γ-VALUE kg to φ via φ's ZApply arrow clause (budget factor m) charges m·jkg into the budget (jkg=kg's budget ≤jγ) ⟹ uδ=jφ+ℕm·jγ ⟹ jδ≈jφ+m·jγ+const; BUT Mδ=bumpk(jγ+3)base₀ carries exactly ONE jγ (from γ's PackDom on kg's MULTIPLIER Mkg≤bumpk jγ Mγ — INDEPENDENT of m, since MH gets Mkg un-prebumped and only JH doubles). Leaf = bumpk(jδ+ℕjγ+3)base₀ = bumpk(jφ+ℕ(m+1)·jγ+const)base₀ = (m+1)·jγ. pack2 consuming kγ provides m·jγ (m× kγ's budget). DEFICIT = (m+1)−m = jγ, for ALL m. So m=2 (HM) gives 3jγ-vs-2jγ, SAME jγ deficit as m=1 (2jγ-vs-1jγ, the H line). THE PROTOTYPE'S FLAW: H18Proto's fnmid-leaf modeled the SOURCE with 1×'s uδ=jφ+jγ (⟹ 2jγ source) but the TARGET with 2× (2jγ) — an INCONSISTENT mix of m. Under consistent m both scale, deficit persists. ALSO CHECKED: uniform pre-bumped pool (MH += bumpk(kbudget)(kmult)) does NOT fix it either — it COMPOUNDS at the diagonal (φ feeds kg whose Mkg already has jγ bumps → bumpk(jkg)(Mkg) ≤ bumpk 2jγ Mγ ⟹ Mδ needs 2jγ ⟹ leaf 3jγ). ROOT: the deficit is the γ-VALUE's MULTIPLIER jγ — the genuine ORDINAL COST of a FUNCTION-TYPED intermediate result. Ground-S works precisely because a ground γ-value is a GFun fed via the ZApply GROUND clause (contributes to the ZONE, NOT the pool/budget) ⟹ Mδ jγ-FREE, no deficit. Function γ-value ⟹ mult cost jγ ⟹ deficit. This CONFIRMS THE ORIGINAL (pre-2×) framing ("near the HEART of the conjecture: higher-type composition compounding") and REFUTES the 2×-excursion optimism. HM line (HM/HM3–HM7, 6 modules, all EXIT 0) is a VALID predicate variant = "the tower under 2× budget", a legit research artifact, but will NOT close fn-middle S (same jγ deficit) — do NOT build fn-middle packs on it expecting success. What WOULD fix it: give the MULTIPLIER (not the budget) extra capacity WITHOUT compounding at the diagonal — an open design problem, genuinely deep. HONEST: this session's 2× line is a well-built dead end for the fn-middle goal; the arithmetic reframing ("budget is <ε₀-free") is still TRUE but INSUFFICIENT (the deficit is a MULTIPLIER-capacity problem, not a budget-value problem). Conjecture OPEN; fn-middle S obstructed.
**★★ AFFINE-MIDDLE SUB-CASE IS VACUOUS 2026-07-08 (chased the one arithmetically-closing special case to ground): the deficit = jγ (the MIDDLE argument kγ's budget); it vanishes IFF jγ=0. Checked whether any closed term has a budget-0 FUNCTION-TYPED middle γ:ι⇒(σ₁⇒σ₂): NO. Base-pack budgets (verified in code): Succ/Ω/Zero j=0 but GROUND-typed (ι⇒ι / ι); K j=rbb σ 0 0 ≥1 ALWAYS (rbb ι 0 0=succ(0+0)=1, arrow clauses only add succ — MultHereditaryH2 rbb def); Iter j=3. A middle returning a FUNCTION must be built with K/S/Iter ⟹ budget ≥1. So budget-0 fn-typed middles DON'T EXIST ⟹ the affine-middle case (the only sub-case where the jγ deficit vanishes) is VACUOUS. CONCLUSION: fn-middle S is FULLY obstructed in the tower for every actual term — the jγ deficit always bites (jγ≥1 for every fn-typed middle), and no non-vacuous restriction escapes it. This is the sharp form of the frontier: the flat-multiplier Howard tower DEFINITIVELY cannot reach fn-middle S. Redirects future effort AWAY from budget/pool bookkeeping tweaks (all shown to fail: 2× scaling, budget-folding, pre-bumping, affine restriction) and toward a fundamentally different multiplier mechanism for function-typed intermediate results — the deep open problem = the conjecture's frontier.
**★★★ ROOT-CAUSE STRUCTURAL INSIGHT 2026-07-08 (why ALL escapes fail with the same signature — the RECURSOR vs fn-middle-S TENSION): the γ-value's multiplier bound Mkg ≤ bumpk jγ Mγ (the "rigid multiplier" PackDom clause, MultHereditaryH.lagda) makes a PARTIAL APPLICATION's multiplier carry its BUDGET as bumps. This is EXACTLY WHAT THE RECURSOR NEEDS: pack-to-Tracked (H4) folds an arg's budget into its multiplier M̂=bumpk(jg)(Mg) precisely so MultHereditaryG's orbit engine can consume it (one bump = one orbit raise = one ω). But that SAME feature is what dooms fn-middle S — it is the source of Mδ's jγ (the deficit). So: the RECURSOR REQUIRES `multiplier carries budget (bumped)`; fn-middle S FAILS BECAUSE `multiplier carries budget`. OPPOSITE requirements on the same multiplier↔budget relationship. NO single flat-multiplier predicate satisfies both — which is why all 6 escapes failed with the identical jγ-deficit signature (they all preserve multiplier-carries-budget, mandatory for the recursor, fatal for fn-middle S). THIS IS THE TOWER'S FORM OF "ONE ω PER TYPE LEVEL": the recursor's budget-in-multiplier IS the ω-per-level growth, and fn-middle S composition cannot absorb one extra level of it. Matches the earlier "fragments genuinely INCOMPARABLE at the frontier" finding (module 44 note): more data (bumped mult) ⟹ recursor works, fn-middle S fails; less data ⟹ vice versa. CONSEQUENCE FOR STRATEGY: a predicate that does BOTH recursor AND fn-middle S needs the multiplier and the orbit-budget DECOUPLED — likely a 2-component (mult, orbit-count) structure where the orbit engine reads the count but function-composition does NOT pay it, i.e. the (height, magnitude) TWO-COMPONENT split (…TwoComponent) lifted to the hereditary/tower level. That is the genuine next architecture, NOT a tower tweak. This is consistent with the canonical dialogue-tree-height.md thesis (depth cannot see itself; magnitude drives the tower, height rides linearly). Conjecture frontier = decouple orbit-count from composition-multiplier hereditarily.
**★ PAPER DESIGN STUDY 2026-07-08 (`dialogue-tree-height-decoupling.md`, no code): worked out the decoupling on paper. KEY REFINEMENT of the diagnosis: the decoupled-multiplier hypothesis (multiplicity composes ⊗, <ε₀) is REAL but INCOMPLETE — it shows the MULTIPLICITY axis is NOT the obstruction (all-ground S is exactly the pure-multiplicity case, and it closes). The recursor is NOT a multiplier: it's AFFINE with an additive CONSTANT c_g⊗ω (Orbit: height(iter' g a n) ≤ (height a ⊕ c_g⊗ω) ⊕ height n), and c_g grows one exponential per recursor-nesting = the ε₀ tower. So the tower's bumpk-BUDGET was really tracking this ARGUMENT-DEPENDENT CONSTANT (folded into the multiplier for uniformity), and THAT (not multiplicity) is the fn-middle obstruction: γ-with-recursor's constant c_γ⊗ω depends on γ, and absorbing it hereditarily through φ's affine structure is what fails. Decoupling doesn't remove the constant — it reveals the constant was the issue all along. RIGHT ARCHITECTURE (confirms & sharpens the existing (A)+(B) split): hereditary two-component (height, magnitude): magnitude bounds the constants (c_g ≤ ω^magnitude, canonical thesis; citable Howard = (A)); height composes LINEARLY (grafting Lemma 3, one ⊕ per plugged subtree regardless of reuse) with recursor increments = ω^(magnitude) = bridge (B). Under this, fn-middle S closes (height linear-⊕, no ⊗-multiplier deficit; constant carried additively/as-value not as multiplier). RESIDUAL = hereditary bridge (B1): a functional's per-step height increment IS ω^(its magnitude), at higher types = conjecture's open core. CONCRETE LOW-RISK NEXT TARGET (before any code): the SECOND-ORDER bridge — φ:(ι⇒ι)⇒ι applied to a function g — proved CONSISTENTLY (same accounting source & target, the mistake the 2× line made). Only after that leaf closes on paper → predicate code. DO NOT build another single-multiplier tower variant.
**★★★ SECOND-ORDER BRIDGE WORKED OUT + TOWER FRAMING RETIRED 2026-07-08 (paper, `dialogue-tree-height-decoupling.md` §"second-order bridge"): the decisive finding. (1) HEIGHT COMPOSES ADDITIVELY: applying a function inside another is Kleisli grafting, Lemma 3 (height-kleisli-extension) adds the grafted cost ONCE (⊕, max over leaves — NOT per-use, NOT ⊗). The ONLY super-linear step is the recursor: iter'  gives (height a ⊕ c_g⊗ω)⊕height n — one ⊗ω, with ADDITIVE increment c_g. ε₀ lives in the INCREMENTS (climbing ω^ω, ω^(ω^ω)… as recursors nest), ADDED never multiplied. (2) FN-MIDDLE S IS NOT A SEPARATE OBSTRUCTION: S φ γ a = φ a(γ a); the middle's cost is c_{γa} (γa's increment), appearing ONCE as a recursor per-step increment. The tower encoded this ADDITIVE constant as a bumpk-jγ MULTIPLIER, which necessarily surfaced in TWO places (jδ budget + Mδ mult) = the 2jγ-vs-1jγ deficit = a SELF-INFLICTED ARTIFACT of multiplicative encoding. Additively it's single, no deficit. (3) CONFIRMED IN CODE (Majorant.lagda): Iter₁ is FIXED at ground ((ι⇒ι)⇒ι⇒ι⇒ι) so every T₁ recursor iterates a FIRST-ORDER function ⟹ only first-order increments (c≈ω^ω); S₁ POLYMORPHIC so fn-middle S ∈ T₁; `fundamental:(t:T₁ σ)→R σ ⟦t⟧₁(μ t)` PROVES height⟦t⟧≤μ t for ALL T₁ INCLUDING fn-middle S (R-S). So fn-middle S's height bound is DONE; the ENTIRE residual is the uniform μ t<ε₀ for T₁, and since all recursors are first-order that's the FIRST-ORDER magnitude→increment bridge — NOT a higher-order kernel. FN-MIDDLE S ADDS NO NEW OPEN PROBLEM AT FIRST ORDER. **CONSEQUENCE — RETIRE THE TOWER FRAMING for fn-middle S: modules 45-66 + the HM* fork tried to bound μ(S φ γ)<ε₀ via a single-multiplier hereditary predicate and hit the self-inflicted deficit. The right route is the UNIFORM first-order μ t<ε₀ for T₁ (hereditary two-component / affine closure with recursor increments = ω^magnitude), which yields fn-middle S FOR FREE.** CONCRETE NEXT TARGET (paper→code): the SECOND-ORDER affine/increment closure — μ of a second-order functional bounded <ε₀, its fn-arg's increment supplied by the MAGNITUDE component (not fixed data, the Design-F limitation; not a bumpk multiplier, the tower deficit) — completing μ t<ε₀ for T₁. First-order in the recursors; BELOW the genuine open kernel B1 (higher types). DO NOT resume the tower's fn-middle packs. Existing tools for this route: Majorant (height≤μ done), Affine/AffineClosure/AffineOrbit (affine-orbit-<-ε₀), MultOrbit, Magnitude/Count (the magnitude side), Epsilon0 (⊕/⊗-<-ε₀ closures).
**★★★ STRATIFY-BY-ITERATOR-LEVEL + GENUINE-ORDINAL AFFINE TRANSFORMERS 2026-07-08/09 (Escardó's steer via his CSL2011 paper "System T and the Product of Selection Functions"; full paper analysis in `dialogue-tree-height-decoupling.md`): THE most tractable & best-founded plan yet. STRATEGY: don't build ONE predicate for all T (the tower's fatal flaw — no uniform top). Stratify Tₙ=System T with iterators of type level ≤n; prove dialogue height <bₙ (bₙ<ε₀) by a logical relation Rₙ (bounded strength ⟹ tractable); induct on n, sup bₙ=ε₀. INDUCTION STEP = CSL2011's Pₙ⟺T_{n+1} (level-n+1 iterator = finite product of level-n selection functions = finite sequential game, bounded by IH) ⟹ b_{n+1}=ω^{bₙ}-ish. S/K WORRY RESOLVES: S/K/app are STRUCTURAL (height composes ADDITIVELY via grafting Lemma 3, one ⊕ per plugged subtree, ANY type level; don't loop) — only ITERATORS drive the ordinal (feed the orbit ⊗ω / ω^(m⊗ω)). So high-level S/K add only finite structural height, can't manufacture higher-level iteration (hierarchy strict). REFINEMENT: the ε₀-tower JUMPS come from iterating a MULTIPLIER>1 function (S duplicates ⟹ mult>1; MultOrbit ⟹ ω^(m⊗ω), one exponential per level), NOT additive iteration (stays low). **DATA CLASS (the missing third option): GENUINE-ORDINAL AFFINE TRANSFORMERS. Design F=FIXED data (d,m), fails fn-middle (d can't depend on arg). Tower=arg-dependent data but recursor-raise=bumpk-BUDGET(count), double-counts. NEW=arg-dependent data as GENUINE ORDINALS + recursor-raise from orbit engines (AffineOrbit/MultOrbit) as a real ordinal factor, NO budget.** Aff ι a:=a<ε₀; Aff(σ⇒τ)φ:=(∀g,Aff g→Aff(φ g))∧ φ affine-transforming (output data = ⊕/⊗/ω^/orbit function of input data). First-order: Design F's Aff `φ a≤(a⊕d)⊗ι[m]` VERBATIM. Second-order((ι⇒ι)⇒ι): transformer D(d,m)=c₀⊕c₁⊗d⊕c₂⊗ω^(m⊗ω). VERIFIED ON PAPER (consistently, source&target same accounting — the 2× lesson): CASE 1 μ-Iter transformer D_Iter(d,m)=d⊗ω (m=1, Orbit/AffineOrbit) or ω^(m⊗ω) (m>1, MultOrbit) — genuine ordinal, no budget; CASE 2 fn-middle μ-S φ γ a=φ(a)(γ(a))=D(d_{γa},m_{γa})=c₀⊕c₁⊗d_{γa}⊕c₂⊗ω^(m_{γa}⊗ω)<ε₀ — d_{γa} appears ONCE, CLOSES, no deficit (tower wrote same quantity multiplicatively-with-bumpcount ⟹ paid twice). REMAINING PAPER: μ-app second-order transformer composition (new lemma, ordinary ordinal arith, expected OK, write CONSISTENTLY first). Then Aff at ι ⟹ μ t<ε₀ for all T₁ incl fn-middle S. PROTOTYPE ORDER: (1) second-order transformer type + ⊕/⊗/ω^ closures (reuse Epsilon0/AffineOrbit/MultOrbit); (2) Aff-Iter + Aff-S-fnmiddle standalone; (3) Aff predicate + fundamental thm over T₁. NO tower, NO bumpk. This completes the FIRST-ORDER theorem (T₁, incl fn-middle S) — below the higher-type kernel B1.
**★ ARITHMETIC CORE MACHINE-CHECKED 2026-07-09 (`AffineTransformerProto.lagda`, EXIT 0, standalone, not in tour): the two ordinal facts the route rests on, whose bumpk analogues gave the tower deficit: `D-Iter-add-<-ε₀:(d)→d<ε₀→d⊗ω<ε₀`, `D-Iter-mult-<-ε₀:(m)→m<ε₀→ω^(m⊗ω)<ε₀` (recursor transformer, additive & multiplicative raise), and `fnmid-μS-<-ε₀:(c₀ c₁ c₂ d m)→all<ε₀→ c₀⊕(c₁⊗d)⊕(c₂⊗ω^(m⊗ω))<ε₀` (the fn-middle μ-S bound). Trivial via ⊕-<-ε₀/⊗-<-ε₀/ω^-<-ε₀/tower-<-ε₀ — and THAT'S THE POINT: d_{γa} occurs ONCE, whole thing is ε₀-closed ops, NO side condition, vs tower's impossible j2≥jφ+ℕjγ. Confirms fn-middle S closes arithmetically with genuine ordinals. NEXT (the substantial development, fresh focused effort like Design F but with transformers): the hereditary Aff predicate (Aff ι a:=a<ε₀; Aff(σ⇒τ) = hered + affine-transforming) + fundamental theorem over T₁ (K/Ω/Succ/Zero routine from Design F; Iter via Case 1 orbit engines; S incl fn-middle via Case 2 transformer; app = second-order transformer composition, the one unwritten paper lemma). Reuse Majorant (height≤μ done for ALL T₁ incl fn-middle S), Design F's Aff (first-order verbatim), AffineOrbit/MultOrbit/Epsilon0.
**★★★ STRUCTURAL CRUX RESOLVED POSITIVELY 2026-07-09 (machine-checked, `AffineTransformerProto.lagda` EXIT 0): the Aff-S composition question — is μ-S φ γ AFFINE (iterable) when φ,γ are? — is YES. The worry was μ-S φ γ (a)=φ(a)(γa) puts `a` NESTED (γa's constant is itself affine in a: (a⊕e)⊗M; recursor-φ yields a⊕(a⊕e)⊗M). LEMMA `nested-a-absorb:(a e M)→S Z≤M→ a⊕(a⊕e)⊗M ≤ (a⊕e)⊗(M⊗ω)` — the nested a COLLAPSES INTO THE MULTIPLIER (via a≤(a⊕e)⊗M [⊕-increasing-right+x-≤-x⊗] then X⊕X≤X⊗ω [⊕-dup-≤-⊗ω] then ⊗-assoc). LEMMA `fnmid-S-affine:(a e M q)→S Z≤M→ (a⊕(a⊕e)⊗M)⊗q ≤ (a⊕e)⊗((M⊗ω)⊗q)` — the full fn-middle μ-S majorant (arg-dependent constant (a⊕e)⊗M from γ, ×q from φ) is dominated by the AFFINE form (a⊕e)⊗m′, m′=(M⊗ω)⊗q, DERIVED data, still affine in a, <ε₀. So μ-S φ γ is AffBounded ⟹ ITERABLE (via MultOrbit). THIS IS THE STEP DESIGN F COULD NOT TAKE (fixed data can't derive the arg-dependent affine data from φ's transformer + γ's data; the TRANSFORMER can). CAVEAT: verified for the RECURSOR-shaped φ (the hard case where the orbit produces the arg-dependent constant); general fundamental theorem needs all combinator cases, but THE KEY NEW MECHANISM is machine-checked. STATE: the affine-transformer route to FIRST-ORDER fn-middle S is now validated at BOTH the ARITHMETIC (fnmid-μS-<-ε₀) AND STRUCTURAL (nested-a-absorb/fnmid-S-affine) level — the two things that decided the route. Both positive. The tower's fn-middle wall was self-inflicted (bumpk encoding); the affine-transformer (genuine ordinals + transformer data + nested-a absorption) closes it. NEXT: build the full Aff predicate + fundamental theorem over T₁ (base combinators from Design F, Iter via orbit engines, S via nested-a-absorb, app via second-order transformer composition) — now with BOTH crux points de-risked & machine-checked. Still first-order (below B1 kernel). AffineTransformerProto has: D-Iter-add/mult-<-ε₀, fnmid-μS-<-ε₀, nested-a-absorb, fnmid-S-affine.
**★★ PREDICATE PINNED & TYPE-CHECKS 2026-07-09 (`AffTransformerPredicate.lagda`, EXIT 0): the design pass. The predicate = `MultHereditaryFAffN`'s BoundT VERBATIM except ONE clause: BoundT((σ₁⇒σ₂)⇒τ) changes from `𝟙` (the collapse = fn-middle boundary) to a TRANSFORMER. `BoundT′ ι a D c M=(w)→a w≤(D w⊕c)⊗M`; `BoundT′(ι⇒τ) T D c M=(T₁:𝓑→𝓑)→BoundT′ τ(T T₁)(λw.D w⊕T₁ w) c M` [ground arg into D, verbatim]; **`BoundT′((σ₁⇒σ₂)⇒τ) T D c M=(T₁:𝕋(σ₁⇒σ₂))(D₁ c₁ M₁)→GoodT ι D₁→c₁<ε₀→ValidMult M₁→BoundT′(σ₁⇒σ₂) T₁ D₁ c₁ M₁→BoundT′ τ(T T₁) D (c⊕c₁) ((M₁⊗ω)⊗M)`** [fn-arg carries its affine data (D₁,c₁,M₁), result data DERIVED: constant c⊕c₁, multiplier (M₁⊗ω)⊗M = EXACTLY fnmid-S-affine]. `JAff′ τ T=Σ D c M,GoodT D×c<ε₀×ValidMult M×BoundT′`. Recursion well-founded (structural on type: σ₁⇒σ₂ and τ both subterms). CONFIRMED BY REFL: BoundT′((ι⇒ι)⇒ι) unfolds DEFINITIONALLY to the transformer form with derived bound (D w⊕(c⊕c₁))⊗((M₁⊗ω)⊗M) — pinned predicate = machine-checked fnmid-S-affine composition, by definition. So: predicate PINNED, TYPE-CHECKS, fn-middle clause = the verified structural lemma. Imports 𝕋/GoodT from MultHereditaryF, ValidMult from MultDominated. NEXT (the build, all crux de-risked): the fundamental theorem — prove JAff′ for each T₁ combinator (base K/Ω/Succ/Zero adapt Design F's JAff-*; Iter via orbit engines; **S fn-middle diagonal via the transformer clause + fnmid-S-affine**; app). Then JAff′((ι⇒ι)⇒ι)(μ t) applied to μ-Ω gives height<ε₀. Design F's JAff-Sg-diag / BoundT-absorb are the templates to adapt (they collapse at fn-args; now don't). Files: AffineTransformerProto (crux lemmas), AffTransformerPredicate (pinned predicate). Both standalone, EXIT 0, not in tour. State: all-ground tower (TS ⊆ T₃, modules 56-64) COMPLETE & sound; fn-middle S obstructed; HM 2× line (6 modules) + H18Proto = documented dead ends (annotated in-file). Conjecture OPEN.
SUMMARY BUILD PLAN: module H8: quad-helper (a⊕(b⊕(c⊕d)) ≤ z⊗ω from 4 bounds — via FAff2.⊕-quad-≤-⊗ω + monos), trip3-reassoc variant (a⊕(b⊕c)), packδ-σι/packδ-σarrow, pack₂, packS, GoodH-S-ρι {σ τ}, then H9: fragment T₄ (Ω/Zero/Succ/Iter/K/S-ρι/app) + goodμ + payoffs = THE NEAR-COMPLETE FIRST-ORDER THEOREM (missing only Sh: ρ-arrow S — pinned obstruction: budget-form needs pool-prebumping refinement; NOTE Sh was TRIVIAL in all F-line designs — a HYBRID fragment embedding argument may recover it: T₁-terms where fn-shared S-instances… or refine PackDom's budget clause to ∀P → bumpk kb P ≤ bumpk j (bumpk JH-ish…) — think fresh).** Conjecture OPEN.
  - `…Bridge` (added 2026-06-30): the (B) attack. `iter-tower-step` (one ω per nesting: increment ω^e ⟹ orbit exponent e+1, via the engine + `ω^ι[e]⊗ω=ω^ι[succ e]`), `Ω-increment` (oracle = ω^0 base). Honest: the tower-step is the engine of (B1); the OPEN kernel is that a functional's increment IS ω^magnitude carried hereditarily (the dialogue content). Thin-ish (leverages the engine); the genuine open (B1) magnitude→height bridge for higher types is NOT proved. (Note: this serves FIRST-ORDER (<ω^ω); full ε₀ across all type levels is the OPEN conjecture, needs higher types.) **HIGHER-TYPE TOWER STEP GENERALIZED (2026-07-01, EXIT 0):** the numeral-exponent `iter-tower-step` (ι[e], caps at ω^ω) is now generalized to a **Brouwer-code exponent** `e : 𝓑`: `iter-tower-step-code : (∀k height(iter f x (succ k)) ≤ height(iter f x k) ⊕ ω^ e) → height(kleisli-ext (iter f x) n) ≤ (height x ⊕ ω^(S e)) ⊕ height n` (one-liner: `height-iter-from-step f x (ω^ e)`, since `ω^(S e)＝ω^ e⊗ω` definitionally — the engine was always exponent-agnostic). And the ε₀ conclusion `iter-tower-step-<-ε₀`: for `e<ε₀`, `height x<ε₀`, `height n<ε₀`, the iteration height `<ε₀` (via new `S-<-ε₀`,`ω^-<-ε₀`,`⊕-<-ε₀`). So the **higher-type tower mechanism** (`ω↑↑ℓ` with exponent an ordinal `tower ℓ`, not a numeral — the actual climb to ε₀, not the ω^ω floor) is now machine-checked, STILL conditional on the open kernel (B1) that supplies the exponent `e` for a functional. Realizes the prose in the old Bridge closing note as a theorem. This is engine-side and honest — does NOT close (B1).
  - `…Majorant` (added 2026-06-29): **sub-development (II) structural core DONE** — the first-order **fundamental theorem** by induction on the term. Height-indexed logical relation `Maj ι = 𝓑`, `R ι x a := height x ≤ a` (hereditary); combinator majorants + lemmas `R-Zero/Succ/Ω/K/S`; ground recursor `R-Iter` via `height-iter-≤-orbit-bound` with orbit-sup `μ-Iter φ a ν = L(λk. iter φ a k) ⊕ ν`. First-order syntax = inductive `T₁` (Iter only at ι; K/S/app polymorphic; constructors suffixed ₁ to avoid TΩ clash). `fundamental : (t : T₁ σ) → R σ ⟦t⟧₁ (μ t)`; `height-≤-μ`, `dialogue-height-≤-μ`. **Height-only majorant suffices at first order** (magnitude only needed for higher-type Part-IV overshoot). Imports MFPS-XXIX for B-Set⟦⟧/iter'/etc. EXIT 0. REMAINING for first-order <ε₀: prove `μ t < ε₀` (the affine-class closure — show all first-order majorants are affine `a↦a⊗ι[p]⊕d`; ε₀ tools ready in `…Epsilon0`).

- **The open core ("orbit lemma"):** `sup_m height(fᵐ x) < ε₀` for every System T `f`. Equivalent to the conjecture for a related term, so it is the whole theorem in local form, not a peelable sub-lemma. The right route is a Tait-style computability / infinitary (ω-rule) ordinal analysis *native to dialogue trees* — NOT Howard 1970 (which bounds values, not rank; height ⊥ value, witnessed by `iter generic base (generic (η 0))`: height ω, magnitude 0). Genuinely new lever available: "depth cannot see itself" (linearity) + "counts are magnitudes" (Count Lemma).

**HOWARD REFRAMING (2026-06-30, per Escardó's steer — supersedes the earlier "Howard inapplicable" position):** Howard IS the lever, not an obstacle. The earlier claim (Conditional.lagda) that "height ⊥ value ⟹ Howard inapplicable" conflated value with rank. Howard's ordinal `o(t)` bounds not the *value* but the *ordinal RANK* of the computation's well-founded structure — and dialogue height is exactly such a rank (NOT a value, NOT even query-count: an ω-branching well-founded tree with all branches finite can have rank ω², ω^ω, …). On the rank-ω/magnitude-0 witness `λα. iter α b (α 0)`, `o ≥ ω` (it sees the iteration, not the value), so `o` dominates the height. So the orbit bound `sup_k height(fᵏx) < ε₀` IS the dialogue instance of `|System T| = ε₀`, split as **(A)** magnitude/value orbit grows at rate `< ε₀` (classical Howard, CITABLE: `iter f x k` is one System T term in k) + **(B)** the bridge "height-increment ≤ ω^magnitude" (dialogue-native, height rides on (A) via the Count Lemma). (B) is the residual; (A) is Howard. The affine route capped at ω^ω BECAUSE it dropped the magnitude component — the type-level tower is driven by the value/magnitude growth, so height-only can see only one ω. Corrected in Conditional.lagda preamble + `…TwoComponent`.
  - `…TwoComponent` (added 2026-06-30, EXIT 0, no postulates, 16 modules): the two-component (height, magnitude) majorant. `Maj₂ ι = 𝓑 × (ℕ→ℕ)`, `R₂ ι x (a,V) = (height x ≤ a) × mag-≤ V x`, hereditary. PROVED structural cases `R₂-Zero/Succ/Ω/K/S/app` (magnitude rows = the `mag-*` lemmas from `…Magnitude`; Ω's = oracle reset). The recursor `R₂-Iter-ground` is the (A)+(B) interface: magnitude discharged by `mag-kleisli` (Lemma R1) with orbit-magnitude data as input [= Howard (A)]; height discharged by `height-iter-≤-orbit-bound` GIVEN the orbit-height bound [= residual (B)]. So the whole conjecture is localized in machine-checked form to (B), with (A) a Howard citation and everything else proved.

  - `…PolyHereditary` (added 2026-07-01, 19 modules, no postulates, EXIT 0, in tour): the **⊞-based hereditary predicate** — the planned next step, scaffold DONE. Rebuilds `Hereditary` over ω-polynomials with the COMMUTATIVE natural sum `⊞` and explicit MULTIPLICITY `k`: `JBp ι a ps k d = a ≤ ⟦ nmul k ps ⊞ d ⟧` (common poly bound `ps` on ground args, `k` natural-summed copies + constant poly `d`); `JBp(ι⇒τ)` quantifies `x≤⟦ps⟧`; `JointAffp = Σ k d,∀ps JBp`; mutual `FunArgs`/`plug`/`𝔅p` (reuses `Maj`,`GroundType` from `Hereditary`). Fixes the two defects of finite-`ι[m]` `Hereditary`: (i) `⊞` commutative ⟹ reorder/duplicate ground args freely (`poly-key` either order); (ii) `nmul k` carries the multiplicity S needs (uses its ground arg twice), scaling COEFFICIENTS not DEGREE (matches `poly-map-orbit-mult`). PROVED: poly algebra `⊞-exch` (middle-four exchange), `nmul-⊞` (distributes over ⊞), `nmul-[]`; ground extraction `𝔅pι-poly-bound`/`poly-bounded-to-𝔅pι` (`𝔅p ι a ⟺ a poly-bounded`) ⟹ `𝔅pι-to-<ε₀`; argument-shift `JBp-shift` (absorb dropped common-bound part into constant WITH multiplicity, via commutative ⊞ — works for every `k`, unlike `Hereditary.JB-shift`); **application closure `𝔅p-app`** (linchpin — ground arg poly-bounded, fed under enlarged common bound `ps⊞dG` then absorbed via `JBp-shift`); base combinators `𝔅p-Zero/Succ/Ω` (Succ = `k=1,d=[]`; Ω = `k=1,d=[1]`). **Honest ceiling** (same as `Hereditary`): poly multiplier caps fragment at `ω^ω`, so ground extraction is poly-boundedness NOT full `<ε₀`; this is the hereditary fundamental theorem for the MAXIMAL POLY FRAGMENT, ε₀ needs `ω^c`/CNF multipliers. **COMBINATOR CLOSURE (added 2026-07-01, all compiling): `𝔅p-K` DONE** (via `𝔅p-weaken` — prefixing an ignored arg of ANY type preserves goodness; K = project first arg, forget second) **and `𝔅p-S-higher` DONE** (the S diagonal `λa.φ a(γ a)` when the SHARED arg has FUNCTION type — closes by three `𝔅p-app`s reusing the same `FunArgs` entry `g`, since function args are freely reusable). **THE WALL, NOW PRECISELY PINNED (2026-07-01): the two remaining cases — ground S (`ρ=ι`, `σ` a function type) and `𝔅p-Iter` — BOTH fail for the SAME reason: the joint bound `nmul k ps ⊞ d` is AFFINE IN THE COMMON BOUND `ps` (coeff-scaling + fixed constant), and neither remaining op is.** (i) Ground S: the arg `γ x` fed to φ is FUNCTION-VALUED and varies with ground x, but `𝔅p` fixes φ's function arg as a single good majorant chosen BEFORE x is quantified ⟹ can't be `γ x` (the transformer wall; `σ=ι` sub-case DOES close via nmul-over-nmul/⊞ multiplicity arithmetic but is subsumed). (ii) `𝔅p-Iter`: from hφ get `poly-map-orbit-mult` hypothesis ⟹ orbit `L(λk iterφ a k) ≤ ω^ι[m]`, m=max(length ps)(length dφ); but `ω^ι[m]=⟦shift m(1∷[])⟧` has DEGREE m+1, exceeding the degree max(length ps)(length D) of ANY fixed `nmul K ps⊞D` — the recursor RAISES the ω-degree (the first-order tower), beyond any affine-in-ps bound. **Both ⟹ the SAME single generalization: `JBp` must bound by `⟦ T ps ⟧` for a polynomial TRANSFORMER `T:Poly→Poly` (closed under nmul,⊞,shift, AND the orbit degree-raise), with `FunArgs` carrying TRANSFORMER-valued goodness — Howard's hereditarily-majorizable functionals with a genuine transformer component.** This is the NEXT concrete step: the poly-transformer predicate `JBpT`/`𝔅pT`. K/S-higher/app/base carry over; the transformer is what unlocks BOTH S-ground and Iter at once.
  - `…PolyTransformer` (added 2026-07-01, 21 modules, no postulates, EXIT 0, in tour): the **poly-TRANSFORMER hereditary predicate** — the pinned next step from `PolyHereditary`, built. Generalizes the affine ground bound `nmul k ps ⊞ d` to an **arbitrary transformer** `T : Poly → Poly`: `JBpT ι a ps T = a ≤ ⟦ T ps ⟧`; `JointAffpT σ φ = Σ T, ∀ps JBpT`; mutual `FunArgs`/`plug`/`𝔅pT` (reuses `Maj`,`GroundType` from Hereditary). **Two payoffs.** (i) Combinator closures get SIMPLER — an abstract transformer composes by ordinary function composition, so app reindexes by `T ↦ T∘(_⊞dG)` (`JBpT-app-reindex`, replacing `JBp-shift`, no nmul/⊞ bookkeeping); PROVED `𝔅pT-app`, `𝔅pT-K`, `𝔅pT-weaken`, `𝔅pT-S-higher`, base `𝔅pT-Zero/Succ/Ω`, ground extraction `𝔅pTι-to-<ε₀` (every `T []` is a poly, hence <ε₀). (ii) **The recursor is now EXPRESSIBLE**: `𝔅pT-Iter-affine` closes Iter for an affine inner `g` (`g b ≤ ⟦nmul kg pb ⊞ dg⟧`), producing the **DEGREE-RAISING** transformer `T ps = shift (max(length ps)(length dg)+1) [1]` (= `ω^ι[m+1]`) — the bound `PolyHereditary.JBp` PROVABLY could not hold (affine shape keeps degree fixed). Orbit discharged by `poly-map-orbit-mult` + `ω^-double` (`ω^ι[m]⊕ω^ι[m] ≤ ω^ι[m]⊗ω = ω^ι[m+1]`). Helpers `ω^ι-≤-poly` (`ω^ι[m] ≤ ⟦shift m [1]⟧` via `x-≤-x⊗`+`shift-pow`), `poly-≤-ω^`. **THE ISOLATED OPEN INTERFACE:** the abstract transformer DROPS the affine shape `(kg,dg)` that `poly-map-orbit-mult` consumes, so the fully hereditary `𝔅pT-Iter` combinator (sees `g` only via `𝔅pT (ι⇒ι) g`) still needs an **affine-tracking transformer class** (coeff-scaling + constant, closed under ∘, ⊞, AND the orbit degree-raise = Howard's hereditarily-majorizable functionals w/ transformer component that retains enough shape to iterate). The two sides — abstract-transformer flexibility (app/K/S) AND affine iterability (Iter) — are both built; their JOIN is the honest remaining step. Still poly-capped at ω^ω (Poly represents only <ω^ω); full ε₀ needs CNF transformers.
  - `…MultOrbit` (added 2026-07-01, 20 modules, no postulates, EXIT 0, in tour): the **MULTIPLICATIVE orbit engine — PAST THE ω^ω CEILING**. `Orbit` is the ADDITIVE engine (fixed increment c ⟹ +c⊗ω, one factor of ω). The higher-type climb is MULTIPLICATIVE: fixed FACTOR m per step, `a(k+1) ≤ a k ⊗ m`, with m a sub-ε₀ ORDINAL (the iterated functional's body height), not a numeral. The ω-poly route (PolyAffine/PolyHereditary) could only do finite/ω^k factors → ω^ω cap; here the factor is ARBITRARY < ε₀. Proved: `pw m k ≤ ω^(m⊗ι[k])` (each power dominated by an ω-power, via inflationary `≤-ω^` (m≤ω^m) + exponent homomorphism `ω^⊗`); power sup `L(pw m) ≤ ω^(m⊗ω)` (DEFINITIONAL: `ω^(m⊗ω)=L(λk ω^(m⊗ι[k]))`, single ≤-L-mono) and `pw-sup-<-ε₀` via new `⊗-<-ε₀`; engine `orbit-mult-sup-≤ : L a ≤ a 0 ⊗ L(pw m)` (base = right unit a⊗S Z=Z⊕a; succ = ⊗-assoc), and `orbit-mult-<-ε₀ : m<ε₀ → a 0<ε₀ → L a<ε₀`. This is the multiplicative counterpart of `Orbit.orbit-sup-≤`, reaching ANY factor below ε₀ — the engine for ONE type-level of the tower, with NO polynomial ceiling. Open kernel unchanged (that a functional's per-step factor genuinely is such an m, hereditarily); the ENGINE no longer caps it. Supporting arithmetic added this session: `Epsilon0.S-<-ε₀`, `Epsilon0.ω^-<-ε₀` (ε₀ closed under succ, ω-exp); `OmegaPoly.⊗-<-ε₀` + helper `OmegaPoly.tower-sq : tower n ⊗ tower n ≤ tower(succ n)` (ε₀ closed under ordinal ⊗, via ω^⊗ + tower-double). **SIGNIFICANCE:** the ADDITIVE (Orbit, now ε₀-exponent-general via Bridge) and MULTIPLICATIVE (MultOrbit) fixed-parameter orbit engines are BOTH now unbounded-below-ε₀ — no ω^ω ceiling on the ENGINE side. The ceiling now lives ENTIRELY in the hereditary/structural side (that the per-step parameter is a fixed sub-ε₀ ordinal carried through K/S/Iter) = the open kernel (B1). The poly-fragment ceiling was an artifact of the ω-poly REPRESENTATION, not the orbit mechanism — MultOrbit sidesteps it by working directly in Brouwer-code ordinal arithmetic (ω^, ⊗, ⊕) rather than List-ℕ polys.

  - **AFFINE-TRANSFORMER ROUTE (2026-07-09, design note `dialogue-tree-height-decoupling.md`; 3 prototype modules in `DialogueTreeHeight/`, all `--safe --without-K`, EXIT 0, NOT in tour, nothing committed):** the genuine-ordinal affine-transformer predicate, clearing the two structural walls that stopped every prior route. Modules: **`AffTransformerPredicate.lagda`** — the pinned predicate `BoundT′`/`JAff′`. Generalizes Design F's `MultHereditaryFAffN.BoundT` (which COLLAPSED to `𝟙` at a function argument — the fn-middle boundary) by a real transformer clause: `BoundT′((σ₁⇒σ₂)⇒τ) T D c M = (T₁ D₁ c₁ M₁) → c₁<ε₀ → ValidMult M₁ → BoundT′(σ₁⇒σ₂) T₁ D₁ c₁ M₁ → BoundT′ τ (T T₁) D (c₁⊕c) ((M₁⊗ω)⊗M)`. Key design choices, both machine-forced: (i) NO `GoodT ι D₁` on the function argument (over-strong — the diagonal `Ta` in the `ι⇒τ` clause is arbitrary, and the arithmetic never reads `D₁`; it absorbs into `M₁⊗ω`); (ii) output constant `c₁⊕c` (NOT `c⊕c₁`) so iteration APPENDS the fixed part `cₖ₊₁=cₖ⊕c_F`, matching the additive engine (`a⊕b≤b⊕a` is FALSE on Brouwer codes: `ω⊕1=Sω≰ω=1⊕ω`). Also proved: `refl` sanity (fn-middle unfolds to the `fnmid-S-affine` composition) and `BoundT′-mono-M`. **`AffineTransformerProto.lagda`** — arithmetic core: `fnmid-μS-<-ε₀`, `nested-a-absorb` (`a⊕(a⊕e)⊗M ≤ (a⊕e)⊗(M⊗ω)` — the nested argument absorbs into the MULTIPLIER, the step Design F's fixed data couldn't take), `fnmid-S-affine`. **`IterFnProto.lagda`** — the two payoffs: `JAff′-Sfnmid` (the **function-middle S** diagonal — DEFINITIONAL: `γ a`'s own affine data is exactly what `φ`'s transformer clause consumes) and `JAff′-Iter-fn` (the **higher-type recursor**, now proved at ARBITRARY type level `σ=σ₁⇒σ₂` for `F:(σ₁⇒σ₂)⇒(σ₁⇒σ₂)` — three orbits: `D` stays `D_F` (GoodT via `GoodT-⊕`), `c` additive `→ c_g⊕(c_F⊗ω)` via `orbit-sup-≤`, `M` multiplicative with FIXED factor `ω⊗M_F<ε₀` `→ M_g⊗ω^((ω⊗M_F)⊗ω)` via `orbit-mult-sup-≤`+`pw-sup-≤`+`validMult-orbit`). This resolves **`MultOrbit`'s flagged open kernel** (that a functional's per-step factor genuinely is a fixed `m<ε₀`) FOR the affine-transformer class — the factor is READ OFF the predicate, not assumed. **KEY FINDING: no new wall at higher type levels** — the orbit data + domination (`Dk≤`/`ck≤`/`Mk≤`) are LEVEL-INDEPENDENT (the transformer recurrence doesn't see `σ₂`); only the final assembly is level-sensitive, handled by generic infrastructure added to the predicate module: `BoundT′-mono-D`/`-mono-c` (companions to `-mono-M`), pointwise sup `sup-𝕋`, and the sup-lemma `BoundT′-L` (all `--safe`, EXIT 0). So the general Iter = same orbits + 3 mono lemmas bringing each iterate's data to fixed `(D∞,c∞,M∞)` + `BoundT′-L`. **COMBINATOR CASES (2026-07-09, `AffCombinators.lagda`, EXIT 0):** application `JAff′-app-ι` (ground arg absorbed into `D` by value — needs only `GoodT ι a`, NOT its bound; note `GoodT ι` is a PAIR `<ε₀`-preservation × monotonicity, so a ground arg's monotonicity is a real hypothesis carried by the majorant invariant) and `JAff′-app-fn` (function arg → transformer clause, multiplier orbited); base leaves `JAff′-id`/`JAff′-Succ`/`JAff′-add`/`JAff′-const` (all-ground, port verbatim from `MultHereditaryFAffN`); `JAff′-S-fnmid` (function-middle S at ARBITRARY result τ — generalizes `JAff′-Sfnmid`); dead-arg insert `JAff′-insert-ι` (via `BoundT′-mono-D`); ground `JAff′-K` (`ι⇒ι⇒ι`, ported). **TWO-LAYER BRIDGE + ASSEMBLY (2026-07-09, `AffFundamental.lagda`, EXIT 0):** the connection between the two existing predicate layers is now explicit. Layer 1 (`Majorant.R`): `R σ ⟦t⟧ (μ t)` ⟹ `height⟦t⟧ ≤ μ t` (first-order proven). Layer 2 (`MultHereditaryF`): `Good σ φ = Σ T:𝕋σ, GoodT σ T × (∀w, Bnd σ w T φ)` bounds a majorant `φ:Maj σ` by a transformer `T:𝕋σ`; `Bnd ι w T a = a≤T w`; `Good-ι-to-<ε₀`. **My `JAff′` is exactly the affine structure of that same `T`.** Combined predicate `GoodJ σ φ = Σ T:𝕋σ, GoodT σ T × JAff′ σ T × (∀w, Bnd σ w T φ)`. PROVED: `GoodJ-ι-to-<ε₀` (reads `<ε₀` off JAff′'s `(D Z⊕c)⊗M`, sharpening `Good-ι-to-<ε₀`); base `GoodJ-Zero/Succ/Ω` (existing `Good-*` transformer + matching `JAff′-ι-id`/`JAff′-id`/`JAff′-Succ` — the transformer shapes coincide, NO adapter needed); **`GoodJ-app` at ALL types** (Bnd half = `Good-app`'s `TF TG`, JAff′ half = `JAff′-app-{ι,fn}`, split on arg type). **KEY STRUCTURAL FINDING on where the open kernel now lives:** the `Bnd`/majorant half is ALREADY GENERAL for app/K/S/S-diag (`MultHereditaryF.Good-app`/`Good-K`/`Good-S`/`Good-S-diag` all at arbitrary types), but **`Good-Iter` is GROUND-ONLY** (`Tg:ι⇒ι`). My `JAff′` half has: app (all types ✓), Iter-fn (ALL levels ✓ — the previously-open `MultOrbit` kernel), S-fnmid (function-middle/ground-diagonal), K/base (ground). So the genuine remaining OPEN piece is the **`Bnd`/majorant half of the HIGHER-ORDER recursor** — a higher-order `Good-Iter` + a higher-order `μ-Iter` (Majorant has only ground `μ-Iter`). The affine-transformer route resolved the ARITHMETIC-layer obstruction (per-step multiplicative factor, `IterFnProto.JAff′-Iter-fn`); the "iterators increase strength with type level" phenomenon now sits ISOLATED at the majorant/`Bnd` layer (higher-order `Good-Iter`/`μ-Iter`), plus the routine general-type `JAff′-K`/`JAff′-S`. This is the precise, narrowed frontier. **Conjecture still OPEN.**

  - **KEY IDENTITY + SQUARING KERNEL (2026-07-09):** `JAff′ (ι⇒ι) Tg ⟺ MultHereditaryFAff.AffBounded Tg` (definitionally identical mod `⊕-assoc`) — so `JAff′` is the **hereditary, all-type-levels extension of the existing (ι⇒ι-only) inner-affine class**, and the ground affine recursor is ALREADY discharged there (`orbit-good-aff`, multiplier `ω^((ω⊗M)⊗ω)`). Why the affine route can't finish: its transformer clause is **LINEAR in the argument multiplier** (output `(M₁⊗ω)⊗M`), but System T generates **self-composition** `Iter g₀ (λg.g∘g)` (= `g₀` composed `2ⁿ` times), and composition **squares** the multiplier (`M₁↦M₁⊗M₁`), which no linear `(M₁⊗ω)⊗M` dominates uniformly. Squaring is genuinely outside `JAff′`. **BUT the squaring orbit is still `<ε₀`** (`MultSquareOrbit.sq-orbit-<-ε₀`: `iter sq w k ≤ ω^(b⊗ι[2ᵏ])`, sup `ω^(b⊗ω)` — the exponent doubles AFFINELY, staying inside one ω-power, via the homomorphism `ω^⊗`; contrast `w↦ω^w` which reaches ε₀ by moving the code up a tower level). The barrier that separates them is structural: in the multiplier-dominated bound `(b⊕c)⊗M`, the multiplier `M` is **`w`-independent** — squaring keeps it so (`M₁²` is a fixed constant), exponentiation needs a `w`-dependent multiplier. **NEW MACHINE-CHECKED RESULT `MDomSquareOrbit.lagda` (EXIT 0, `--safe --without-K`, not in tour, nothing committed): the ground self-composition orbit is `MDom`.** `MDom-∘` (existing, `MultDominated`) already squares the multiplier / doubles the constant for `φ∘φ` via the `crux` absorption; iterating it (`MDom-iter-sq hφ k = MDom-∘ IH IH`) gives the k-th self-composite with multiplier `iter sq M k` and constant `iter double c k` (proved by `Mk-eq`/`ck-eq`, `ap(λz.z⊗z)`/`ap double` — η on Σ). `MDom-sq-orbit`: `λb.L(λk. iter Fsq φ k b)` is `MDom` at multiplier `ω^(S b_M⊗ω)` (multiplier orbit dominated via `sq-orbit-≤`, each `M_k≤ω^(b_M⊗ω)` since `ι[2ᵏ]≤ω`) and constant `L(iter double c)` (`double-orbit-<-ε₀`); `MDom-sq-orbit-<-ε₀` gives `<ε₀`. **This closes the squaring recursor kernel AT GROUND LEVEL** — the multiplier orbit read off `MDom-∘`, discharged by `MultSquareOrbit` in the exact role `MultOrbit` plays for the plain recursor. Obligations 1–2 of the squaring plan (ground absorption + the `ValidMult` squaring-orbit) DISCHARGED; the paper hand-checks (self-comp squares `M`; `w`-independence is the barrier; higher-type degree blow-up `2^(2ᵏ)` still sups to `ω^(b⊗ω)` since all finite degrees sup to `ω`; each type level stacks one `ω^(–)` ⟹ tower height = type level, the CSL2011 phenomenon) held up. **REMAINING OPEN (obligation 3): the HEREDITARY version** — self-composing a *functional* (each type level adds one `ω^(–)`), packaged as a closed transformer predicate, + its fundamental theorem. That is the "no closed class is known" residual (MultHereditaryFAff's own closing note). **SHARPENED DESIGN (2026-07-09, arithmetic all verified-present):** the ground layer is COMPLETE — `MDom` (ValidMult multiplier = positive, doubling-absorbing, `<ε₀`) is closed under composition (`MDom-∘`, squares mult), plain recursor (`MDom-orbit`), S-diagonal (`MDom2-diag`), AND now the squaring recursor (`MDom-sq-orbit`); `dbl-ω^` shows `ω^(X⊗ω)` always absorbs doubling, so post-orbit multipliers stay valid. The hereditary lift has a clean candidate: **the `ω^(–)` functor from the affine class `JAff′` to the squaring class.** Write the multiplier as `ω^β`; then composition `ω^{β₁}⊗ω^{β₂} = ω^{β₁⊕β₂}` (via `OmegaPoly.ω^⊗`) is EXPONENT-ADDITION, and self-composition `ω^β⊗ω^β = ω^{β⊕β} = ω^{β⊗ι[2]}` (the `MultSquareOrbit.eq` chain: `ω^⊗`+`⊗-left-distrib`+`ι-+-homo`) is EXPONENT-×2 — BOTH are AFFINE maps on the exponent β. So the multiplier-action of every relevant functional (identity `κ M=M`; successor-ish `κ M=M⊗ω=ω^{β⊕1}`; self-comp `κ M=M⊗M=ω^{β⊗ι[2]}`) is AFFINE-ON-EXPONENT, and the affine-on-exponent class is closed under composition (affine∘affine) and the self-comp orbit (exponent orbit `β↦β⊗ω`, `Affine.double-orbit`), staying `<ε₀`. **So obligation 3 = reinterpret the existing `JAff′` affine transformer machinery with its `M`-slot as the EXPONENT β, wrapped in `ω^(–)`** — the affine structure on β is already built; `ω^(–)` transports it (additive→multiplicative) via the homomorphism. This resolves the "fixed M₁-degree" impossibility (a single clause can't be both linear for app/base and quadratic for self-comp) by moving to the exponent, where both are affine. RISK/RESIDUAL: (a) confirm the actual System T functionals' actions stay affine-on-exponent hereditarily (base/K/S/app + the self-comp recursor); (b) the `ValidMult(ω^β)` doubling-absorption bookkeeping (needs β≥1, limit-form — automatic post-orbit via `dbl-ω^`, needs tracking at base). First concrete Agda target: an `ExpDom`/`JSq` predicate with multiplier `ω^β`, `ExpDom-∘` (exponent `⊕`) + `ExpDom-sq-orbit` (exponent `β⊗ω`) as the keystone closures, then the hereditary clause + fundamental theorem. This is a DESIGN FORK worth Escardó's eyes (the `ω^(–)` functor is elegant but its hereditary closure is the unproven part). **GROUND KEYSTONE MACHINE-CHECKED 2026-07-09 (`ExpDom.lagda`, EXIT 0, `--safe --without-K`, not in tour, nothing committed):** `ExpDom φ = Σ β, (SZ≤β)×(β<ε₀)×ValidMult(ω^β) × Σ c, (c<ε₀)×(∀b. φ b ≤ (b⊕c)⊗ω^β)` — `MDom` with multiplier pinned to `ω^β`, exponent explicit. `ExpDom→MDom` trivial; `ExpDom-app` = `MDom-app∘`. **`ExpDom-∘`: exponent ADDS** — `MDom-∘` gives opaque mult `ω^βψ⊗ω^βφ`, `transport` by `ω^⊗ βψ βφ` rewrites to `ω^(βψ⊕βφ)` (both the `ValidMult` and the bound); output `SZ≤` via `⊕-increasing-left`, `<ε₀` via `⊕-<-ε₀`. **`ExpDom-sq-orbit`: exponent DOUBLING-ORBITS to `β⊗ω`** — reuses `MDomSquareOrbit.MDom-iter-sq` (k-th self-composite), local `Mk-eq`/`ck-eq`, multiplier dominated by `sq-orbit-≤ β (ω^β) (≤-refl (ω^β))` (each `iter sq (ω^β) k ≤ ω^(β⊗ι[2ᵏ]) ≤ ω^(β⊗ω)` — CLEAN exponent `β`, no `w-≤-ω^` detour since the input multiplier is literally `ω^β`); output `ValidMult(ω^(β⊗ω))` via `dbl-ω^ β β+`, constant `L(iter double c)` via `double-orbit-<-ε₀`. So the two-tier arithmetic is VERIFIED: the exponent `β` lives in the affine world, closed under composition (`⊕`) and self-comp orbit (`⊗ω`), with `ω^(–)` transporting additive→multiplicative. This is the `ω^(–)` functor's core mechanism, machine-checked at ground. **REMAINING (still the open part): the HEREDITARY clause** — lift `ExpDom` to a transformer predicate on `𝕋(σ)` whose function-type clause carries the exponent-`β` affine-transformer data (reinterpret `JAff′`'s `M`-slot as `β`), + the fundamental theorem showing System T functionals stay affine-on-exponent through K/S/app/self-comp-recursor. That closure is the unproven "no closed class is known" residual. **FALSIFICATION TEST RUN 2026-07-09 (asked to check if K/S and the accumulator kill the `ω^(–)` route; result: NOT falsified, but two corrections + a red flag):** (1) **The turn-1 "accumulator D explodes under self-composition" fear was WRONG — a conflation of the oracle input with the value argument.** In the transformer/`BoundT′` layer the accumulator `D` is a function of the ORACLE INPUT `w` and is threaded UNCHANGED through composition (every application evaluates `D` at the same `w`; the function-argument's own `D₁` is dropped by the clause). `D` is never fed the growing multiplier-carrying value, so it cannot explode. The value-threading that DOES square the multiplier is a GROUND phenomenon (`MDom`/`MDomSquareOrbit`), where the accumulator is the IDENTITY and absorbs via `crux`. Ground self-comp squares the multiplier; functional self-comp is LINEAR (clause reads only the arg's multiplier `M₁ ↦ (M₁⊗ω)⊗M`, dropping `D₁`) and is already handled by `JAff′-Iter-fn`. Two different compositions, both handled. (2) **The actual mechanism the functor supplies, MACHINE-CHECKED (`ExpClauseComparability.lagda`, EXIT 0):** a single clause must bound both the LINEAR outputs of K/S/app and the SQUARING output of self-comp. In the MULTIPLIER world these are incomparable — `mult-coeff-needs-hyp : ω≤M₁ → (M₁⊗ω)⊗M ≤ (M₁⊗M₁)⊗M` shows domination REQUIRES `ω≤M₁` (false for small `M₁`) = the wall. In the EXPONENT world it is UNCONDITIONAL — `exp-coeff-dominates : (β₁ e) → ω^(β₁⊕e) ≤ ω^((β₁⊗ι[2])⊕e)` (via `β₁≤β₁⊗ι[2]`, `x-≤-x⊗`, NO hypothesis). So a coefficient-≥2 affine-on-exponent clause bounds every lower-degree output uniformly. That is precisely why the functor removes the single-clause wall, and it is sound (finite-⊗ monotonicity). The coefficient-level closure of K/S/app (`⊕`/`⊗` combos) and the recursor (squaring orbit) all reduce to already-checked lemmas (`⊕-<-ε₀`, `⊗-<-ε₀`, `sq-orbit-<-ε₀`). (3) **RED FLAG (honest):** the paper analysis keeps arriving at "it closes," which for a known-open conjecture means the residual is in what I have NOT built. Prime suspect, now sharpened: NOT the accumulator (cleared), but **the higher-order recursor MAJORANT** — `Majorant.μ-Iter` and `Good-Iter` exist only at GROUND type; iterating a functional (recursor at a level-2 type) has NO majorant defined. The engine for its orbit exists (functional self-comp orbit = squaring orbit of the fixed factor `ω⊗M_G`, `MDomSquareOrbit`-shaped), but the majorant CONSTRUCTION + the proof that the pointwise-sup majorant lies in the class are unbuilt — and that is exactly where a hidden unsoundness (or genuine wall) would finally surface. Also unverified: soundness of the clause DROPPING the function-argument's `D₁` for the full theorem (only base+app cases of `AffFundamental` typecheck). NEXT: pin the hereditary squaring clause (coefficient-affine-on-exponent) and BUILD the higher-order `μ-Iter`/`Good-Iter` case — the one place the route can still die. **Conjecture still OPEN.**

**Re-examination of the open core (2026-06-29 session):**
- **Equivalence R3 ⟺ conjecture confirmed, both directions, not peelable.** The realizing term is `t = λα. Iter (F α) (X α) (α 0) : (ι⇒ι)⇒ι`, giving `dialogue-tree t = iter' f x (generic(η0))` and, via the machine-checked `height-iter'`, `height(dialogue-tree t) = sup_k (height(fᵏx)+1)`. So `< ε₀ ⟺ sup_k height(fᵏx) < ε₀` (= R3). Note `generic(η0)=β η 0` has height 1 but unbounded magnitude.
- **No pure-height sidestep exists (structural, not just the Part IV majorant artifact):** the per-step height increment `height(f y) ≤ height(y) ⊕ d_f` has `d_f` depending on the *magnitude* of `y` (e.g. `ω^{value}` when `y` is a count), independent of `height(y)`; and magnitude ⊥ height (rank-ω/value-0 witness). So tracking magnitude separately is *forced*; a finite/infinite bit is insufficient (need the actual growing number).
- **A/B decomposition (the real narrowing of the open core):** R3 ⟸ (A) orbit *magnitude* `V_k(b)` grows at rate `F_β`, fixed `β<ε₀` — this is the classical System-T-values-are-`<ε₀`-recursive theorem (Tait/Schwichtenberg/Howard), **legitimately citable** because `fᵏx = iter f x k` is one System T term in `k`; PLUS (B) the dialogue-native bridge: per-step *height* increment `≤ ω^{(local count magnitude)}`, so `sup_k height ≤ ω^β·ω < ε₀`. The Conditional.lagda "not citable from Howard" remark conflates *one tree* (rank ⊥ value, true) with the *orbit* (height-growth governed by magnitude-growth, which Howard bounds). **Open residual is now (B) alone**, not a from-scratch ordinal analysis of T.

To check a module: `agda EffectfulForcing/DialogueTreeHeight/<Leaf>.lagda` from `source/` (e.g. `<Leaf>` = `Index`, `Orbit`, `Majorant`, `Affine`, `Classical`, …). See [[honesty-attribution-elegance]] for how the user wants this handled (don't overclaim; the open lemma is open).
